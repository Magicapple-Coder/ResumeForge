# 变更记录

所有值得关注的项目变化都会记录在此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## Unreleased

### Added

- **面试深挖（新模块）**：入口在「事实台账」页的「拿去深挖」按钮（路径 `/claims/drill`）。它按台账里**已确认**的条目逐条做压力测试，回答的是"这条主张我到底讲不讲得清、哪里还站不住"。
  三条刻意的设计：**提问前先锁定评分契约**——每道题在生成时就把"要验证什么、必须听到哪些证据、什么情况该追问、什么情况可以结束"写进库里，之后**不因回答得流利而放宽**；这是防"事后改标准"的唯一办法，也是它与"给个分数"最根本的区别。**用证据状态代替分数**——`已验证 / 部分验证 / 未验证 / 存在矛盾 / 未覆盖`，界面与接口都不出现任何总分（有测试钉住"不出现 `N 分`"）。**不做推断**——模型只能依据用户真实说过的内容给状态，说"这次算过了"却**列不出任何证据**时一律不判成「已验证」。
  逐轮追问由契约驱动：追问类型按缺口选（背景 / 职责 / 结构 / 实现 / 决策 / 替代方案 / 失败与排查 / 指标口径 / 代价 / 复盘），**不机械遍历**。结束后出一份「该去补什么」的清单：看清哪些讲得清、哪些还站不住、行动项落到**补事实 / 补知识 / 降表述**三选一，另有**复练队列**（只收部分验证 / 未验证 / 存在矛盾的主张）。复练**不重复原题**——换一个角度出变体题 / 反事实题 / 故障题 / 证据题 / 压缩表达，因为"把上次的答案背一遍"不算会了。
  两个反馈策略：**真实模拟**（默认）每题后不念判定，只在结束时统一复盘——念了会让人按判分标准答题，而不像真面试；**训练模式**每题后立刻给一段简短反馈，判定本身在两种模式下都如实记录。**求职助手**新增 `list_drill_sessions` / `get_drill_report` 两个工具可以读记录帮你复盘，但**开不了新的深挖**（标准要先按主张锁定，且需要你自己逐轮作答）。
- 新增 Alembic 迁移 `0014_interview_drill`：新建 `drill_session` / `drill_contract`（一道题的评分契约，提问前锁定）/ `drill_turn`（一轮问答）三张表（只加表、幂等、带完整 `downgrade`）。契约与轮次随会话 `CASCADE` 删除；台账条目被删掉后契约仍要可读（`SET NULL` + 快照标题），因为复盘要看"当时问了什么、判定成什么"。
- **简历版面诊断与「自动一页」（新能力）**：简历预览上方新增「版面诊断」面板，把"看起来空/挤"变成可测的数字——单页正文占用 82%~96% 为合适、超过 96% 偏高、低于 82% 偏空；多页时另看**末页**（低于 70% 说明最后一张几乎是空的）。它按**固定顺序**给建议：**重排 → 结构 → 间距/字号 → 精简内容 → 扩页**。顺序本身就是规则：先让人调间距、调不好再让人删内容等于让人白改一轮；反过来先说"精简内容"，用户又可能删掉本来放得下的经历。溢出的简历上出现「自动一页」按钮，它**逐档试、够放下就停**而不是一步把字号缩到最小：按「页边距 → 区块间距 → 行高 → 字号」共十几档，每试一档只是往预览里注入一段 CSS 再量一次（没有网络往返）。字号下限按**绝对像素**算——不低于 12px（≈9pt）——而不是按比例：同一个系数在小字号档上会缩得更狠，只盯比例会让小字号档一路缩到 10px 以下。收到底仍放不下时如实说明"只能精简内容或增加页数"，绝不假装成功。
- **按简历的版式覆盖**：新增 `resume_record.format_config`（迁移 `0013_resume_format_config`，只加列）。「自动一页」的方案写在这里而不是去改具名格式模板——后者会让所有引用它的简历一起变，而自动一页的结果只对当前这份内容成立。预览、导出 HTML、服务端 PDF 三处走**同一个解析函数**，避免出现"预览收紧了、导出的 PDF 没变"这种只有下载之后才发现的不一致。
- **实测高度由浏览器提供，规则全在后端**：`POST /api/resumes/{id}/layout/analyze` 接收前端量到的两个高度，返回诊断结论与逐档方案。规则放在后端是因为它纯是判断（多少算满、先动哪个旋钮、缩到哪为止），能被逐个钉住、也能被助手与导出复用；测量只在浏览器里做，因为版面只有真正排版之后才存在。
- **求职进度（新模块）**：侧栏在「投递台」之后新增「求职进度」。投递台记录的是**动作**（这个岗位投出去了、失败了），这一页记录的是**结果**（对方走到哪一步了）——一家公司一个岗位一条记录，走「已投递 → 筛选中 → 测评/笔试 → 面试 → Offer」，另有「已结束」「待确认」两个不参与推进的状态。四条与既有功能的接合点：**投递成功的岗位会自动记一条「已投递」**（不必手工再录一遍，用条目上的快照字段，岗位被删掉也记得住）；**把邮件/短信/站内通知粘进来就能识别进度**（也支持截图与 pdf/docx，复用岗位识别那条链路），识别结果**先预览再确认**——面板上每条写明"会新增 / 会更新 / 没变化"和原因；**合并按「公司 + 岗位」**，同一组合只有一条记录；**导出 CSV / JSON**（CSV 带 BOM，Excel 打开不乱码）。两条硬规则写在 `models/tracker.py` 并在界面与接口共用同一份：**状态只能前进，拒信除外**（乱序粘贴三个月前的「申请已收到」不会把已经在面试的进度打回去），以及**普通自动回执只能算「已投递」**，不能从「感谢投递」推断出面试或 Offer——识别不出来就是「待确认」，由用户自己判断。**识别接口只算不写**：`/parse` 返回合并预览，`/apply` 才写入，且两者执行的是同一份合并计划（有测试钉住预览与执行一致）。
- 新增 Alembic 迁移 `0013_resume_format_config`：给 `resume_record` 加一列 `format_config`（只加列、幂等、带完整 `downgrade`）。加列不改表集合，所以"旧备份仍可导入"依然成立；旧记录升级后该列是空对象，渲染结果与升级前一致。迁移对"表不存在"的历史库做跳过而不是报错——迁移链也会被用在只有部分业务表的老库上。
- 新增 Alembic 迁移 `0012_application_tracker`：新建 `application_track` 一张表（只加表、幂等、带完整 `downgrade`）。同一公司同一岗位有唯一约束——"漏斗只有一个口径"由结构兜住。岗位/简历用 `SET NULL`，记录不会因为岗位被删而消失。
- **事实台账（新模块）**：侧栏在「我的资料」之后新增「事实台账」。它解决的是"简历上这句话站不站得住"——个人资料记录**发生过什么**，台账记录一条条**可对外表达的主张**，每条写清四件事：原始事实（照实写、不包装）、简历表述（准备怎么写）、承担程度（参与 / 负责模块 / 主导方案或交付 / 项目负责人）与个人边界（团队做了什么、你做了什么）。**核实状态决定用途**而不是决定好坏：`已确认` 才能进正式简历；`待确认` 只能进草稿且必须带【待补】占位符；`已过期` 需先更新；`不采用` 保留原因供复盘但不对外。三条与既有功能的接合点：**生成简历时**只有已确认的条目会作为事实基线进入提示词（未确认的说法会被列成"禁止使用"，且承担程度与个人边界会约束表述强度）；**导出时**正文里还留着【待补】之类标记的简历会被拦下并指出具体位置——弹窗里可以选「仍要导出草稿」（接口层的 `allow_incomplete=true`）四种格式与浏览器打印都适用，拦下不等于堵死；**求职助手**新增 `list_claims` / `get_claim` / `create_claim` / `update_claim` 四个工具。**助手不能替用户确认**：写入路径一律不接受核实状态，新建的条目都是「待确认」——让模型把自己新建的条目标成"已确认"等于让它自己给自己发通行证。台账为空时整条生成链路的行为与以前**逐字节相同**（有测试钉住）。
- **未完成标记统一为可检索的约定**：`【待补…】`、`【待确认】`、`【待补充】`、`【待核实】` 四种写法在模型、接口与界面三处用同一份取值；界面里这类标记会高亮，因为同一条标记同时意味着"这条还没核实"和"导出终稿会被拦下"。**只有标记本身会被拦**，正文里正常出现的"待补充说明"之类不会被误伤（有测试钉住）。
- 新增 Alembic 迁移 `0011_claim_ledger`：新建 `claim_record` 一张表（只加表、幂等、带完整 `downgrade`）。与 `0010` 同样的理由——`inspect_archive` 会先把旧备份迁到 head 再校验表集合，所以停在更早 revision 的备份会自动补上这张表，不会因为"缺少数据表"被拒收。台账**不建外键**：它记录的是"关于某段经历的主张"，主体用 `subject` 文本关联，这样资料库怎么改都不会连带删掉用户已经整理好的事实基线。


- **设置里可以配置联网搜索了**：新增「联网搜索」卡片——搜索来源（Bing / DuckDuckGo / 自建 SearXNG）、SearXNG 实例地址、**抓取正文的条数**（0~3）与每次返回条数（1~15）。这几项一直存在于后端、接口也齐全（`GET/PUT /api/settings/search`），但此前**没有任何界面入口**，于是「抓取正文」永远是默认的 0，助手能拿到的只有搜索摘要。助手页那个开关是"这一次要不要联网"，这张卡片配的是"联网时怎么搜"。
- **大模型配置支持 Anthropic 原生协议**：快速预设新增「Claude（Anthropic 原生协议）」，一次填好地址、模型名与接口协议；选预设时**协议跟着一起切**，从 Claude 切回 DeepSeek 也会切回 OpenAI 兼容。
- `scripts/tests/Test-Uninstall-ResumeForge.ps1`：卸载器的回归测试（默认保留数据、`-Purge` 删除数据、拒绝在非项目目录运行、取消时不删任何东西）。此前这个脚本既没有测试也没有任何文档引用。
- **自动投递中心的数据底座**：为"岗位自动采集 + 匹配度分析 + 全自动投递"新增 4 张表——`job_match_analysis`（一个岗位保留**最近一次**匹配结论，`job_id` 唯一即覆盖）、`apply_queue_item`（用户显式勾选的待投递队列，同一岗位唯一）、`apply_task`（一次执行批次，采集与投递共用，靠 `kind` 区分）、`apply_task_item`（批次内每个岗位的执行条目，也就是投递记录）。岗位/简历被删除后记录仍可读（`SET NULL` + 快照字段），批次内的条目随批次 `CASCADE` 删除。新增 Alembic 迁移 `0010_apply_center`（只加表、幂等、带完整 `downgrade`）：**只加表**这一点是"旧备份仍可导入"成立的前提——`inspect_archive` 会先把旧备份迁到 head 再校验表集合，因此停在 `0009` 的旧备份被自动补上这 4 张表，不会因为"缺少数据表"被拒收（已补回归测试 `test_backup_apply_tables.py` 钉住）。匹配状态（`matched`/`expression_gap`/`evidence_insufficient`/`real_gap`/`to_confirm`）与准入闸门映射（`allow`/`block`/`needs_confirm`）只写在 `models/apply.py` 一处，前端镜像同一份取值，避免两处判断漂移。**注意**：这 4 张表只在含该迁移的版本中存在，降级到 `0009` 会丢失投递记录（降级前请另行备份）。
- **投递专用浏览器桥接层与站点适配器框架**：这是本功能第一次让应用访问招聘网站，因此把最不可测的两件事——**驱动真实浏览器**（Chrome DevTools Protocol）与**理解陌生表单**——封在两个可离线测试的边界之后。`browser/cdp_client.py` 用既有 `httpx` 访问 `/json/version`、`/json/new` 等 HTTP 端点、用 WebSocket 发 CDP 命令，**传输层可注入**（沿用 `create_provider(transport=httpx.MockTransport(...))` 的离线注入点）；`browser/browser_manager.py` 用 `subprocess.Popen(list, shell=False)` 拉起"投递专用浏览器"——**独立 `user-data-dir`**（Chrome 136+ 禁止在默认用户目录开远程调试）、优先 Chrome 未装则回退 Edge、只关后端自己拉起的那个进程（持有进程句柄，不按 PID 满世界找，避免误杀用户自己的浏览器）。站点适配器用注册表模式，首期只有 `BOSS直聘`（`sites/boss.py`），选择器集中为常量；**"选择器/入口失效"是一等的失败类型**（`selector_invalid`），失败时带可操作诊断（当前 URL / 页面标题 / 匹配控件数 / 期望控件描述）直接展示给用户。**局限**：BOSS 直聘的真实 DOM 与投递流程**无法联调验证**，首期不保证一次投递成功，需要用户首次真机试用后回传诊断信息来收敛选择器；应用**不做任何"绕过验证码 / 代替登录"的实现**。
- **投递台可以自选浏览器**：投递配置新增「投递台使用的浏览器」——`自动（优先 Chrome）` / `Chrome` / `Edge` / `自定义路径`（含路径输入框）。此前 `BrowserManager._locate_browser()` 写死"优先 Chrome、未装回退 Edge"，用户无法干预；更关键的是**明确选了 Chrome 却启动 Edge 是比报错更坏的行为**，因此指定浏览器后只找对应的那个，找不到就给可读的中文提示（说清是哪个没找到、让他去装或改用自定义路径），**绝不静默回退**；自定义路径必须是真实存在的文件。配置变更**真正生效**：浏览器管理器单例的缓存键从"只按端口"扩成"端口 + 浏览器选择 + 自定义路径"，改完不重启也生效，切换时先关掉旧实例。状态回报补上人类可读的浏览器名（`BrowserStatusOut.browser_name`），界面不再只甩一串路径。
- **界面标明当前招聘网站，且新增站点只改后端**：显式引入"当前站点"概念——配置新增 `site_key`（默认取注册表里第一个站点），`default_entry_url()` 等依赖站点的逻辑都改为按"当前站点"取；新增 `GET /api/apply/sites` 返回**已注册站点列表**（`key` / 展示名 / 主机名 / 入口地址 / 是否支持采集与投递）与**当前选中项**；投递台顶部标注「当前招聘网站：BOSS直聘」并如实说明目前仅支持这一个、后续会陆续增加。**硬性约束**：站点名与站点清单全部来自接口与类型，前端组件里不写死任何站点名——以后新增招聘网站只在后端注册表 `register` 一行，界面自动多出选项，前端零改动（`SiteOptionOut` / `SiteListOut` 是唯一数据来源）。
- **采集改走「网络响应优先、DOM 兜底」**：岗位列表与详情本来就是 XHR 渲染出来的，直接读接口响应拿到的是**同一份数据**，但字段更全、且不受渲染时序与字体反爬影响。最大的实际差别在详情页：接口带回**完整 JD**，而 DOM 只渲染出摘要——采集器把接口的描述写进 `Job.description`，于是**岗位匹配读到的是全文**；薪资也按站点自己的口径格式化（`20-40K` / `300-800元/天` / `20-40K·16薪`），与用户在页面上看到的那一条对得上。`browser/network_capture.py` 只做纯解析（吃 CDP 事件、吐结构化数据，不碰浏览器），所以能离线测。
  几条刻意的取舍，以及几个**只有真机时序才暴露得出来**的坑（离线测试用的假客户端默认"事件已躺在缓冲区里"，正好会把它们全遮住，所以假客户端也改成按真实时序推送）：
  **订阅必须早于导航**——列表接口的响应是在页面加载过程中回来的，导航之后再订阅会漏掉第一批；**取走与停止必须晚于等待结束**——`Page.navigate` 只是**发起**导航，返回时页面还没开始跑 XHR，导航完就取走并停止订阅等于永远拦不到东西，而且"取走"还要发生在"停止"之前（停止会连缓冲区一起清掉，顺序反了同样白拦）；**接口这条路一律不抛异常**——路径改了、被风控换成了验证页、结构不是预期的那一种，都只是安静地**退回 DOM**，不让一次采集失败（"选择器失效"仍然是一等的失败类型，兜底不等于把问题咽下去：只有页面**明确**无结果才返回空页）；**判断顺序反过来**——接口拿到了岗位就说明这次搜索有结果，此时 DOM 匹配 0 个只说明页面没渲染出来，所以"用不用接口结果"的判断放在"DOM 说无结果"之前，否则会把"抓到了"说成"没搜到"；**事件订阅是可选能力**——`CdpClient` 给它配的是"不支持"的空实现而不是抽象方法，于是不支持订阅的浏览器桥接实现照样能用，代价是**忘了在包装层转发它不会报错**（`StopAwareCdpClient` 曾经就是这样），两者叠加的净效果是：网络优先在生产里从未生效，而离线测试全绿。现在这几条时序与透传都有测试直接钉住。
  **局限**：接口路径与响应结构没有官方保证，仍要靠真机试用收敛；接口多带出来的经验/学历/技能标签目前**还没有下游**（存在 `SearchResult.extra`，`Job` 模型没有对应的列），等接匹配度分析与 HR 活跃过滤时再取。
- **投递台与匹配度分析界面（前端）**：侧栏在「简历中心」之后新增「投递台」。一页四块——**投递队列**（上移/下移调顺序、逐条改简历与招呼语、勾选后只投选中的或整队列投递、显式「开始投递」）、**自动采集**（关键词 + 城市 + 薪资/经验/学历，无法映射到站点查询参数时明确标出「未生效」，绝不静默忽略）、**执行进度**（逐岗位状态机「待投递→进行中→成功/失败/已跳过」、用「已处理 N/M」而非百分比、熔断时顶部醒目警示条、暂停/继续/停止按钮常驻）与**投递记录**（按结果检索、失败条目单独重投）。岗位详情新增「匹配度分析」入口（与只读招聘原文的「岗位需求解读」并列，并用 Tooltip 讲清区别）和「加入投递台」入口——命中「证据不足/待确认」需你逐条确认，命中「真实缺口」显示为「不投」并说明原因（入队接口 409 的结构化原因经 `QueueConflictError` 上抛，界面据此弹出对应确认）。准入结论只读后端返回的 `admission`，前端不自行再判一次；界面不渲染任何百分比/评分，进度用**轮询**（1.5s，任务进入终态自动停）而非 SSE。**不引入任何新的前端运行时依赖**（仍是 antd 5 + React Router，5 个运行时依赖不变）。

### Fixed

- **点「开始采集」时浏览器里明明加载出了岗位，应用却说"共新增 0 个岗位"**（用户实测反馈）：`collect_search` / `fetch_job_detail` / `open_apply` 在 `new_tab(url)` 之后**立刻**执行采集脚本——`new_tab` 只是让浏览器**开始**打开地址，返回时文档还是空的，`querySelectorAll` 匹配 0 个元素，脚本于是返回空列表、`has_next` 为假，采集循环第一页就 `break`，`collected = 0`。这个缺陷最恶劣的地方不是"没采到"，而是它把"什么都没做"伪装成"采集完成"，用户完全无法察觉自己一个岗位都没拿到。修复：新增"等待页面可用于采集"的通用原语 `browser/page_ready.py`（导航后轮询直到目标选择器匹配数 > 0，或出现登录失效 / 验证码时**立刻**失败，或超时失败；等待可被用户点的"停止"打断——轮询步经过 CDP 客户端既有的检查点，不用一个长 `time.sleep` 把停止信号堵在外面），采集与详情两个站点方法复用它；导航改为**复用同一标签页**（`Page.navigate`），不再每页 / 每岗位各新开一个标签（按 20 个岗位算会堆出 20+ 个标签页）。
- **采集的 0 结果不再无条件当成功**：区分"关键词真的搜不到"（页面明确呈现无结果状态 → 返回空页是合法的）与"没抓到"（**必须失败**，并按项目既有约定抛出带可操作诊断的失败：当前 URL / 页面标题 / 匹配到的控件数 / 期望控件的候选描述）。**关键取舍**：等待的**唯一失败时限是超时**，**不**按 `readyState === 'complete'` 提前判失败——`readyState` 只代表文档与静态资源加载完毕，**不代表 SPA 该渲染的内容已经渲染完**（BOSS 直聘的岗位卡片是 `complete` 之后由 XHR 异步渲染的，首屏稍慢就会被误判成"结构变化"）；超时后再按证据给不同文案：没切换到目标地址 / 已加载但找不到卡片（可能结构变化，可反馈给维护者）/ 尚未加载完（网络慢或拦截，稍后重试）。采集过程中抛出的 `SiteFailure` 现在会被任务运行器接住并让任务**失败**（message 带上诊断），而不是像以前那样被顶层兜底成一句"内部错误"、或被当成"完成"；真正的 0 则在完成态明确说明"没有找到匹配的岗位（关键词或城市可能太窄）"，整页都是重复岗位时则明确说"都已存在、没有新增"。几种收尾在界面上一眼可辨。
- **多份招聘信息导入后，确认面板里每份的"原文"都是空的**：拆分时让模型给出的原文摘录只被当作字段锚点用完就丢，前端读的 `recognized_text` 一直是空串——用户看不到可核对的原文，也就无法判断拆分对不对。现在每份草稿都带回**它自己那段**摘录（没有可用模型、退回本地切分时，每段本身就是原文）。
- **联网搜索"最多 3 次"实际是 4 次**：打开联网开关时那次自动预搜没有计入预算，而系统提示、README 与使用指南都写着 3 次。现在预搜也占一次额度（失败的预搜不占——它没给模型任何资料）。
- **开了正文抓取，却仍告诉模型"不打开网页"**：工具描述与系统提示里都写死了"只返回搜索摘要（不打开网页）"。描述是模型判断"这个工具能给我什么"的唯一依据，于是它放着拿到的正文不用、反复换关键词搜，或者直接告诉用户"我只能看到摘要，建议你自己去看"。现在两处措辞都跟着「抓取正文的条数」走。
- **Anthropic 协议的 Base URL 提示少了 `/v1`**：接口协议那一栏的提示写的是 `https://api.anthropic.com`，而 provider 拼的是 `{base_url}/messages`——照着提示填会打到官方根本不存在的路径上，报错只会说 404/400，看不出是地址写错了。
- **助手系统提示否认了它自己有的能力**：提示里写着"教育经历、工作经历、项目、技能、奖项等结构化条目你也改不了"，而 `add_profile_entry` 一直能追加这些条目。模型照着提示回答，用户听到的是"我没有修改你资料的权限"——「把资料箱里的材料整理进个人资料」这条路就这样被挡在门外。现在改为：**能追加一条，但改不了已有条目**，并补了一条测试钉住这个矛盾（提示里必须提到工具，且不能再出现那句一票否决）。
- **系统提示里"不要声称已打开搜索结果页面"与设置冲突**：打开「抓取正文」后应用确实会抓取正文并交给模型，而这句写死的规则会让它放着正文不用。现在这条移到联网工具说明里，按是否抓正文分别措辞。
- **删除两处死代码与过时说明**：后端未使用的 `DEFAULT_ANTHROPIC_BASE_URL` 常量删除；README 与使用指南里"当前仅直接支持 OpenAI Chat Completions 兼容协议，原生 Claude 需要兼容网关"的说法已不成立，改为说明两种协议怎么选；设置页顶部那条"支持所有兼容 OpenAI Chat Completions 协议的模型服务"的提示补上 Anthropic 原生协议。
- **点「启动浏览器」只弹出一个空白窗口**：拉起浏览器时最后一个参数写的是 `about:blank`，于是窗口确实开了、调试端口也确实就绪（界面显示「运行中」），但用户面对的是一张白页——既不知道该去哪里，也没有可以扫码登录的页面，而界面还提示"请在弹出的窗口里扫码登录一次"。启动浏览器的全部意义就是打开那个要登录的网站，因此站点适配器新增 `entry_url`（BOSS直聘为 `https://www.zhipin.com/`），拉起时直接打开它。同时新增 `POST /api/apply/browser/open` 与界面上的「打开招聘网站」按钮：标签页被关掉或跳走时用它找回登录页，不必重启浏览器（走 `Page.navigate` 复用当前标签页，反复点不会越堆越多标签页）；`BrowserStatusOut` 增加 `entry_url` 并在界面上展示，用户能提前看到这个窗口会去哪里。

### Changed

- **首页从"看数据"改成"看该做什么"**：此前首页只有四个计数卡加两个列表，功能变多之后它没跟上——进了首页看不出"我现在该做什么"。新增 **接下来做什么**，它只列**待办**、不列"你已经做了多少"（概览页上能推动人的只有前者）：还没核实的事实台账条目、投递队列里排着还没投的岗位、超过一周没有任何更新的进行中投递。每一条都带数字和去处，点一下就到那件事本身；三类都清空时明确说"眼下没有待办"，不留一片空白。另有按"要做的事"组织的**快捷入口**，以及"最近投出去的"列表。接口 `GET /api/stats` 相应增加 `pending_claim_count` / `pending_claims` / `apply_queue_count` / `stalled_application_count` / `favorite_job_count` / `latest_applications`，全部带默认值，旧前端不会因为多了字段而失败。
- **设置页分成三个标签页，编辑按钮跟着它真正控制的那一页走**：设置项随功能增加而变多，一整页排下来找起来很费劲——想改数据备份得先翻过整套模型参数。现在分成 **AI 模型**（模型配置、配置记录、联网搜索、助手技能）、**数据**（数据集备份与切换）、**应用**（版本更新）三页。**「编辑设置」从页面顶部移到了「AI 模型」页里**——它此前挂在页顶，却只控制模型配置，而其余几页一直是可编辑的，那个位置本身就在误导用户；现在它明确地是该页的操作，其余两页不需要进入编辑态就能用。
- **弹窗改成在卡片内部滚动，不再撑高整个页面**：`ConfigProvider` 把弹窗挂到 `body`，卡片因此脱离文档流，`<body>` 不再被撑高——此前滚动条跑在整个窗口的右侧，用户得先滑页面才能看到卡片下半部分，卡片右上角反而没有滚动条。配合给 `.ant-modal-content` 一个 `max-height` 与内容区 `overflow-y: auto`，投递设置这类长表单现在**在卡片自己身上滚动**；只给上限不给固定高度，内容少时卡片仍按内容收缩。所有弹窗一起生效。
- **简历照片只显示"正在使用"的那一张**：此前大图与下方缩略图并列，同一张脸在同一个卡片里出现两次，看起来像存了两张照片。备选照片移进 **照片库** 弹窗（那里本来就该承担"挑一张"的动作），卡片本身只回答"简历上用的是哪张"；删到只剩一张时照片库自动关上。
- **助手多选不再支起一个大框**：多选操作条此前是独立的一整条浅蓝卡片，下面还没有内容时就先占了一大块版面。改成与消息列表同宽的一行轻量工具条——左边报数、右边动作，用一条分隔线收尾。
- **「退出」按钮移到界面右上角**：它此前在侧栏底部、和「使用指南」挤在一起，而"退出应用"是全局动作、不是某个模块的入口。现在与仓库链接并排放在顶栏右侧；窄屏只留图标，避免顶栏被挤到换行。
- **使用指南的排版修好了**：布局其实是坏的，而且坏在一个不容易看出来的地方——`.user-guide-body` 声明了**两列**网格，而 DOM 里有**三个**孩子（步骤条、竖分隔线、内容区）。分隔线占掉第二列，内容区被挤到**下一行的第一列**：正文只有 196px 宽，中文每行两三个字就折行，整个弹窗被拉到 3000px 高（截图反馈"排版完全是乱的"）。改成用命名网格区域显式指定每个孩子的位置，孩子个数再变也不会悄悄错位；正文宽度从 104px 恢复到 388px，弹窗高度从 3406px 降到 866px。
- **深挖页补上返回入口**：`/claims/drill` 是从台账「拿去深挖」进来的独立路由，此前除了浏览器后退，界面上没有别的出口——用户会以为"进去就出不来了"。左上角新增「返回事实台账」。

- **「字号系数」现在对 HTML 预览也生效了**（此前只影响服务端 PDF）。它走的是"渲染前把系数预乘进基准字号"这条路——那是唯一对内置模板与用户自制模板都生效的路径，因此**刻意不给它 CSS 变量映射**：给两条路会叠乘，界面上调 1.1 实得 1.21 倍，而这种偏差只有拿尺子量才看得出来（已加测试钉死 `format_css` 对它不产出任何内容）。
- 模板的版式默认值（页边距、行高、区块间距）改为在 `resume_templates.py` 里声明，并有测试**逐个模板读文件核对**。自动一页靠这些数字判断"当前值是多少、还能往紧收多少"，抄错会让它收紧一个用户根本没设置过的值，或者该收没收——而这两件事在界面上都表现为"按了没反应"。
- 新增接口：`POST /api/resumes/{id}/layout/analyze`；`PATCH /api/resumes/{id}/layout` 新增可选的 `format_config`（不传 = 不涉及、空对象 = 清掉覆盖，两者语义不同且都有测试）。
- 新增运行时依赖 `websocket-client==1.8.0`（钉死版本、纯 ASCII 注释）：自动投递的 CDP 客户端用它走 DevTools 的 WebSocket 通道，HTTP 端点复用既有 `httpx`；纯 Python、有 wheel，首启安装仍是零编译。一键启动器的依赖探针同步包含 `websocket`，旧虚拟环境会在下次启动时自动补装。`.gitignore` 增加 `backend/data/browser-profile/`——投递专用浏览器的 `user-data-dir` 含登录态、体积可能很大，**绝不进 Git，也不进数据库备份包**（登录态由 Chromium 自己持久化，应用既不读取也不解析）。
- 使用指南的联网搜索一节重写：写清"最多 3 次"是**总数**（含开头那次自动搜索）、四个搜索参数各自的作用与代价，以及"开关"和"设置"是两件事。
- **助手消息支持多选批量删除**：消息区右上角新增「多选」，勾选后一次删掉多条（走后端已有的批量接口，一次最多 200 条，中途失败不会删一半）。换会话会自动退出多选，免得把上一条会话的消息 id 带到新会话里误删。删除前有二次确认。
- **拖拽导入补齐四个漏掉的入口**：经历参考文件（.md/.txt）、技能包（.md/.zip）、简历模板 HTML、数据集备份包（.zip）现在都能直接把文件从资源管理器拖进去。此前只有岗位识别、备选截图、资料箱附件、个人照片、助手附件这五处接了统一拖拽容器，剩下四处还是只能点按钮选文件。拖拽区贴着按钮本身、不做整行接管——设置页一行里常并排放着好几个导入入口，整行接管会让"拖到哪个框里"变含糊。
- `FileDropZone` 补上单元测试。这一层此前**一条测试都没有**，而项目里九个导入入口全挂在它上面——它要是坏了，"拖进来"会在所有地方一起失灵，却没有任何断言会红。
- **卸载流程补齐**：`uninstall.cmd` / `scripts/Uninstall-ResumeForge.ps1` 补上测试与文档。它默认只删启动器生成的东西（虚拟环境、`node_modules`、构建产物、`runtime/` 与各类缓存），保留 `backend\data` 与 `backend\.env`，**永远不删源码**，且只在真正的项目目录里运行；`-Purge` 才会连数据一起删。测试全部在临时目录里复制一份脚本来跑，不会碰当前仓库。
- 应用内使用指南同步：模板数量改为六套样式模板 + 格式模板（原文还写着"三套模板"）、侧栏「技能工作台」改为「工作台」（原文是改名前的旧称呼）、补上接口协议与联网搜索设置、消息多选删除、退出与卸载。`UserGuideModal.test.tsx` 此前把旧名字钉住了，所以它没能在改名时报警——已改为断言新措辞。

- 应用内使用指南**就地扩写「导入岗位」这一步**（标题改为「导入岗位与投递」、改进对应 `heading` 与 `description`）：补上自动采集的入口与「未生效」限制、匹配度分析与「岗位需求解读」的区别（一个回答"岗位要什么"，一个回答"你够不够"）、投递台的操作方式与「登录在你自己浏览器里完成」。**不新增第 8 步**——采集与投递仍是同一条「收进来 → 判断 → 投出去」的链路，指南是流程向导而非发行说明。`path` 保持 `/jobs` 不变。
- `.gitignore` 增加 `deliverables/`：需求、设计、评审等**协作过程文档**不属于产品代码，默认不提交（如需纳入版本管理，删掉这一行即可）。

## 0.8.0 - 2026-09-17

### Added

- **模拟面试（新模块）**：侧栏新增「模拟面试」。可设定关联岗位、面试类型（技术面 / 项目深挖 / 行为面 / HR 面 / 综合面 / 案例分析 / 英语面试 / 压力面）、难度、面试官风格（严谨专业 / 温和引导 / 持续追问 / 压力质询）、轮数与自定义人设，面试官逐轮提问、点评上一答并追问，满轮或主动结束后生成评分报告（总分 + 四个维度 + 做得好的地方 + 可改进项）。报告可以一键存进资料箱的「面试复盘」分类。**流程由代码控制**：轮数、何时结束、何时出报告都由后端决定，模型只负责同一轮里问什么、怎么点评，所以界面上的"第 N/6 轮"是可信的。助手新增 `list_interview_sessions` / `get_interview_report` 两个工具，可以读取记录帮你复盘（面试本身需要用户逐轮作答，助手不代答）。
- **多份招聘信息一次导入**：粘贴含多份招聘信息的材料（招聘网站的整段复制、聊天记录、多张截图与文档）后，系统会拆成多份草稿并**先让你确认再保存**——每份显示岗位名、公司、地点、薪资与原文摘要，可取消勾选不要的、可"单独编辑"某一份，也可一次保存勾选的全部。拆分走 AI（提示词 `job_multi_extract.md`，每份必须给出**原样复制的原文摘录**作为字段锚点，跨段字段会被丢弃）；没有可用模型或 AI 拆不出来时退回本地按分隔线 / 序号小标题 / 第二个公司标签切分。新增 `POST /api/jobs/parse-multiple`。
- **简历模板体系**：样式模板从 3 种扩到 6 种（新增优雅 / 技术 / 极简），并新增**格式模板**——只调行高、页边距、区块间距、强调色、字号系数的版式覆盖，与样式模板自由组合（例如"优雅样式 + 紧凑版式"）。`GET /api/resumes/templates` 一次下发模板、字号、格式参数清单与内置版式预设；生成前与预览时都能切换，`PATCH /api/resumes/{id}/layout` 只重新渲染、不重新生成内容。
- **工作台（技能 + 模板）**：原「技能工作台」升级为「工作台」，分「助手技能」「简历模板」两页。模板页可以逐个预览每种样式（真实渲染缩略图，可切字号与版式）、从内置模板复制一份来改（右侧实时预览，服务端渲染所以看到的就是导出效果）、导入 HTML 文件、自制格式模板（只填参数，不用写代码）、重命名与删除。自制模板走**沙箱渲染**：导入时剥离 `<script>` 与外链并补齐 CSP，渲染用 Jinja `SandboxedEnvironment`。
- **所有导入入口都支持拖拽**：岗位识别、备选岗位截图、资料箱附件、个人照片、助手附件统一用一个拖拽容器（`FileDropZone`）——直接把文件从资源管理器拖到对应区域即可导入，按 `accept` 过滤不支持的类型并给出提示；只接管文件拖拽，不影响页面内其它拖拽（例如资料页的区块拖动排序）。
- **助手消息可以引用、删除与导出**：右键任意一条消息可「引用这条继续问」（被引用内容的快照存进消息里，原消息删除后引用仍可读）、复制或删除；输入框上方显示引用条，只对下一次提问有效。会话列表的「更多」新增「导出为 Markdown」（含时间、附件名、助手做过的操作与参考来源）与「存进资料箱」（直接变成一条面试复盘材料）。
- **可以退出应用**：侧栏底部新增「退出」，点击后后端优雅退出，页面切成"已退出"并提示如何重新启动（此前只能去关掉后端窗口）。
- 新增接口：`POST /api/jobs/parse-multiple`、`/api/resume-templates`（自制模板增删改查 + `builtin-source` + `preview`）、`/api/interview`（创建 / 列表 / 详情 / 逐轮回答 / 结束 / 删除 / 存进资料箱）、`/api/assistant/conversations/{id}/export` 与 `/to-material`。
- 新增 Alembic 迁移 `0009_templates_and_interview`：新建 `resume_template`、`interview_session`、`interview_message` 三张表；`resume_record` 加 `format_name`；`chat_message` 加引用字段。

### Fixed

- **真实运行环境端到端联调发现的两个问题**（用隔离数据库 + 本机假模型服务跑通 82 项检查时暴露，已各自补上回归测试）：
  - **模拟面试开场必然 500**：面试官需要候选人的个人资料作为提问依据，而那段代码把资料按 `model_dump()` 序列化后用 `json.dumps` 直出——资料里带 `created_at` / `updated_at` 两个 `datetime`，`json.dumps` 直接抛 `TypeError`。已改用 `model_dump(mode="json")`。此前单测把这一步打桩绕过了，所以只在真跑时才会出现。
  - **生成时选的格式模板没有存进记录**：生成接口把样式模板、页数、字号都写进了 `resume_record`，唯独漏了 `format_name`，于是重新打开预览或导出时会悄悄退回模板自带版式——用户会以为"我选的版式没生效"。
- 列表行的操作不再重复：岗位广场的「详情 / 生成简历」与简历中心的「预览 / 导出」此前在行上蓝色链接和「更多」菜单里各出现一次，菜单现在只保留行上没有的操作；收藏夹同理——「取消收藏」只由行上的星标按钮承担，不再出现在菜单里。整行右键仍给出完整清单（右键时鼠标不在行内链接上，补全更方便）。
- **模型列表端点与 Base URL 校验共用一套规则**：`POST /api/settings/llm/models` 复用 provider 的地址校验（远程必须 HTTPS、HTTP 仅限本机），避免两处判断漂移；`cn.bing.com` 不可用时联网搜索回退 `www.bing.com`。
- **简历预览页脚的按钮组溢出**（截图反馈）：按钮换成可换行布局，不再把「完成」挤出弹窗。
- **使用指南的步骤标题互相挤压**（截图反馈）：步骤改为左侧竖排导航 + 右侧内容，标题每项独占一行，再加模块也不用重新排版；内容补上资料箱、工作台、模拟面试与对话导出。
- **资料图片上限前后端不一致**：后端常量曾是 6 张，与界面上写的 2 张不一致，现已统一为 2 张（总文件数 4 个）。

### Changed

- **求职助手页头不再重复显示技能**：右上角的技能模块去掉（输入框旁边已有可勾选的技能菜单，勾选框不再只是高亮）。
- **消息与页头的时间、思考强度更清楚**：思考强度加上文字标签（只显示「默认」时用户不知道在调什么）。
- **个人照片一行横排、可左右滑动**：此前多张照片竖着堆会把「我的资料」右栏撑得很长。
- 助手系统提示与默认引导对话同步新模块（模拟面试、工作台模板、消息引用与导出），并明确"助手开不了模拟面试、只能读取记录帮你复盘"。
- 使用指南、README 与 CHANGELOG 同步本次新增能力；版本号提升到 `0.8.0`。

## 0.7.0 - 2026-09-17

### Added

- **资料箱（新模块）**：侧栏新增「资料箱」，用来放还没整理进简历的零散材料——证书、作品、链接、笔记、实习/校园材料。条目可以带正文、链接、备注与附件（图片最多 2 张、文本文件最多 4 个，正文与图片都存在本地数据库）。求职助手可以直接创建、修改和读取资料（`list_materials` / `get_material` / `create_material` / `update_material`），所以"帮我把这段记下来"现在有地方落。
- **助手技能工作台（新模块）**：侧栏新增「技能工作台」，可以新建技能（提示词 + 知识文件）、查看与编辑已有技能的提示词全文、启停和删除，页面上有「技能是什么 / 工作台怎么用」的引导和三个可直接改用的模板（简历要点精炼、面试问题预测、岗位匹配分析）。设置页的「助手技能」列表现在点技能名就能打开同一个详情弹窗。后端新增 `POST/PUT /api/assistant/skills` 与 `GET /api/assistant/skills/{id}`（详情带回知识文件正文，超出上限时标记 `files_truncated`，前端据此避免覆盖式写入）。助手也拿到了 `list_skills` / `get_skill` / `create_skill` / `update_skill` 四个工具。
- **备选岗位**：岗位广场新增「备选岗位」抽屉，把还没核对的招聘信息（粘贴文本或截图）先存下来，之后再点「导入到岗位」用正式表单预填原文导入，导入状态会标记成"已导入 #id"。助手同样可以增改与导入（`list_candidate_jobs` / `create_candidate_job` / `update_candidate_job` / `import_candidate_job`，重复导入不会创建第二份）。
- **简历版式可选**：生成前可以选最大篇幅（1-3 页 A4）、字号（小/标准/大）与模板（经典 / 现代 / 精简）；默认一页 + 标准字号。内容塞不下时预览里会提示，并提供"增加到 N 页 / 改为小字号"一键重排——只重新渲染，不重新调用模型。三套模板共享同一份正文片段与 `data-resume-path` 标注，点击定位编辑照旧可用。
- **导出 PDF 可以直接下载**：新增服务端 PDF 生成（`fpdf2` + 系统中文字体：Windows 微软雅黑/黑体、macOS PingFang、Linux Noto CJK），`GET /api/resumes/{id}/export?format=pdf` 直接返回文件；找不到中文字体时该按钮退回「浏览器打印 / 另存为 PDF」，接口返回 409 并说明可执行的处理办法。两个入口都在导出菜单里。
- **生成简历时可以补充要求**：生成弹窗新增「补充要求（选填）」，最多 2000 字，会附在生成提示词后面影响表达方向（事实锚定、篇幅上限与防虚构规则不变）。
- **个人照片可以存多张**：我的资料页的简历照片支持上传多张（最多 8 张）、点缩略图切换使用哪一张、删除（删掉正在用的那张会自动改用下一张）；切换时同步写回 `user_profile.photo`，因此简历生成、预览与导出链路一行未改。旧数据里已有的一张照片会在首次打开照片面板时自动转成可管理的记录。
- **大模型配置增强**：模型名称旁新增「获取可用模型」（`POST /api/settings/llm/models`，按当前账号拉取服务商模型列表并可直接选用）；快速预设从 5 个扩到 15 个（新增通义千问、智谱 GLM、MiniMax、硅基流动、OpenRouter、Gemini、xAI Grok、Groq、Mistral、LM Studio）；新增可选的「高级调整」——Top P、频率惩罚、存在惩罚、随机种子，留空即不发送。
- **求职助手增强**：输入框下方的工具条里可以直接开关已启用的技能；新增「思考强度」选择（默认/关闭/低/中/高，通过请求级 `reasoning_effort` 下发，不支持的服务商会忽略）；消息气泡显示发送时间；首次进入且没有任何会话时自动创建一条带内置引导的对话（`prompts/assistant_welcome.md`）。
- **助手会话管理对齐常见聊天应用**：每条会话的「更多」或整行右键支持置顶、收藏、复制分享链接（`/assistant?conversation=<id>` 深链）、重命名、在新对话中继续（复制最近 10 条消息到新会话）、移动到分组、复制会话 ID、归档与删除；侧栏筛选新增「已归档」，列表显示消息条数与更新时间。后端 `chat_conversation` 新增 `archived`、`group_name`，并新增 `POST /api/assistant/conversations/{id}/fork`。
- **助手可以自己联网搜索**：打开「联网搜索」后会下发 `web_search` 工具，助手能自己决定搜什么、换关键词多搜几次（**一轮回答最多 3 次，由代码强制**，超出后工具会告知模型次数已用完），命中的来源会累积到回答下方的来源卡片里；搜索实现同时改进——结果上限 5→8、按主机名与路径去重、用人单位官网与招聘页优先排序、`cn.bing.com` 不可用时回退 `www.bing.com`。
- **软件更新检查与一键更新**：设置页新增「软件更新」卡片，调用 GitHub Releases 对比本地版本（结果缓存 15 分钟，网络不可用时只提示不报错）；项目根新增 `update.cmd` 与 `scripts/Update-ResumeForge.ps1`——git 检出走 `git pull --ff-only` + 依赖同步，普通目录下载 zip 后只覆盖程序文件，`data/`、`.env`、`runtime/`、`node_modules/`、`.venv/` 一律不动，支持 `-DryRun` 预览。
- **列表操作不再裸露**：岗位、简历、收藏夹、技能工作台与设置页的技能列表，行操作都改成「主操作 + 更多菜单」，删除只出现在菜单里并需二次确认；整行支持右键唤起同一份菜单（岗位：详情/生成简历/自行编写/相关简历/编辑/删除；简历：预览导出/重命名/收藏/删除）。首页的「最近岗位 / 最近生成的简历」两张卡片不在此列：它们只是跳转链接，右键菜单没有可放的动作。
- **岗位备注支持图片**：备注可以再放两张图片（招聘截图、内推码等），列表里显示图片数量、详情抽屉里可放大查看；助手新增岗位时会标注 `AI 助手录入`。
- 新增运行时依赖 `fpdf2==2.8.4`（连带 `fonttools==4.65.0`，均钉死版本、纯 ASCII 注释）。
- 新增 Alembic 迁移 `0008_materials_and_modules`：新建 `material`、`candidate_job`、`profile_photo` 三张表；`chat_conversation` 加 `archived`/`group_name`，`job` 加 `note_images`/`recognition_source`，`resume_record` 加 `template`/`page_limit`/`font_scale`/`custom_instruction`，`llm_config_record` 加 `top_p`/`frequency_penalty`/`presence_penalty`/`seed`；`database_compat.py` 的老库补列清单同步更新。
- `GET /api/resumes/templates` 返回可用的模板、字号档位、默认值与"服务端能否直接生成 PDF"；`PATCH /api/resumes/{id}/layout` 只改版式不重新生成。
- 新增接口：`/api/materials`（增删改查 + 分类）、`/api/candidate-jobs`（增删改查 + 导入标记）、`/api/profile/photos`（上传/切换/重命名/删除，最多 8 张）、`/api/update/check`。

### Fixed

- **软件更新后旧备份导不回来**（用户反馈的"导出的数据在更新后还能不能导入"）：两处根因都已修掉。
  - 备份格式校验从 `format != BACKUP_FORMAT_VERSION` 改成"只拒绝比当前代码更新的格式"，并把版本号类型校验补上——此前只要导出格式升到 2，所有历史备份会立刻被判为"不受支持"；现在 `format=1` 的包在将来仍可导入，来自更新版本的包给出"请先升级应用"的明确提示。
  - 应用表清单（`_APPLICATION_TABLES`）从手写常量改为从 `Base.metadata` 推导——此前新增一张表却忘记同步清单，会让**自己导出的备份因为"缺少数据表"被拒收**，而这类故障只有用户真去恢复数据时才会暴露。
  - 未知表与 `sqlite_sequence` 的处理同步放宽（后者是 SQLite 内部表，不再被当成"未识别的数据表"）。
- 求职助手联网搜索在无关结果上的误报收敛：结果去重与官网优先排序后，第三方平台仍保留但排在官方招聘页之后，不会再让百科/诗歌类结果出现在摘要里。

### Changed

- **岗位的录入来源会自动写进备注**：保存岗位时按识别输入自动补一行「来源：粘贴文本识别 / 图片识别 / 文档识别」（从备选岗位导入或助手录入时分别标注对应来源），已标注过的不重复追加；岗位详情抽屉新增「信息来源」一栏。
- **助手系统提示重写**：补上各模块的用法（岗位、备选岗位、资料箱、技能、简历版式）与技能改动的确认要求，并新增一段"联网搜索工具已开启"的使用规则（何时搜、怎么换关键词、摘要不可信、不得声称已打开网页）。
- 使用指南：步骤标题在 720px 弹窗里会互相挤压（显示成"完善资料…"），已改为允许换行并留出间距；六步的文案同步了资料箱、备选岗位、照片多张、版式选择、直接下载 PDF、技能开关、会话菜单、更新方式等新能力，最后一步改名为「数据备份与更新」。

- 岗位识别、个人资料识别与助手附件新增**文档**支持：PDF 与 DOCX 的文字由后端在**本机**提取，原始文件不外发，因此文档识别不需要多模态模型，没配置模型时本地规则也能解析出一部分字段。
  - 提取出的文字与粘贴文本合并成同一份 `source_text`，字段锚定校验照旧生效。**不插来源标记**：本地规则会把 `[文档：jd.docx]` 这样的孤行当成正文（岗位描述里会多出一行文件名），文件来源在界面上本来就以附件形式可见。
  - 解析不可信文件也各自设限：PDF 最多读 30 页、一次识别的文档文字总量封顶（超出时截断并在结果里说明）、DOCX 只读白名单成员 `word/document.xml` 而不解压整包。
  - 扫描件（没有文字层的图片型 PDF）会明确提示改用截图上传，加密 PDF 提示先解除保护，损坏或改名的文件在调用模型之前就以 422 失败。
- 识别与助手附件新增常见图片格式：`bmp`、`tif/tiff`、`jfif`。BMP/TIFF 先在本机转成 PNG/JPEG 再发送——多数服务商不接受这两种格式，直接放行等于必然返回 400。截图类内容优先用无损 PNG，超限才退到 JPEG 并逐级降采样；TIFF 在浏览器里渲染不出来，暂存区与消息里显示为文件名标签而不是破图预览。
- 新增运行时依赖 `pillow==12.3.0`、`pypdf==6.18.1`（均钉死版本，纯 ASCII 注释）；Windows 一键启动器的依赖探针同步包含这两个包，旧虚拟环境会在下次启动时自动补装。
- 不支持的文件给可执行的提示，而不是笼统的"格式不受支持"：HEIC/HEIF、SVG、旧版 `.doc`、RTF、ODT、xlsx/pptx 各有对应文案（例如 HEIC 提示先在系统里导出为 JPEG，`.doc` 提示另存为 `.docx` 或 PDF）。
- 文档提取的说明会跟着附件回传并展示：`ChatAttachmentOut` 新增 `notes`，`kind` 增加 `document` 取值（旧记录与旧客户端不受影响，缺省为空）。
- 新增 `scripts/Build-Release.ps1`：发行包只从 git 档案生成（`git archive <ref>`），因此不会漏掉跟踪的文件、也不会夹带私人数据——`backend/data/`、`backend/.env`、`runtime/`、`node_modules/` 与各种缓存都被 `.gitignore` 挡在外面，`.env.example` 是唯一例外（它本就要随包发送）。写完后脚本会重新打开压缩包自检：必需文件在、必需目录非空、禁止路径不在、`start.cmd` 保持 CRLF 行尾；任何一项不过就**删除该压缩包并报错**，不会留下半成品。CI 在 Windows 上运行 `scripts/tests/Test-Build-Release.ps1`，真的打一次包并断言包内文件与 `git ls-tree -r HEAD` 完全一致、出包清单与后端 `app/preflight.py` 的运行时清单一致。

### Fixed

- **修复手工压缩包缺文件时"完全看不出病因"的问题**（真机反馈：朋友解压后双击 `start.cmd`，后端在导入阶段就抛 `FileNotFoundError: backend\app\data\skills.json`，而控制台只有一句 `Backend exited with code  before becoming healthy. See runtime\backend.stderr.log.`——退出码是空的，也没有任何可执行的线索）。三处一起改：
  - **启动器把病因打出来**：后端/前端未就绪时，控制台直接打印 `runtime/backend.stderr.log`（或 `frontend.stderr.log`）的最后 12 行，并修掉退出码为空时印出 `exited with code  before` 的问题（`ExitCode` 在某些时刻仍是 null，`Get-ProcessExitCodeText` 现在会退回 `unknown`）。
  - **启动器回收自己遗留的进程**：端口被上次启动留下的进程占着时，先按记录（PID + 启动时间 + 命令行三者都匹配）结束它再启动，而不是让用户自己去找。只有端口被别的程序占用时才报错，报错文案不变。
  - **后端启动前自检**：新增 `app/preflight.py`，由 `app/__init__.py` 在导入任何子模块之前调用。缺文件或技能词典结构不对时抛一条中文消息（缺哪些文件、怎么处理），不再是一串导入栈。
- 进程记录的读写原语从 `Stop-ResumeForge.ps1` 移入 `ResumeForge.Common.ps1`（`Stop-RecordedProcess` / `Get-ProcessRecordMatch` / `Get-ResumeForgeProcessPattern`），启动路径与停止路径共用同一套 PID、启动时间、命令行校验，避免两处各自漂移；属性读取改走 `PSObject`，手改过的记录在 `Set-StrictMode` 下也不会抛错。

### Changed

- **简历生成的写作口径重写**（`prompts/resume_generate_system.md`）：角色改为资深 HR 视角，并补齐三类此前没有的约束——
  - **措辞标准**：去口语、空话套话与互联网黑话（"赋能""抓手""闭环"）；强动作动词开头，但**不得升格职责范围**——"参与"不能写成"主导"，"协助"不能写成"负责"。这条属于防虚构的一部分。
  - **篇幅与粒度**：每条描述性要点 30 字以内、同一段经历或项目的要点不超过 6 条、个人总结 2-3 句；单段内优先核心成果，其次核心职责，最后日常事务。
  - **分模块呈现要求**：工作/校园/项目/教育/技能/奖项各自给出什么内容——技能只能选候选事实里已有的项并按熟练度与岗位相关度排序，教育只在资料里有 GPA、课程、荣誉时呈现，奖项没有就输出空数组。
  - 岗位分支的第 2 条增加「先拆解 JD（高频硬技能、职责动词、职级、行业术语）」与关键词自然融入，并明确**只在候选事实确有对应内容时**使用：候选人确实不具备的 JD 要求不硬塞，避免"关键词覆盖"变成新的虚构入口。
  - 原有 9 条规则一条未删（真实性、事实锚定、参考事实、量化、输出格式、身份字段留空、岗位筛选/全貌优先、附件事实不得遗漏），只重新编号；通用简历的「全貌优先」等约束逐字保留，并新增按两种模式渲染模板的回归测试。
  - 这些约束只在**提示词层**生效，代码不做强制校验；`docs/prompt-tuning.md` 新增「措辞与篇幅口径」一节说明边界，并写明不要为了字数上限去放宽 `resume_quality.py` 的质量门槛。
  - 按这套口径真机生成一份简历后补了第 12 条：**候选条目本身没有可写事实时不要输出空壳条目**（资料里有个项目只有名称，被候选筛选选中后模型原样输出，成品上就是一条没有内容的经历）。
  - 真机两轮实测后按结果收紧：字数上限从「30 字以内」改为「30 字左右、最多不超过 35 字」（两轮各有一条贴线超出），个人总结补上「不超过 120 字」，并要求经历/校园/项目的要点以强动作动词开头、写清"做了什么 + 结果"，项目 highlights 不能只堆技术名词。
  - **新增代码侧措辞门槛**（`services/resume_wording.py`）：提示词点名禁掉的黑话与空话由本机做确定性检查（`CLICHE_WORDS` / `CLICHE_PHRASES`），命中后复用既有的**一次质量重试**改写（重试提示词同步说明了短板清单里可能只有措辞问题）；重试后仍命中时在结果 warnings 里告知用户，而不是静默留下。只在启用美化拓展时检查——关闭美化时正文是用户资料原文，改它等于篡改用户自己的表述。
  - 四轮真机实测后，把**可度量**的约束（每条要点最多 35 字、单段不超过 6 条、个人总结不超过 120 字、不得出现黑话与空话）同时写进用户提示词末尾的「输出前自查」清单：只写在系统提示里时，超标条数在 0~5 之间波动（最长 42 字）；加上清单后落在 31-34 字区间（上限内），黑话也不再触发改写。
- 前端识别暂存区从"图片暂存"扩展为"截图与文档暂存"：`useImageStaging` → `useRecognitionFiles`、`ImageStagingField` → `RecognitionFileField`，按钮文案与 `aria-label` 改为「添加截图或文档」，接受的文件类型由 `utils/attachments.ts` 统一导出（`RECOGNITION_ACCEPT` / `ASSISTANT_ACCEPT`）。
- `POST /api/jobs/parse-text` 与 `POST /api/profile/parse-text` 新增可选请求字段 `documents`；图片与文档是两个字段，但共用同一份额度：一次最多 4 个、单个 ≤ 2 MB、合计 ≤ 5 MB。未传 `documents` 时行为与之前完全一致。
- **附件格式判定改为「内容为准」**：扩展名（以及浏览器按扩展名给出的 MIME）与实际内容不符时，按内容的真实格式收下。图片站/CDN 常对 `.jpeg` 链接按浏览器偏好返回 WebP，另存下来名字与内容就对不上——此前这种情况会以「内容与声明图片格式不一致」直接拒绝，用户既不知道真实格式也不知道下一步。现在这类文件照常可用：按真实格式发送给模型，并在附件说明里注明「文件实际是 WebP，已按真实格式处理」。被改名的文档同样按内容判定（真 docx 命名为 `.pdf` 也能提取文字）。
  - 放宽的只有"名字 vs 内容"这一层：内容认不出、或真实格式不在白名单内（HEIC/HEIF、AVIF），仍然拒绝，且提示会写明真实格式与可执行的处理办法（改成 `.png`、先导出为 JPEG、"那是文档，请改名 `.pdf`"等）。声明 MIME 与扩展名不一致、data URL 前缀与扩展名不一致，仍按原样拒绝。
  - 个人照片（`PUT /api/profile` 的 `photo`）仍是旧规则：它的校验在 `schemas/profile.py`，与附件不共用实现，本次未放宽——`schemas` 目前不依赖 `services`，为复用原语而反向依赖不划算，留作后续（先把校验原语移到中立模块再统一）。
- **奖项不再参与岗位相关性筛选**（`profile_matching._select_entries`）：奖项名里几乎不会出现岗位技能词（"国家励志奖学金"对任何 JD 都是零分），按相关度打分等于每份定向简历都静默丢掉全部奖项——真机四轮生成里 `awards` 一直是空数组，用户填过的获奖经历一次都没出现在成品里。现在奖项按资料录入顺序取到 3 条上限，与通用简历走同一条语义；**校园经历仍然按相关性丢弃**（这两类此前共用一条分支，已拆开）。

## 0.6.0 - 2026-09-16

### Added

- 识别结果现在**常驻标明来源**：「AI 识别」还是「本地规则」以往只在识别完成的提示条里出现一次，提示条几秒后消失，而这两者的可信度并不相同——AI 识别的每个字段都校验过能在你给的内容里找到出处，本地规则只是按模式匹配。现在岗位识别与个人资料识别的结果下方都有一枚常驻标记，悬停说明该来源意味着什么、要核对到什么程度；改动粘贴的文本会撤掉标记，因为那次识别的结论已不再对应当前内容。
  - 原 `RecognizedTextField`（只显示模型抄录的原文）扩展为 `RecognitionOutcome`，同时负责来源标记与抄录折叠块，两个识别入口共用一份实现，不会各说各话。

### Fixed

### Changed

## 0.5.0 - 2026-09-16

### Added

- 全应用补了一轮**悬停提示**，目标是"看不懂的地方都能问一下鼠标"，而不是给每个控件都挂上提示：
  - **只有图标的按钮现在都有说明**。此前有 5 个漏了——对话标题的保存、简历编辑器里每条经历的删除、通用简历的重命名与删除、技能卡片的删除；它们只有 `aria-label`，读屏能用，但看得见的人悬停上去什么也得不到。删除类按钮的提示还会说明**删掉的是什么范围**（例如技能会连知识文件一起删）。
  - **说不清自己的词都补了人话**：简历预览的「抓手 / 编辑」两个模式（"抓手"是设计软件的行话，"编辑"听着像跳转到别的页面）、简历列表的「美化拓展」档位标签（"均衡"单看等于没说，现在悬停会讲这一档到底做了什么，用的是生成弹窗里那份文案，通用简历的"深度"档说法也跟着区分）、数据集与模型配置记录上的「当前」（"当前"指代不明，现在说明是哪一份在生效）。
  - **长内容不再既被截断又看不到全文**：助手的附件名来自用户、长度没有上限，而标签本身不换行，此前会把聊天气泡顶变形——现在截断到 260px 并保留悬停查看全名；助手页头的会话标题（用户可编辑到 128 字）同样加了截断与悬停全文；调整资料模块顺序时被截断的模块名也补上了。
  - 悬停提示一律走**既有约定**：图标按钮的 `title` 与该按钮的 `aria-label` 同源，表单字段继续用 `Form.Item` 的 `tooltip`，与周围代码保持一致。

- 应用内**使用指南**补齐了它一直没跟上的功能：从 5 步扩到 6 步（配置 AI / 完善资料 / 导入岗位 / 制作简历 / 求职助手 / **数据备份**），并把已有步骤的措辞更新到当前行为——截图识别、通用简历、纯手动配置预设、最大输出 Token「不限制」、助手可以直接读写数据（但永远不会删除）、以及数据集导出与切换。
  - `docs/user-guide.md` 一直是新的，只有这份应用内的指南停在最初版本；而它才是用户（尤其新用户）唯一会主动打开的那份。
  - 新增 `AGENTS.md` 的「使用指南同步」一节，把"用户可见的新功能要同步改哪一处、什么情况可以不改"写成规则，供后续维护者与协作代理遵守。
  - 指南里每一步的 `path` 由测试对着 `App.tsx` 的真实路由表校验：改了路由名而没改指南，测试会直接失败并点名是哪一步；步骤标题的唯一性与每步的文案完整性也在同一处守着。

### Fixed

### Changed

## 0.4.0 - 2026-09-16

### Added

- 求职助手页面现在会**显示正在生效的助手技能**：页头常驻一枚「技能：<名称>」标识（点它直接去设置页管理），让"回答为什么是这个风格"在界面上可解释——此前技能只在服务端改变作答方式，界面完全看不出来。没有启用任何技能时，空会话的引导区会说明**技能这个功能存在**并给出入口，用完一次就自动退场，不与页头重复。
  - 页头只列前两个名字，更多时收成「等 N 个」，长名字不会把标题挤变形。

- 大模型配置的「快速预设」新增**纯手动配置（不套用任何预设）**：选中后清空 `Base URL` 与模型名称，从空白开始填（API Key 不受影响），适合走中转站、公司网关等不属于任何内置厂商的地址。它与既有的「自定义模型（OpenAI 兼容）」区别只在选中时的行为——后者**保留**当前内容，用来避免误点后丢失已填好的接口信息；保存后下拉也会停在「纯手动配置」上，不会跳回「自定义模型」。

- 新增**通用简历**：在「我的资料」页可以编写**不针对任何岗位**的跨行业简历，用于方向还没定的投递。可以**AI 生成**（从资料一键生成，不做岗位定向）也可以**从头手写**，可以有多份并各自命名、重命名、删除。通用简历同样出现在简历中心（目标岗位列显示「通用简历」标签与求职意向）。
  - **生成时"全貌优先"**：不做岗位相关性筛选，也不重排——各方向的经历、校园经历、项目、技能和奖项都保留（只受单份简历的篇幅预算限制，超出的部分会在结果的告警里说明）。这与"把岗位传成空"完全不同：后者会静默丢掉校园经历与奖项、清空个人总结、并且只保留被经历提到的那些技能。
  - 求职意向沿用资料里用户自己填的那份，生成时不会编造岗位名；个人总结也不会出现"面向某某岗位"这类绑定岗位的表述。
  - 后端新增 `has_job` 列表过滤（`false` 只取通用简历）与 `PATCH /api/resumes/{id}` 改名接口（只改名称，不动正文与告警）。`POST /api/resumes/generate` 的 `job_id` 变为可选，并新增可选的 `title`。
  - 无关联岗位的简历记录本身一直是合法的（`job_id` 可空），因此**本次不需要数据库迁移**；但 `ManualResumeModal` 此前在无岗位时会提前返回、资料都不加载，导致通用简历根本打不开编辑器，这一处一并修好。
  - 简历中心与详情弹窗的「通用」标记一律按 `job_id` 判断：无岗位记录的 `job_title` 存的是求职意向，按它判断会把通用简历显示成岗位简历。
  - 「我的资料」页很长，通用简历卡片在页面底部，因此页面头部加了一个「通用简历」直达按钮。

- 「手动添加岗位」和「我的资料」的识别支持**图片输入**：可以直接在文本框里按 Ctrl+V 粘贴截图，也可以选择本地图片文件，一次最多 4 张（单张 ≤ 2 MB、合计 ≤ 5 MB，png/jpg/webp/gif）。图片与粘贴文本可以同时给，两边都参与识别。
  - **图片识别的防虚构做法**：模型在同一个请求里先**逐字抄录**图片文字（`transcription`），字段必须逐字出现在这份抄录里才会被采纳——沿用文本识别那套来源锚定校验。需要说明的是，这个保证比文本路径弱：它证明的是"字段与模型自己写下的文字一致"，而不是"字段来自用户给的材料"，模型把幻觉写进自己的抄录就能通过。因此识别的抄录原文会回传给前端供对照核对（`recognized_text`，默认折叠展示），图片模式的结果也会额外附一条「请对照截图核对」的提示。
  - **发了图片但模型没抄录时直接判失败**，不做"看起来成功"的降级：否则「文本+图片」的请求里，粘贴文本中合法锚定的字段会全部活下来、结果被标成 AI 识别成功，而图片其实一个字段都没贡献。
  - **识别图片需要多模态模型**。当前配置的文本模型会返回 HTTP 400，此时接口仍按既有契约返回本地草稿，并给出明确提示（「图片识别失败，可能是当前模型不支持图片输入（需要多模态模型）」）；未配置模型时也会说明图片识别无法进行，而不是含糊地说一句"已使用本地规则识别"。
  - 图片识别的请求体上限沿用助手的 5 MB 合计：base64 编码后约 6.7 MB，仍在默认 8 MB 的 `MAX_REQUEST_BODY_MB` 之内，因此这两个接口**不需要**也不应该加入 `larger_body_paths` 豁免。
  - 岗位弹窗在识别结果为空时**不再清空表单**：图片识别失败时的本地草稿是全空的，无条件回填会把用户已经手填的标题、公司一起抹掉。现在只有识别出内容才回填。
  - 附件校验原语抽到 `services/attachments.py`，助手与识别接口共用同一份图片白名单、魔数检查与体积上限；`assistant_service` 保持不变地 re-export，助手侧行为与错误文案逐字未动。
  - 前端把附件限值、类型判定与 `readAsDataUrl` 抽到 `utils/attachments.ts`（此前 `readAsDataUrl` 有两份拷贝），并新增共用的图片暂存 hook 与组件。
  - 请求与响应新增字段：`images`（请求）与 `recognized_text`（响应），两者都是向后兼容的可选/默认值。

- 求职助手支持导入**技能**：一份技能可以只是一个提示词（`.md`），也可以是提示词加一包知识文件（`.zip`，需要一份 `SKILL.md` 加若干 `.md` / `.txt` 资料）。在 **设置 → 助手技能** 里导入、启用/停用或删除。
  - 技能有两种作用方式：**提示词是用户主动导入的指令**，拼进系统提示约束助手作答；**知识文件是不可信资料**，与岗位描述、经历参考文件同级，复用同一套注入清洗流水线，其中出现的命令、角色设定或格式要求都不会被当成指令执行。这条切分写进了系统提示，也写进了测试。
  - **知识不会预加载**：助手经 `read_skill_knowledge` 工具按需读取，整包资料不会挤占上下文；不指定文件时按当前问题从各知识文件里挑相关片段。
  - 导入同名技能按**覆盖更新**处理——提示词与知识文件整体替换、启用状态保留，方便对着同一份技能反复调整。
  - 技能提示词与知识读取都有长度预算：被截断或被跳过的技能会在系统提示末尾点名说明，不做静默丢弃（只加载半个提示词会让助手表现异常，而用户看不到原因）。
  - 导入链路把 **zip bomb、路径穿越、加密成员、非法扩展名、成员数超限**都挡在 `services/skill_archive.py`：逐成员校验路径与解压后大小、检查压缩比，解析只走白名单成员，不使用 `extractall`。
  - 新增 `0007_assistant_skills` 迁移（`assistant_skill` / `assistant_skill_file` 两张表），并同步登记进 `_APPLICATION_TABLES`，使迁移前备份、旧库识别与备份校验继续覆盖新表。
  - **旧备份仍然可以导入**：备份校验要求包里包含全部应用表，新增数据表本会让所有旧备份被判成"缺少数据表"而拒收。现在校验前先把解出的候选库升到当前 head，加表不再是一次不兼容改动；版本方向与清单一致性比对仍排在迁移**之前**，否则迁移后 revision 恒等于 head，比对将永远成立。
  - 校验用的候选库迁移时**不再生成迁移前备份**：它只是压缩包解出来的一次性副本，原始压缩包还在，而那份备份会落在 `restore/` 目录里按导入次数累积。
  - 系统提示改为每条消息现拼，技能改动（以及提示词文件本身的改动）**不再需要重启应用**。

### Fixed

- **修复导入技能时技能名变成一串随机 UUID**：上传走的是裸二进制请求体，原始文件名根本进不到后端，于是"没有 frontmatter 的 `name` 就用文件名"这条兜底只能拿到落盘用的随机临时名。现在前端用一个请求头把原始文件名带过来（头只能放 latin-1，中文名先 URL 编码），真实文件名既当显示名也当兜底技能名。
  - 连带的**更严重症状一并消失**：技能名是"同一个技能"的判据，名字每次都随机的话，**同一个文件反复导入会不断堆出新副本**，而「助手技能」卡片上写着"导入同名技能会覆盖更新"——现在这句话才真的成立。
  - 请求头来自客户端，所以后端只把它当显示名：先剥掉路径部分（`../../evil.md` → `evil.md`）和不可打印字符再入库。
- 修复求职助手**每次进入空会话都会闪一下**：首次加载时"还没有会话"和"会话正在加载"在详情为空时长得一样，页面分不清，于是先画出引导、被骨架屏顶掉、再画回来。现在会等会话列表与详情都确定下来再决定画哪个。（回归测试用 `MutationObserver` 数空态元素的挂载次数，闪一下就会留下 `remove` + `add`。）
- 修复 `scripts/bump_version.py` 在中文 Windows 上**算完版本号却崩在最后一行提示**：控制台编码是 cp936，而"本次包含新增数据库迁移"那行提示里的 `⚠` 无法编码，抛 `UnicodeEncodeError` 让本该成功的 dry-run 以非零退出。现在输出流降级为 `errors="replace"`：无法编码的字符变问号，中文提示照常显示。

- **修复了三个会让"干净电脑双击 start.cmd"直接失败的问题**（全部在真机上复现；CI 因为跑在英文 locale、无空格路径、固定 Python 3.12 上，一个都抓不到）：
  - **中文 Windows 上 `pip install` 直接崩**：`backend/requirements.txt` 首行是中文注释，而 pip 24.x 对无 BOM 的 requirements 文件会回退到 locale 编码（中文 Windows 是 cp936），抛 `UnicodeDecodeError` 后首次安装即中止。现在该文件保持纯 ASCII，且启动器在**任何 Python 子进程启动之前**就设好 `PYTHONUTF8=1`（原来是装完依赖才设，只保护了 uvicorn）。同一文件也影响 `pip install -r requirements-dev.txt`，因为它是 `-r requirements.txt`。
  - **路径含空格时前端永远起不来**：前端启动手写了 `cmd /s /c ""<npm.cmd>" run dev …"`，而 `Start-Process -ArgumentList` 不加引号、`/s /c` 又会剥掉首尾引号，于是 `C:\Program Files\nodejs\npm.cmd` 变成裸路径，cmd 报 `'C:\Program' is not recognized`。现在把 `npm.cmd` 直接交给 `Start-Process -FilePath`，由它自己套好 cmd 包装——记到的进程仍是 `cmd.exe`，`stop.cmd` 的识别不受影响。
  - **Python 3.14 装不上锁定的依赖**：`py -3` 会挑最新的解释器，而 `pydantic-core==2.33.1` 没有 cp314 轮子（`No matching distribution found`）。现在支持的版本窗口是 **3.10 – 3.13**，按 `-3.12`、`-3.13`、`-3.11`、`-3.10` 依次探测（3.12 优先，与启动器自身安装的版本和 CI 一致）；只有 3.14 的机器会明确提示版本窗口并走既有的自动准备路径装一个 3.12。若 `.venv` 是超出窗口的解释器建的，会移到 `runtime/venv-unsupported-<时间戳>/` 后重建，而不是反复用同一个坏环境失败。
- 启动链路的其它修复：
  - `start.cmd` 现在用 `%*` 转发参数，`start.cmd -BackendPort 8010` 可用（此前 README 文档化了这些参数，而端口被占用时启动器恰好提示用户用它，却传不进去）。
  - 启动失败的清理改用 `taskkill /T`，不再孤儿化 `cmd → npm → node` 进程树（此前会残留 Vite 占着端口）。
  - 健康检查的超时从固定 30 秒改为按服务区分（后端 90 秒、前端 120 秒：冷启动要跑迁移，且杀毒软件会扫描刚装好的依赖），并在**子进程已退出时立即失败**、把退出码写进错误信息，不再白等满超时。
  - 依赖是否就绪改为让解释器**真的 import 一次**，而不是检查四个目录是否存在——缺 pydantic 之类的半截环境此前会被判成"已就绪"，然后以"启动超时"的形式失败。
  - Node 便携包改为先试国内镜像（华为云、npmmirror、腾讯云，实测三者与官方产物字节一致）、再回官方源，**每个候选都校验 SHA-256**（期望值来自仓库常量而非镜像），并把总下载预算限制在 30 分钟——此前是每个地址两次 × 1 小时，网络不通时表现像卡死两小时。
  - 打不开浏览器不再连带停掉刚启动的服务（无默认浏览器关联的机器上 `Start-Process <url>` 会抛错并触发回滚）。

### Changed

## 0.3.0 - 2026-09-15

### Added

- 岗位和个人资料粘贴识别支持基于用户当前模型配置的结构化 Prompt 抽取，并返回识别来源提示。
- 设置页「最大输出 Token」新增「不限制」选项：勾选后发往模型的请求体不再包含 `max_tokens`，输出上限改由服务商和模型决定，用于缓解推理型模型把思考过程计入输出预算、预算偏紧时正文被挤空的问题。该选项以 `0` 保存，不需要数据库迁移。
- 求职助手支持**工具调用**，可以直接操作项目数据：查询岗位、简历与个人资料，并在用户明确要求时新增/修改岗位、更新个人资料的基础字段（教育/工作/项目等结构化条目仍需在页面编辑）。
  - **不提供任何删除类工具**，模型即使判断失误也造不成不可逆损失；设置里的模型配置与数据集管理有回环强制校验，不做成工具以免模型间接绕过该边界。
  - 「更新个人资料」工具采用 read-modify-write：资料接口是整份替换语义，只提交模型给出的字段会清空姓名、电话、照片和全部经历条目。
  - 模型侧按 index 累积拼接流式返回的工具参数碎片；服务商不支持 tools 时自动降级为普通对话并在回复里说明，不让功能整体不可用。
  - 每次工具调用都会在回复中直接列出（成功的可跳转到对应页面），并记录在消息的 `context` 里，历史对话回看时同样可见。
  - 提示词同步更新：只在用户明确要求时写入，且**工具返回值与其它输入一样属于不可信数据**（岗位描述里可能夹带注入指令）。
- 新增「数据集」：设置页可把全部本地数据（岗位、个人资料、照片、经历参考文件、简历、收藏、助手会话与附件、模型配置记录）导出为一个 zip，换电脑后在新装的 ResumeForge 里导入即可完整还原；也可以在本机保留多份数据集并随时切换。
  - **导入的默认语义是新建一份数据集，不触碰当前正在使用的数据**——选错文件不再有损失。切换会把整个应用切到那份数据，页面自动重载。
  - 每份数据集是一个完整的 SQLite 文件（照片与附件都以文本存在库内），切换只是把引擎指过去，不搬运数据，因此不存在"改动没写回原数据集"的隐患；有正在进行的 AI 请求时切换会返回 409，避免那次请求的结果被写进刚切过去的数据集。
  - 删除数据集只移入 `data/datasets/.trash/`，不做永久删除；正在使用的数据集不可删除。
  - 切换时按新库补跑建表/补列/Alembic 迁移，因此导入一份来自旧版本的数据集也能正常使用。
  - **备份中不含大模型 API Key**（清空操作配合 `VACUUM` 重建文件，避免删除过的配置记录把明文密钥残留在空闲页里），恢复后需重新填写并测试连接。
  - 备份包记录导出时的 Alembic revision，旧版本会明确拒绝导入更新版本的备份，不做静默降级。
  - 新增 `MAX_BACKUP_UPLOAD_MB` 配置项（默认 512）单独放宽备份上传的体积上限，其余接口仍受 `MAX_REQUEST_BODY_MB` 约束；恢复相关端点要求本机回环访问。
  - `backend/data/backups/` **保持不自动清理**：它存放迁移前与恢复前的自动备份，属于用户的安全网，自动删除其中的旧文件风险更大；改为在用户指南、升级文档和 README 中说明该目录会持续累积、可手动清理。
  - 它**不是真的无限**：部分服务商（如 DeepSeek 的 `deepseek-chat`）默认上限就是 4096，勾选后可能比手动设置的值更小。
  - 回退到旧版本前请先把该字段改回 256 以上的具体数值，旧版本会把 `0` 判定为非法并重置整份模型配置。

- Windows `start.cmd` 在找不到可用 Python 3.10+ 时会优先尝试 `winget` 用户级安装，并在失败后使用固定 SHA-256 校验的 Python 官方 x64 安装器兜底；安装完成后会刷新 PATH 并重新探测解释器。
- Windows `start.cmd` 可自动探测并准备 Node.js 20.19.0+/npm：优先复用现有环境或通过 `winget` 用户级安装，失败时使用固定 SHA-256 校验的 Node.js 24.19.0 官方 x64/ARM64 便携包；新增离线启动器回归测试并接入 Windows CI。
- 求职助手会话支持置顶和收藏，置顶会话在全部列表中自动排在最前，侧栏仍可按收藏筛选；新增 `0006_chat_conversation_flags` 数据库迁移并兼容已有 SQLite 数据。
- 置顶或取消置顶不再刷新会话更新时间，取消置顶后会话恢复到置顶前的时间排序位置。
- 聊天图片附件支持即时显示、紧凑缩略图和点击查看大图，避免等待助手响应期间图片暂时消失。

### Fixed

- 收紧 AI 粘贴识别的原文锚点校验：本地规则未识别到的字段不再接受模型返回的无依据内容；补充岗位和个人资料接口在 AI 成功、AI 失败和未配置模型时的回归测试。
- 放宽 Windows 首次启动时 pip/npm 依赖下载的读取超时并增加有限重试；改用关键运行包和 Vite 可执行文件判断依赖完整性，使下载中断后重新运行启动器可以自动修复残缺环境。
- 修复 Windows 一键启动器从资源管理器启动时在项目根目录执行 `npm ci`、误报缺少 `package-lock.json` 的问题；缺少锁文件的旧压缩包会安全回退到一次 `npm install`。
- 修复启动失败清理阶段对空前端进程调用 `Stop-Process` 的异常，并支持通过 `py.exe -3` 检测真实 Python 解释器，避免 Microsoft Store 别名导致误报。
- 探测 Node.js/npm 版本后不再把 PowerShell 5.1 报告的 `$LASTEXITCODE` 残留给调用方：此前该值可能为 `-1`，导致通过 `-Command` 包装调用启动器的环境（如 Windows CI）在启动成功后仍被判为失败。
- 修复 `stop.cmd` 在非 UTC 时区上无法停止服务的问题：进程记录改存 Unix 秒用于启动时间比对，此前记录里的 UTC 时间被按本地时间重新解析，差值恒等于时区偏移，启动器会一直以“记录与进程不符”为由拒绝停止。
- 修复岗位详情抽屉的操作按钮栏：内容不足一屏时按钮浮在正文中间，内容可滚动时下方内容又会从吸底栏与容器底边的缝隙透出。操作栏改用抽屉自身的 footer 插槽常驻底部，正文仍由内容区独立滚动。
- 修复助手会话置顶迁移在 SQLite 外键级联下重建父表、可能清空历史消息的问题；新增迁移回归测试并为会话列表增加稳定的 ID 排序兜底。
- 修复助手历史消息恢复后按更新时间显示异常的问题，保留会话原始更新时间并确保置顶状态变化不会污染排序。
- 前端 CI 使用受控 Vitest worker 数和更宽的异步测试超时，避免共享 runner 资源紧张时误报测试失败。
- 设置页 API Key 回归测试等待异步配置回填完成，避免慢速 CI 环境把初始空值误判为失败。

### Changed

- 粘贴识别采用“AI 优先 + 本地规则兜底”：模型只抽取不可信文本中的已有事实，输出经过 JSON、字段长度和原文锚点校验；未配置模型或调用失败时继续使用原有本地解析结果。

- 一键启动器默认后端端口调整为 8005；手动开发仍使用 8000，并可通过 `-BackendPort` 参数覆盖，相关文档已同步说明。
- 个人资料文本解析器按职责拆分为独立模块，保留 `app.services.profile_text_parser` 原有入口和解析行为，便于后续维护规则而不影响现有数据。
- 岗位粘贴文本解析按职责拆分为 `services/job_parser/`，JD 技能/学历/年限规则拆分为独立模块；保留 `job_text_parser` 与 `jd_parser` 原有导入入口和解析行为。
- 岗位相关性筛选与简历生成按职责拆分为资料上下文、岗位匹配、参考事实、Prompt 预算、JSON 规范化、事实回填、一致性检查和质量门槛模块；保留原有服务入口和导出符号，未改变生成行为。
- 前端共享类型、资料/设置/岗位/简历编辑页面与 CSS 按职责拆分为独立模块；原有类型入口和页面入口保留兼容导出，交互行为与样式契约不变。
- 后端 FastAPI 装配迁移到 `app/application.py`，早期 SQLite 兼容字段集中到 `app/database_compat.py`；`app.main` 继续导出 `app`、生命周期、健康检查和兼容常量，启动命令无需修改。
- 将岗位解析、资料解析、助手搜索、简历质量重试等超长测试按主题拆分为独立测试模块；岗位元数据、英文标题和分隔符场景现独立维护在 `test_job_text_parser_metadata.py`。仅移动测试代码并保留原有断言，新增模块会在 CI 中一起收集。
- Windows 启动器按公共工具、Python/Node 运行时和进程管理拆分为 PowerShell 模块，`Start-ResumeForge.ps1` 保留原参数和启动入口。
## 0.2.0 - 2026-08-21

### Added

- Windows 一键启动与停止入口：`start.cmd` 会在首次运行时准备缺失依赖、校验本地端口和代理连接、启动前后端并打开浏览器；`stop.cmd` 仅停止由启动器记录的 ResumeForge 进程。
- 首次访问使用引导、左侧常驻使用指南入口，以及面向用户的升级/备份/回滚文档。
- 完整用户使用手册，涵盖模型配置、资料维护、岗位导入、简历制作、求职助手、隐私边界与故障处理。
- 开源仓库徽章、社区行为准则和 npm 包仓库元数据。
- 岗位关联的用户手写简历流程，可从个人资料预填并在简历中心区分“AI 生成”和“用户编写”。
- 岗位和简历收藏状态，以及统一展示两类收藏内容的分页收藏夹。
- 岗位需求解读：仅依据招聘原文按需生成结构化总结、带原文证据的需求和通用求职建议。
- AI 求职助手：支持本地会话历史、SSE 流式回复、停止生成、显式关联岗位/简历/脱敏资料、文本附件、兼容模型的图片附件，以及可选的 Bing RSS 联网搜索。
- 简历预览“编辑”模式，可点击或使用键盘选择纸面内容并定位到结构化编辑器字段。
- 设置页为 `temperature`、超时时间和最大输出 Token 增加悬停说明。
- GitHub Actions 跨平台后端检查、前端测试/构建检查和非阻断依赖审计。
- Ruff、覆盖率、统一换行和编辑器基础配置。
- 贡献指南、安全政策、PR 模板和 Dependabot 更新配置。
- 项目根目录 `AGENTS.md`，集中记录渐进式改造、测试、安全、文档和发布维护规则。
- 可独立创建空库且支持降级往返的 Alembic 版本化迁移，以及检测到任一业务表用户数据时的升级前 SQLite 自动备份。
- Alembic `0003_job_additional_info`、`0004_resume_favorite`、`0005_chat_assistant` 和 `0006_chat_conversation_flags` 迁移，兼容补充岗位其他信息、旧简历收藏默认值、助手会话历史表及会话置顶/收藏字段。

### Changed

- 个人资料局部粘贴识别采用合并草稿策略：本次文本未提供的既有字段不会被清空，方便分段补录后再核对保存。
- 删除配置记录只移除记录本身；即使删除当前记录，当前正在使用的模型配置仍会保留，之后可以重新保存为记录。
- 联网搜索先解析受限的候选结果集，再执行招聘相关性过滤，避免无关结果过早占满可用结果位。
- 发布质量清理：移除已确认的无用导入并启用 Ruff `F401` 检查，同时复核忽略规则、文档链接和本地数据文件边界。
- ESLint 忽略 Vite 运行时缓存，避免一键启动后把生成的第三方模块误判为项目源码。
- 移除未被项目使用的 `httpx2` 开发依赖，减少安装和供应链审计范围。
- 完善 README 与架构文档中的简历一对多、生产部署、数据库升级和测试说明。
- 设置页提供明确的“自定义模型（OpenAI 兼容）”预设入口，已有自定义配置可正确回显；预设切换会同步服务标识，无需鉴权的本地兼容服务可留空 API Key，配置记录与既有数据保持兼容。
- 设置页会根据规范化后的官方 Base URL 正确回显历史 DeepSeek 配置，即使旧数据的展示标识仍为 `custom`；自定义模型名称保持不变。
- API Key 框默认隐藏配置记录内部引用，用户点击眼睛时才临时显示当前已保存密钥，隐藏、取消、保存、修改地址或切换配置后立即清除显示值。
- 首次访问和常驻使用指南扩展为五步，并同步当前资料、岗位、简历、收藏和求职助手工作流。
- 临时开发虚拟环境不再纳入版本控制；保留现有 Git 历史，不做破坏性重写。
- 增强跨行业岗位粘贴识别：除互联网岗位外，扩展医疗、教育、制造、零售、物流等常见职位、组织、技能和职业资质表达；支持中英文/编号分区、字段同义词、全角文本、同一行多字段头部、常见英文月份日期，以及 Markdown 或方括号包装的职责/要求标题。规则仍先生成可编辑草稿，不保证覆盖所有网站模板。
- 修复招聘官网紧凑文本的岗位识别：支持从“地点-岗位名(职位编号)公司 + 官网链接”中拆分职位、公司和链接，从压缩元数据行提取地点、校招类型及发布日期，并将“职责要求”正确归入任职要求而非职位描述。
- 岗位新增“其他信息”字段，将福利、团队/公司介绍、职位 ID、部门、工作安排及申请流程与职责、要求分开保存；该字段参与搜索、技能标签、岗位相关性和简历生成上下文。
- 岗位广场将原“更新时间”改为招聘“发布时间”；解析只接受明确发布语义，“更新于/last updated”保留在其他信息，无法确认时显示为空。
- 自行编写简历时在编辑器旁展示只读岗位职责、要求、技能和其他信息，方便边写边对照。
- 增强个人资料粘贴识别：支持 `Employment Dates` 等经历字段、校园组织信号、连续无空行的多段经历，以及无标题的“组织/公司 + 角色 + 日期”结构；异常超长字段会安全截断并提示核对。
- 岗位技能标签统一常见缩写/别名（如 `K8s`、`Golang`、`Fast API`、`Postgre SQL`），能识别紧凑的技术技能列表，同时过滤日期年限、框架后缀、普通英文词和 `closed-loop` 等造成的误判。
- 缓存粘贴解析中的可复用正则，降低大段合法输入的解析延迟。解析仍为本地规则，结果保存前可编辑核对。
- 助手联网搜索会压缩可识别的求职问题、过滤缺少招聘语义的词典/百科/诗歌等结果；找不到直接相关来源时不再展示无关链接。助手回答以安全的轻量 Markdown 呈现标题、重点、列表、代码和 HTTP(S) 链接，提升可读性而不执行模型生成的 HTML。
- 求职助手支持常用 Markdown 表格的响应式展示；回答生成期间提供动态状态提示，消息气泡会按内容自适应宽度，联网参考来源默认收起并可按需展开。
- 优化求职助手的历史对话与输入区：悬停可查看完整对话标题，管理操作按需显示；回答中的 Markdown 或裸 HTTP(S) 链接均可直接访问，关联上下文与开关控件会随屏幕宽度合理换行。
- 求职助手历史记录现以三点菜单收纳重命名和删除操作；超长标题悬停时会滚动展示，同时保留完整标题提示。输入区将关联选择器与开关移至不同层级，减少视觉拥挤。
- 修复求职助手查询“最近招聘信息”时被 Bing RSS 误解为首字检索的问题：互联网企业招聘会使用稳定的短查询，并保留官网常见 `Careers` / `Jobs` 页面；模型不会把未标注日期的摘要误称为刚发布招聘。
- 调整求职助手历史记录标题区宽度；超长标题以右侧渐隐效果收尾，同时在悬停时平滑滚动展示完整内容。
- 移除求职助手历史记录三点操作区的 Ant Design 默认左侧大间距，使标题与操作按钮的布局更紧凑。
- 求职助手的新对话会展示可点击的常用提问提示；回答完成后的会话刷新保留已显示消息，并在绘制前直接定位最新内容，避免先跳回顶部再滚动到底部。

### Removed

- 移除手动添加岗位时通过招聘链接自动读取并填充的实验性功能及其网络抓取、官网适配和模型整理链路；保留本地招聘文本识别与可手动填写的投递链接字段。

### Security

- A4 HTML 导出为固定脚本生成一次性 CSP nonce，并收紧内联脚本策略；简历内容仍按原有转义和本地资源边界渲染。
- 生成简历发送给模型的上下文不再包含照片、姓名、联系方式等身份字段，身份信息仅在本地结果组装时恢复。
- 将岗位 JD 和总结文件内容明确隔离为不可信模型资料，提示模型忽略其中夹带的命令或角色设定。
- 设置接口和配置记录只返回 API Key 脱敏占位符，并校验脱敏引用绑定的 Base URL，避免误发到其他主机。
- API Key 显式查看使用独立 POST 接口，仅接受直接连接本机回环地址的请求并设置禁止缓存响应；普通读取、保存和记录接口继续保持脱敏。
- 求职助手只在用户显式选择时加载岗位、简历或脱敏个人资料，且没有直接写入项目数据的权限；会话历史和模型上下文均有条数与字符预算。
- 助手附件增加扩展名、MIME、UTF-8、图片签名、base64、单文件和总大小校验；历史图片不会在后续轮次重复发送。
- 助手联网只访问固定 Bing RSS 端点，限制响应体和结果数、不跟随结果链接、拒绝 XML DTD/实体并把摘要视为不可信数据；招聘搜索提示优先官网，但仍要求用户核验来源。
- 关闭 HTTP 客户端包含完整 URL 的 INFO 请求摘要，避免助手联网搜索词进入本地日志；保留不含敏感查询参数的业务错误日志。
- 限制 OpenAI 兼容上游仅使用回环 HTTP 或 HTTPS，拒绝凭据、查询参数和片段；统一处理异常响应并限制响应体大小，日志不记录上游正文。
- 增加请求体大小边界、请求 ID 和友好的未处理异常响应。
- 修复 SSE 生成流程的持久化顺序：简历成功落库后才发送 `saved`/`done` 事件。
- 升级 FastAPI/Starlette、React Router、pytest 与 pytest-asyncio 到已修复/兼容版本；Python（含开发依赖）和 npm 生产依赖审计均不再报告已知漏洞。

## 0.1.0 - 2026-08-17

### Added

- FastAPI、React、SQLite 构成的本地简历管理基础版本。
- 岗位、个人资料、AI 定制简历、历史记录、搜索和多格式导出功能。
- OpenAI 兼容大模型配置，以及基于事实的一致性检查。
