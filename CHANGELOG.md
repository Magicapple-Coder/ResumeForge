# 变更记录

所有值得关注的项目变化都会记录在此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## Unreleased

### Added

### Fixed

### Changed

## 0.9.0 - 2026-09-20

### Added

- **首页快捷入口可自定义 + 近期提醒默认列表**。首页「快捷入口」卡片可点「编辑」自行增删与排序（从侧栏全部菜单里勾选，localStorage 持久化，至少保留一项）；「近期提醒」卡片默认回到列表样式，卡片右上角可切「列表 / 月历」，偏好同样记住。
- **投递记录详情抽屉 + 投递队列右键菜单**。投递记录的失败信息（含页面 URL / 标题 / 批次 / 第几次尝试）不再塞进表格单元格，点「详情」在右侧抽屉完整查看；表格去掉横向滚动条、长文本硬截断。投递队列支持**右键**呼出操作菜单（编辑 / 移出队列），操作按钮右移。
- **自动采集条件历史**。保存采集条件后记一份快照（去重、上限 20），下次可从「历史条件」下拉一键回填，也可清空。
- **题库与面试复盘：生成自动保存历史 + 历史富还原**。题库生成、面试复盘分析完成后**自动**写入历史（无需手点保存）；点开历史记录用与刚生成时**完全相同**的交互还原——题库的「参考答案」、复盘的「反向优化简历」都能点，新生成的结果回写到同一条历史。旧版纯文本历史仅做兼容展示并提示"仅可查看"，不崩。
- **简历版本对比改成字段级视图**。不再把两份简历的 JSON 当源码逐行展示，改为按字段分区、只显示有变化的区块，字符串行内词级高亮、列表条目 +/- 标记、照片显示 [图片]；「查看原始差异」折叠作为兜底。
- **开发调试：接入 code-inspector**。本地 `npm run dev` 时按住 Alt+Shift（Windows）点击页面元素，自动在 VS Code 里定位到对应源码行（仅开发环境生效）。

- **「匹配度参考分」接上界面**（此前只有后端与文档，界面上看不到）。岗位详情 →「匹配度分析」→ 五类结论**之后**新增「匹配度参考分」：0–100 的圆环 + 5 个分项（技能覆盖 / 年限 / 项目相关度 / 硬性门槛 / JD 关键词覆盖）的横条，每条带一句可读依据（如「核心/加分项共 18 条，已匹配 6 条」）。三条口径约束写进代码与用例：① 它是**纯本地规则**现算的派生值，不调模型、不落库；② **不参与投递准入**——能投 / 需确认 / 不投仍只看五类结论；③ **强制原样展示后端下发的免责文案**（"不代表真实 ATS 解析结果或投递成功概率"）。分数用**单一色相**画、不按高低变色：后端没有定义档位，前端自己划"好/差"的线就是在编造一个它没有的判断。放在结论区之后而不是之前，也是同一个理由——不能让它盖过准入结论。

- **求职统计扩成四个主题区块，并补上"没有数据时不许画零"的诚实口径**。此前这一页只有 4 张指标卡 + 漏斗 + 趋势图，下半屏大片留白；排查后发现真正的问题不是图表不够，而是**没有数据可画**——趋势图按 `applied_at` 分月，而当时 3 条投递记录**一条都没填投递日期**，所以它不是"这几个月没投递"，而是每一行都缺日期。因此本批分两步：① **让数据被填上**（见下方 Fixed 里表单的改动）；② **把指标与版面补起来**，并且**任何一张图都不许渲染会撒谎的零**。新增四个区块：**转化与卡点**（进行中 / 超过 7 天没有更新 / 没有下一步 + 六阶段漏斗）、**时间与节奏**（月度趋势 + **周内投递分布** + 最近 7/30 天新增 + 待办提醒四档紧急度）、**渠道与去向**（内推转化与内推状态分布、**投递最多的公司**按 `company_key` 归并、记录来源）、**简历与健康度**（简历份数、带一致性提醒 / 带【待补】的份数、待确认主张数、**投递-简历关联覆盖率**）；概览另补 Offer 率。**诚实口径**有两层：新增 `applied_date_gap`（有 / 没有可用投递日期的条数）驱动一条 `DataGapNotice`，如实说明"有几条未填、未计入"；当一张图的输入**全空**时渲染明确空态而不是一排零轴——一张全零的趋势图会被读成"这几个月真的一份没投"。**跨模块复用而非各写一份**：`services/ratios.py`（比率唯一实现，放叶子模块以避开 `analytics ↔ referral_service` 成环）、`models/tracker.py` 的 `STALLED_DAYS` / `is_stalled`（"卡住"口径，与 `/api/stats` 同源）、`referral_service.referral_stats` / `referral_status_counts`（内推转化只认 `_is_converted`）、`reminder_service.reminder_urgency_counts`（四档阈值只此一处）、新增 `services/resume_health.py`（复用 `resume_completeness.find_incomplete` 与 `VERIFICATION_PENDING`）。**刻意不提供**的指标也写进了 `services/analytics.py` 的 docstring，免得被反复提议：平均"投递→面试"天数 / 各阶段流失率 / 面试通过率（**没有状态历史表**，只有当前状态 + 一个 `status_date`，算不出来）、按行业分布（全库无此字段）、按岗位类型看转化与每份简历的表现（`job_id` / `resume_id` 在录入界面上**没有入口**，是结构性为空，所以本轮只把关联覆盖率显示出来让缺口可见）。口径命名守住一条线：`updated_at` 会被任何编辑刷新，所以该指标叫「**超过 7 天没有更新**」而不是「面试卡住」。**前端零新增 SVG 组件**：抽出 `HorizontalBarChart`（漏斗与排行共用同一份几何与 `maxWidth` 封顶——封顶散成两份的话，上次"图表随窗口放大"的修复只会修到一半），`FunnelChart` 变成它的薄壳（**原有测试一行未改仍通过**，这正是"泛化没改变漏斗渲染"的证据），`BarChart` 放宽为通用 `label/count` 序列并参数化 `aria-label`（否则新增的周内分布会与趋势图撞同一个可访问名）；另加 `StatCard` / `SectionCard` / `DataGapNotice` 三个非图表组件。助手侧新增 `dashboard_brief` 投影：`get_analytics_overview` 此前把整个看板 dict 直接塞进上下文，新增约 25 个字段后会把长数组也带进去、稀释标量，现在保留全部标量、裁掉趋势/周内/内推状态并只留公司榜前 5。

- **知识库（新模块）**：侧栏在「资料箱」之后新增「知识库」，沉淀愿意反复查阅的**成文内容**——面经总结、简历技巧、求职策略、面试问答、公司信息、行业知识等。条目带标题、分类、标签与 Markdown 正文，支持搜索、按分类/标签筛选、新增、编辑与删除（删除走回收站软删除，可恢复）；正文预览复用助手的 Markdown 渲染。它与「资料箱」的分工是：资料箱放**还没成体系的零散材料**（证书、链接、笔记），知识库放**已经整理成文、愿意反复看的笔记**。求职助手能检索与增改知识库（`list_knowledge` / `get_knowledge` / `create_knowledge` / `update_knowledge`）。新增 `GET/POST /api/knowledge`、`GET /api/knowledge/categories`、`GET/PUT/DELETE /api/knowledge/{id}`。

- **内推增强：内推码 + 图片备注 + 状态分色**。内推记录现在可以记内推码（`referral_code`）与备注图片（`note_images`，如内推海报截图），列表按状态分色一眼区分「进行中 / 已投递 / 已关闭 / 无效」。两列由迁移 `0019` 补（只加列），业务读写走既有的 `/api/referrals`。

- **日历提醒上首页 + 紧急度分色 + 「打开应用时弹出提醒」开关**。提醒不再是「求职进度」页里的一个面板：首页也会展示近期提醒，并按紧急度分色——**逾期 / 24 小时内 / 3 天内**三档一眼看出哪件最急。设置里新增「打开应用时弹出提醒」开关（默认开，`GET/PUT /api/settings/reminder-popup`），关掉后启动不再弹近期提醒。

- **题库单题生成详细参考答案**：个性化题库里可以给单题生成详细参考答案（不再只有题目），方便提前自测后对照。

- **题库与面试复盘历史（可保存、回看、删除）**：个性化题库与面试复盘都可以**保存成历史记录**，之后回看或删除——不再是一次生成、关掉就丢。两张表 `question_bank_record` / `interview_review_record` 由迁移 `0019` 新增（带 `deleted_at` 软删除，删除进回收站可恢复）。求职助手能读这两类历史（`list_question_banks` / `list_reviews`）。

- **全局搜索扩域**：首页搜索不再只搜岗位与简历，还覆盖**内推 / 提醒 / 面经 / 事实台账 / 资料箱 / 助手技能**——一个框找到更多内容。

- **求职助手「了如指掌」：补齐 9 个读工具 + 3 个写工具，新增「能力地图」与「行为准则」**。为让助手对整个界面和数据库都答得上、且**实事求是、绝不编造**，系统提示重写：置顶「行为准则」（不知道就说不知道、只在你明确要求时才写入、涉及用户数据必须用工具查证后再答），并新增「能力地图」逐条说明 15 个功能域在哪、助手能帮什么。工具侧补齐**只读**的提醒 / 内推 / 面经 / 题库历史 / 复盘历史 / 知识库 / 求职统计看板 / 分享包列表，以及**写入**的知识库增改（`create_knowledge` / `update_knowledge`）与提醒新增（`create_reminder`）——写工具全部进入提示词「写入类工具」清单并与 `Tool.writes` 双向一致（有测试钉住）。

- **岗位匹配新增「参考分」：五类结论之外再给一个 0–100 参考分与 5 个分项，仅作展示辅助、不进入投递准入**。此前匹配分析只给「已匹配 / 表达缺口 / 证据不足 / 真实缺口 / 待确认」五类结论与准入建议，并且刻意不显示任何百分比评分——因为一个"看起来精确、其实是编的"分数比没有分数更误导人。参考分延续这条底线：它由 `services/match_scoring.py` 用**纯本地规则**现算（技能覆盖 / 年限 / 项目相关度 / 硬性门槛 / JD 关键词覆盖五个分项加权求和），**是派生值、不落库、不回写分析结果**，而且**不参与、也不改变五类结论与准入闸门**（「能投 / 需确认 / 不投」仍只由 `admission_of` 单源判定）；信号缺失时给中性分（50）而不是把"没信息"误报成"不匹配"。分数旁固定带免责文案（"不代表真实 ATS 解析结果或投递成功概率，投递准入仍以五类结论为准"），未配置模型走本地降级时不产分数，不假装做过 AI 打分。

- **简历写作增强（四个 LLM 变换 + 版本差异对比）**。编辑简历时，对一段经历描述可以做四种就地变换：**STAR 量化改写**（把"负责后端开发"改写成"情境 → 任务 → 行动 → 结果"并尽量量化；可挂一条台账主张作为「事实边界」，改写不会越过个人边界）、**话术生成器**（同一段事实一次生成简历版 / STAR 版 / 面试口述版三种表达）、**多风格润色**（大厂严谨 / 简洁技术 / 应届校园）、**中英互译**（忠实原意，数字 / 日期 / 术语不篡改）。四个变换都是「一段文本进、一段文本出」，结果直接回填到对应字段、零适配；未配置模型时接口返回清晰中文错误、不做本地降级（不伪造结果）。另有**版本差异对比**：任选两份简历看三态差异（新增 / 删除 / 不变），用本地 difflib 计算、**不调用模型**。

- **质量与合规检查（查重 / 敏感词 / 夸大风险 / 面试深挖风险点 / 合规校验 / ATS 本地检测）**。简历编辑页新增质量检查面板，把"这份简历投出去会不会踩坑"拆成六类：**查重**与**敏感词**在本机做（确定性规则、不含模型），**夸大风险**与**面试深挖风险点**联动事实台账——能说多强由「承担程度 / 个人边界」约束，越界的表述会被点出；**合规校验**覆盖保密 / NDA / 违规表述等红线；**ATS 本地检测**只做格式 / 关键词覆盖 / 信息位置三类静态检查，并带明确免责（它不是招聘系统的真实解析结果）。

- **面试与求职工具链（个性化题库 / 面试全流程 / 面试复盘 / 日历提醒 / 内推管理 / 求职数据看板 / 面经知识库）**。新增**个性化题库**（基础 / 项目深挖 / 反问 HR 三类，按关联岗位与资料生成）与**面试全流程**（录入真实面试问题 → AI 分析 → 反向优化简历）；面试后可**复盘**沉淀；**日历提醒**跟进投递与面试日程；**内推管理**记录通过人脉拿到的推荐机会并统计**转化率**（口径由关联的求职进度 `ApplicationTrack` 派生，进入面试及以上才算转化，写入侧不接受手填 `converted`）；侧栏新增「**求职统计**」数据看板；**面经知识库**沉淀面试经验。

- **导出与分享补齐（Word / 纯文本导出、自定义水印、一键隐私脱敏、离线分享包、本地模板市场）**。导出格式从 HTML / JSON / Markdown / PDF 扩到 **Word（docx）与纯文本（txt）**（Word 复用同一套版式口径、不引入第二套排版引擎），并支持**自定义水印**（HTML 用 CSS 覆盖层、PDF 用 pypdf 叠加、Word 写页眉；纯文本与 JSON 没有版面概念，请求加水印时报明确错误）与**一键隐私脱敏**（姓名 / 电话 / 邮箱 / 公司 / 学校等，始终基于脱敏内容导出）。新增**离线分享包**：把一份简历打包成脱敏 HTML/PDF + 只读快照 + 评论回传文件 + 本地 token，可选「只读」或「可评论」，发出去给别人看、不带真实敏感信息；以及**本地模板市场**（互联网大厂 / 国企事业单位 / 外企 / 应届校园四套预设）。

- **回收站扩展到后四张新表（内推 / 提醒 / 面经 / 分享包）**。此前回收站只覆盖岗位、简历记录、投递记录、事实台账、资料箱材料、助手会话六类；本批新增的内推、提醒、面经、分享包四张表也带 `deleted_at` 列，因此一并纳入 `TRASH_SPECS`，删掉后同样可恢复、可彻底删除，不再出现"删了就找不回"的半软删。注册表驱动的设计让这类扩展只需加一行。

- **简历生成改为后台任务：真实阶段条 + 显式取消 + 完成弹窗**。生成弹窗不再是"点完开始就锁死、只能盯着一串文字转圈"。前端新增结构化阶段条（**整理资料 → 筛选资料 → 调用模型生成 → 校验修复 → 完成**），阶段由后端 `progress` 文案映射、映射不上的只显示原文不硬凑；进度只显示"已接收 N 字"，**刻意不做百分比**——模型逐字流式输出、后端无法预知总长，任何百分比都只能靠猜。后端新增 `resume_generate_task` 表（迁移 `0017_resume_generate_task`，只加表、幂等、带完整 downgrade，旧备份仍可导入）与 `ResumeGenerateRunner` 单例后台线程——**借 `apply/task_runner.py` 的单线程纪律，但不用它的 CDP 内核**（生成只需把异步生成器转成同步消费，与浏览器无关）。接口新增 `POST /api/resumes/generate/tasks`（返回 `task_id`）、`GET /api/resumes/generate/tasks/{id}`（前端 1.5s 轮询）、`POST /api/resumes/generate/tasks/{id}/cancel`；旧 `POST /generate`（SSE）原样保留，不破坏既有流式语义与一批测试。三条数据一致性保证：**只在完成时落库简历**（取消/失败都不写半成品）、**取消标记 `cancelled` 且不落库**（线程在流循环里检查停止信号，`except asyncio.CancelledError` 标 cancelled，复制 `assistant_stream.py` 的取消范式）、**防重复**（已有进行中任务时返回 `409`，连点两次不会造出两份任务/两份简历）。前端关掉弹窗 = 后台继续（不再是 `maskClosable=false`），关掉后仍按 1.5s 轮询，完成后弹「简历已生成」通知并直接打开结果预览；配置页的「请勿关闭弹窗」文案同步改掉。前后端均有测试：阶段映射与字数计数、后端无 progress 不崩、完成落库、取消不落库、重复拒绝、后台状态流转、迁移表集合不变/旧备份兼容/可 downgrade。

- **简历预览在内容超出时不再"裁掉看不见"，而是展开成按 A4 高度切分的近似分页**。此前内容超出所选页数时，预览被 `overflow:hidden` 直接裁掉，屏幕上只剩一句"内容超出了 N 页"——用户既看不出超了多少、也看不出超出的内容长什么样。现在：① 预览自己用浏览器量到的版面算出"按当前设置约需几页（上限几页）、正文还多出多少"（与后端 `derive_pages` 同一套 `ceil` 算术口径，但不走网络、也不依赖只在 PDF 导出才有的 `X-Resume-Pages` 头），紧贴预览上方醒目展示；② 内容确实超出时，放不下的部分**继续向右并排展开成第 2、3…张 A4**（左右铺开、水平滚动，用 CSS `columns` 让同一份连续流"报纸式"横排到 N 列，**不造页盒、不引入第二套分页口径**，各列共用同一个 `--fit-scale`，所以第 2 页的字号 / 边距与第 1 页天然一致），每页边界画一条虚线并标注"第 N 页 / 共 M 页（近似）"；③ 全程**明确标注这是"按 A4 高度切分的近似分页"**，真实分页以「浏览器打印 / 下载 PDF」为准——不标注的近似等于新的"看起来真、其实不准"（有测试盯着标注不能被单独去掉）。另在「浏览器打印 / 另存为 PDF」入口补了一句悬停提示：**打印分页以浏览器为准、可能与屏幕近似分页略有不同**（浏览器不给 JS 暴露打印页数，应用内算不出真实打印页数，只能如实说明"打印是权威、屏幕是近似"）。两条边界：**不改用户存的 `page_limit`**（多页只作展示、不影响导出）；**内容装得下时行为与之前完全一致**（仍缩到一页、仍 `overflow:hidden`），只有确实超出才切多页。

- **求职助手正文里的 `[来源N]` 现在能点击跳转到对应原文了**。开启联网搜索时，模型正文会写 `[来源1]` 这类编号引用，此前它们是**纯文本、点不动**（行内 Markdown 只认裸 URL 与 `[文字](链接)`，不认 `[来源N]`）。之前**刻意没做**，是因为编号不可靠：自动预搜与每次工具搜索各自从 1 重新编号，而界面展示的是"按 URL 去重合并"后的列表，编号对不上、会跳错来源。现在改成**全局稳定编号**：一次回答里的每个来源在首次收集时分配一个全局单调递增编号（跨自动预搜与所有工具搜索共用一个 `SourceNumberer` 计数器），`web_context` 与工具搜索结果都用这同一个编号输出，并把"编号 → URL"映射持久化进助手消息的 `context.source_map`；前端 `[来源N]` 的链接 href **只用这份映射**（绝不用去重后的数组下标），所以模型当时看到的第 N 条就是用户点到的第 N 条。**宁可不可点、也不跳错**：映射缺失 / 编号越界 / URL 不是 http(s) 时一律退化成纯文本，不渲染链接、不报错。系统提示同步把"可按编号引用"改成"编号在本次回答内唯一、可直接引用"。


- **求职助手：开启「思考强度」后可以查看模型的思考过程**。回复下方新增**默认折叠**的「思考过程」面板，点开即可看到模型作答前的推理（流式生成过程中就能实时展开查看，历史消息里也能回看）；没有思考内容时整块不渲染，所以不开思考、或更早存下的历史消息的界面与之前完全一致。四层一起打通：provider 协议新增 `reasoning` 帧（OpenAI 兼容系读 `reasoning_content`（DeepSeek）与 `reasoning`，原生 Anthropic 读 `thinking_delta`——字段名缺失是常态，缺失时什么都不产出、绝不臆造）、SSE 新增 `reasoning` 事件、思考内容按 `tool_calls` 同款写进助手消息的 `context` 供历史回看。两条刻意的边界：**思考内容有存储上限**（`MAX_REASONING_CONTEXT_CHARS=20000`，超出**如实截断并在 `context.reasoning_truncated` 标记**，既不静默丢弃、也不让库里字段无限膨胀）；**思考内容不回灌给模型**（它只是给用户看的解释，塞回下一轮会让模型把自己的草稿当成事实，也会触发 Anthropic 的协议要求）。**已知限制**：原生 Anthropic（`api_style: anthropic`）+ 思考强度 + **多轮工具调用**时，官方要求把上一轮的思考块原样回传给下一轮，这一步尚未实现，该组合的第二轮可能被服务端以 HTTP 400 拒绝；已在 `docs/user-guide.md`「常见问题」与代码注释里说明，遇此情况把「思考强度」调回「关闭」或改用 OpenAI 兼容协议即可（只回答、不调用工具时不受影响）。

- **求职助手可制作「简历格式模板」与带知识文件的「助手技能」**。新增 `create_format_template` / `update_format_template` 两个工具，**只处理格式模板**（强调色 / 行高 / 页边距 / 区块间距 / 字号系数），参数清单与范围直接取自格式模板编辑器用的同一份 `FORMAT_FIELDS`，落库复用工作台既有的 `create_user_template` / `update_user_template`（重名拒绝、总量上限 40、名称字符集都沿用后端原话回给模型，不另写一份校验）；`update` 只改一项时**合并**进现有 `config`，不会把其它已调好的参数悄悄清空。`create_skill` / `update_skill` 补上 `files` 知识文件参数（单文件 ≤ 20000 字符、合计 ≤ 60000、最多 10 个，工具描述里对模型明说上限）。**刻意不让助手生成样式模板（完整 HTML）**：模型长篇写 HTML 容易出错，且保存前会经 `sanitize_template_html` 清洗掉脚本与外链，用户可能拿到"看起来生成了、其实不对"的结果——那类改动仍留给工作台里的编辑器。模板工作台的「格式模板（版式）」卡片新增「找求职助手制作」入口（格式模板行另有「找助手改这个模板」），跳转时用新增的 `?ask=` 查询参数在助手页**预填提问**（只预填、不自动发送，可改完再发）。系统提示同步改为"可以新建 / 修改格式模板，但不能改样式模板"，并说明原因。

- **保存本次采集抓到的站点原文（排查解析问题 / 真实样例回归）**。仓库里**一份真实抓取样例都没有**（`tests/fixtures/` 是空的），现有站点解析测试用的全是**手写 payload**——那只能证明"解析器能解析我们想象出来的结构"。要做"真实样例回归"（站点改版时立刻发现），必须先有真实样例；为此新增一条把站点接口响应原文落到本机的通道。在「投递台 → 自动采集」的「开始采集」旁多了一个**默认不勾选**的复选框「保存本次抓到的站点原文（用于排查解析问题）」，勾选后本次采集会把**搜索与详情两个接口**的响应原文存到 `backend/data/captures/<站点>/`（文件名形如 `<时间戳>-<序号>-<search|detail>.json`，内容是 `{url_path, captured_at, body}`）。三条底线：**默认关闭、每次采集显式勾选**（绝不默默记录，为一个采集建任务时 `save_site_samples` 为假则不写进 `task.config`、不安装录制器、连目录都不创建）；**不落任何凭据**（URL 只保留 `path`、丢掉 query 与 fragment——query 里可能有站点内部参数甚至令牌，而夹具只需要 path；也不记录请求头、Cookie、请求体）；**只写进 `backend/data/captures/`**（该目录已被 `.gitignore` 忽略，不进仓库、不进备份、也不跟着数据集切换，绝不写进投递浏览器的登录态目录 `backend/data/browser-profile/`）。限额为一个采集最多 30 份、单份最大 2 MB，超限会记日志说明已停止保存而不是静默丢弃。实现上新增 `SampleRecordingCdpClient`（`app/services/browser/sample_recorder.py`，与既有 `StopAwareCdpClient` 同构的 CDP 装饰器，靠 `requestId → url` 映射把先到的 URL 与后取的响应体配对），要保存哪些接口由**站点适配器**通过新增的 `sample_markers` 声明（站点知识只属于适配器层，录制器不含任何站点常量）；`POST /api/collect/tasks` 新增可选请求体字段 `save_site_samples`（默认 false）。

- **岗位「补齐详情」：给单条 / 一批岗位只补抓职位描述**。由来是一条真实的历史遗留——当年采集时详情页抓不到（该成因已修好），但**已经落库的那几条修不了**：采集按 URL 去重，重新采集会直接跳过它们，于是 JD 一直空着。新增的这个动作只做一件事：按岗位 id 重新打开详情页补 JD。它**不新增任务类型**，而是复用采集任务的整套机制（浏览器会话、详情抓取、限速、暂停/停止、进度显示），所以它仍是一个 `kind=collect` 批次，只是把"这一批岗位从哪儿来"从关键词翻页换成用户点名。入口在**岗位广场**：只有当这一页存在 JD 为空的岗位时才出现「补齐详情」按钮（否则点了什么也不会发生），选择模式下可以只补勾选的几条；点击后的提示会明确写出去哪儿看进度（「投递台 → 自动采集」）。三条纪律：**只补空**（已有描述的直接跳过，不覆盖用户改过的内容）、**跳过回收站里的**、**跳过没有投递链接的**（补详情的唯一入口就是那个地址）；补到正文后会重算技能标签（否则搜索 / 匹配仍按空标签走）。单批上限 200（补详情要逐个打开页面，很慢），超过则拒绝并说明请分批。新增 `POST /api/collect/backfill` 与 `apply_service.create_backfill_task`；任务完成文案区分"补到几条 / 跳过几条"，一条都没补上时如实说明原因（已有描述 / 仍然抓不到 / 已在回收站里），而不是显示成"成功但没做事"。

- **数据集导入导出的闭环测试**：一份"每一类内容都有"的数据集 → 导出 → 导入为新数据集 → 切过去，逐类核对内容与**回收站状态**都原样还在。现有测试只核对了岗位标题这一种实体；而备份要真能"把数据带走"，就必须覆盖简历（含正文与版式）、资料、台账、投递记录、助手会话与备选岗位。**尤其回收站状态**：如果导入把 `deleted_at` 丢了，用户会发现自己**删掉的东西又回来了**——而这类差别不专门核对根本发现不了（列表里多一条少一条，谁也不会去数）。

- **回收站页面**（侧栏「回收站」）：六类内容混排在一张表里，带类型标签与**删除时间**（用户来找的往往是"前几天删掉的那个"，只有标题没有时间等于让他一条条点开看），可按类型筛选、显示每类的条数。两条边界在交互上刻意做了不同重量：**恢复**一键就行（安全），**彻底删除**与**清空**都要二次确认，且确认文案把后果说白（"将被永久删除，无法恢复"）。删除成功的提示也从「已删除」改成「已移入回收站，可在「回收站」里恢复」——措辞说成"已删除"等于告诉用户东西没了，而它其实在回收站里等着。

- **回收站**：新增 `GET /api/trash`（总量 / 按类型计数 / 类型中文名 / 条目列表，支持按类型筛选）、`POST /api/trash/{type}/{id}/restore`、`DELETE /api/trash/{type}/{id}`（彻底删除）、`DELETE /api/trash`（清空，返回真正删掉的条数）。六类内容共用一个形状，界面用一张表混排 + 类型筛选，比六个页签更好找。迁移 `0016_soft_delete_marks` 给六张表各加一列 `deleted_at`（**只加列**，旧备份仍可导入；旧记录的新列必须是 NULL——若默认成时间戳，升级后用户所有历史数据会一次性全部出现在回收站里，看起来就像"数据全被删了"，有测试专门钉这条）。类型的中文名由**后端**下发，前端不再抄一份"job→岗位"的映射（抄一份就会漂移）。
- **`services/trash.py`：回收站的唯一实现**。六类内容由一个 `TrashSpec` 注册表描述（key / 中文名 / 模型 / 展示字段），新增一类只需要加一行；`live_only()` / `trash_only()` 把"哪张表用哪个列名"也只写一次（六张表各写一遍 `deleted_at.is_(None)`，总有一次会漏或写反，而那种错**不会报错**，只会让回收站里的东西继续出现在列表里）。表名清单由注册表推导，迁移与它对齐（有测试把两边钉在一起：漂移会导致"有列却没进回收站"或"进了回收站却没列，一删就崩"两种都不会报错的故障）。

- **每个内置模板的"输出一致性"快照测试**。新增 6×3 条按模板参数化的断言，逐模板钉住：自适应必须走 `--fit-scale`（**不许再用 transform**，且整个文档里都不许出现）、分页只有一份 `@page` 规则、打印保留背景、以及**技能标签的设计**（classic/elegant/modern/technical 有框，compact/minimal 是刻意的**无框**设计，两个方向都要断言——少了框是用户报告过的缺陷，多出框则是把别人的设计改掉了）。按模板逐个而不是抽查一个，是因为"只有一个模板坏掉"恰恰是最容易漏掉的形态。
- **`pdf_exporter` 终于有测试了**（此前该模块零覆盖）。分三层：几何比例必须与 HTML 模板对齐、折行边界（算错不会报错，只会让标签悄悄跑出页面，所以逐条钉：恰好放下、边界换行、单个标签超宽、空输入、**任何输入都不得丢标签**）、以及**绘制行为——每个技能都要真的画出一个"填色 + 圆角"的框**（只断言"没报错"是不够的，以前那版就是"没报错"地把标签画成了一行纯文本）。
- **采集结果先进暂存区，由你挑完再进岗位广场（采集闭环）**。以前采集是**直接入库**：采到什么，岗位广场里就多什么。而采集条件（关键词 + 城市）本来就宽、噪声是常态，代价是用户只能事后去岗位广场一条条删；采集完还只弹一句"已完成"，既不知道采到了什么，也不知道下一步该做什么。现在改成三步：
  **① 采集只写暂存区**——岗位落进既有的「备选岗位」（`candidate_job`，不新建概念），投递台「自动采集」页签下多出「本次采集结果」：标题 / 公司 / 城市 / 薪资 / 原站链接逐条陈列，勾选后点「导入选中的岗位」才收进岗位广场。
  **② 每次采集都有记录**——新增「采集记录」页签，按时间倒序列出每次采集的**条件与结果**（已暂存 / 重复 / 失败原因），展开就能看那次采到了哪些岗位，也可以在那里直接挑着导入。配套新增 `GET /api/apply/tasks`（按 kind 倒序列历史批次）。采集是个"跑完就看不见过程"的动作，没有这份记录，第二天就不知道上次按什么关键词、哪个城市采的。
  **③ 说清下一步**——采集面板顶部常驻一段流程说明（采集 → 挑选导入 → 投递队列 → 投递记录），任务完成文案也从"共新增 N 个岗位"改成「已暂存 N 个岗位。在下方「本次采集结果」里勾选要收进岗位广场的岗位，再点「导入选中的岗位」」。
  配套：新增迁移 `0015_candidate_job_collect_fields`（给 `candidate_job` 加 `location` / `salary` / `source_url` / `description` / `requirements` / `collect_task_id` 六列，**只加列**，旧备份仍可导入）；新增 `POST /api/candidate-jobs/import` 批量导入接口，**逐条**返回结果（`imported` / `duplicate` / `invalid` / `missing`），界面据此把"哪几条没进去、为什么"列出来——只汇报"导入了 7 条"会让用户去岗位广场数不出来。`description`/`requirements` 单独成列是刻意的：`raw_text` 是"粘贴进来的原始文本"，语义不同，塞在一起会把已切分好的 JD 结构丢掉。
- **`scripts/repair_collected_job_text.py`：修复被反爬混淆污染的历史采集数据**。文本混淆的修复只对**新采集**生效，而库里已经写坏的行**重采集也修不好**——采集器按 `source_url` 去重，同一个岗位会被判成"已存在"直接跳过。所以必须能就地修：脚本输出每个字段的前后对比，**默认只预览**（`--apply` 才真正写库），写库前自动把数据库整体复制一份到 `data/backups/`，且**幂等**（修好的行不会被再改一次）。它对用户已经手动改过的薪资 / 任职要求**一律不覆盖**——只补空值（有测试钉住这条，因为"就地改用户数据"最容易犯的错就是把用户的劳动成果冲掉）。
- **测试并行化 + 分层验证纪律（工程效率）**：后端全量 1000+ 用例串行要 ~8 分钟，这是协作里最贵的一环。引入 `pytest-xdist`（`requirements-dev.txt`），标准命令改为 `pytest -n auto --cov=app --cov-config=..\pyproject.toml`，同一套断言从 ~476s 降到 ~2 分钟。`pyproject.toml` 的 `[tool.coverage.run]` 必须同时打开 `parallel = true`——否则每个 worker 抢写同一个 `.coverage` 文件，会在**所有用例都通过之后**的收尾阶段抛 `attempt to write a readonly database`（这是最难排查的失败位置：看起来像测试挂了，其实是覆盖率写盘冲突）。`conftest.py` 的测试库与数据集目录本就按 PID 隔离，所以 worker 之间天然不共享状态；相应地新增一条纪律：**测试不要引入跨进程共享的固定路径**（仓库内固定文件名、固定临时文件名、`os.chdir`），那会把"更快"变成"偶发不可复现的失败"。同时把「先跑相关用例 → `ruff` → 交付前全量」的分层顺序、以及"提速不能靠跳过全量 / 放宽断言 / 删掉慢用例"写进 `AGENTS.md`（`--cov` 只在全量那一步开：它自身有明显开销；而且**不能**和"只跑几个文件"混用——覆盖率门槛是 80%，子集必然只有二三十，那是个假失败）。前端同样处理了长期存在的一类抖动：`vitest` 默认按 CPU 数开线程（本机 32 核 → 30 多个 jsdom 实例抢 CPU），重页面（设置页六张卡 + 消息列表）单个用例实测要 8~12 秒，而 `findBy*` 的全局等待窗口只有 5 秒，于是**单跑全过、全量随机挂几个**。窗口提到 15 秒（仍低于 `testTimeout: 30000`，免得真失败要等满 30 秒才报），全量从"随机挂 3 个"变为 266 个全过。
- **全局滚动条统一样式**：`scrollbar-width: thin` + `::-webkit-scrollbar` 统一成浅色细滚动条。应用里到处是弹性容器（消息列表、预览区、弹窗体），默认粗滚动条在浅色界面上非常显眼，一屏出现两三条就显得乱（用户连续两次截图反馈）。**只调外观、不动尺寸语义**：滚动条仍按需出现，不会因为样式而消失——那会让"这里能不能滚"变得无法判断。
- **工作台的内置样式改成「缩略图墙」**：以前「内置样式」只有一排文字标签（经典 / 现代 / 精简 / 优雅 / 技术 / 极简），用户看不出它们长什么样，只能挨个点开才知道——等于把"选样式"变成盲选。现在每个内置样式都渲染一张**真实效果**的缩略图（A4 同比例），点开看大图。缩略图用的是与正式预览同一条渲染接口加内置示例简历，**是真实模板的缩放 iframe 而不是另画的示意图**——示意图会随模板演进悄悄脱节。每张独立成败：单张渲染失败只留占位，不会把整块墙打掉（有测试钉住）。内置样式分组默认展开，进页面就能看到。
- **侧栏导航按使用顺序重新分组**：顺序改为「找岗位（首页 / 岗位广场 / 收藏夹）→ 做简历（简历中心）→ 投递与跟进（投递台 / 求职进度）→ 面试准备（模拟面试 / 求职助手）→ 我的数据（我的资料 / 资料箱 / 工作台）→ 系统（事实台账 / 设置）」。三处刻意的调整：**收藏夹**从「求职进度」和「求职助手」之间挪到岗位广场旁边（它本来就是"找岗位"的一部分）；**我的资料**排到资料箱 / 工作台之前（它是后两项的数据来源，放在后面会让人觉得是附属功能）；**事实台账**归到设置一侧的低频入口。分组只靠顺序表达，**不加分隔标题**——侧栏 200px，加标题会把 13 项挤成两屏。
- **面试深挖（新模块）**：入口在「事实台账」页的「拿去深挖」按钮（路径 `/claims/drill`）。它按台账里**已确认**的条目逐条做压力测试，回答的是"这条主张我到底讲不讲得清、哪里还站不住"。
  三条刻意的设计：**提问前先锁定评分契约**——每道题在生成时就把"要验证什么、必须听到哪些证据、什么情况该追问、什么情况可以结束"写进库里，之后**不因回答得流利而放宽**；这是防"事后改标准"的唯一办法，也是它与"给个分数"最根本的区别。**用证据状态代替分数**——`已验证 / 部分验证 / 未验证 / 存在矛盾 / 未覆盖`，界面与接口都不出现任何总分（有测试钉住"不出现 `N 分`"）。**不做推断**——模型只能依据用户真实说过的内容给状态，说"这次算过了"却**列不出任何证据**时一律不判成「已验证」。
  逐轮追问由契约驱动：追问类型按缺口选（背景 / 职责 / 结构 / 实现 / 决策 / 替代方案 / 失败与排查 / 指标口径 / 代价 / 复盘），**不机械遍历**。结束后出一份「该去补什么」的清单：看清哪些讲得清、哪些还站不住、行动项落到**补事实 / 补知识 / 降表述**三选一，另有**复练队列**（只收部分验证 / 未验证 / 存在矛盾的主张）。复练**不重复原题**——换一个角度出变体题 / 反事实题 / 故障题 / 证据题 / 压缩表达，因为"把上次的答案背一遍"不算会了。
  两个反馈策略：**真实模拟**（默认）每题后不念判定，只在结束时统一复盘——念了会让人按判分标准答题，而不像真面试；**训练模式**每题后立刻给一段简短反馈，判定本身在两种模式下都如实记录。**求职助手**新增 `list_drill_sessions` / `get_drill_report` 两个工具可以读记录帮你复盘，但**开不了新的深挖**（标准要先按主张锁定，且需要你自己逐轮作答）。
- 新增 Alembic 迁移 `0014_interview_drill`：新建 `drill_session` / `drill_contract`（一道题的评分契约，提问前锁定）/ `drill_turn`（一轮问答）三张表（只加表、幂等、带完整 `downgrade`）。契约与轮次随会话 `CASCADE` 删除；台账条目被删掉后契约仍要可读（`SET NULL` + 快照标题），因为复盘要看"当时问了什么、判定成什么"。
- **简历版面诊断与「自动一页」（新能力）**：简历预览上方新增「版面诊断」面板，把"看起来空/挤"变成可测的数字——单页正文占用 82%~96% 为合适、超过 96% 偏高、低于 82% 偏空；多页时另看**末页**（低于 70% 说明最后一张几乎是空的）。它按**固定顺序**给建议：**重排 → 结构 → 间距/字号 → 精简内容 → 扩页**。顺序本身就是规则：先让人调间距、调不好再让人删内容等于让人白改一轮；反过来先说"精简内容"，用户又可能删掉本来放得下的经历。溢出的简历上出现「自动一页」按钮，它**逐档试、够放下就停**而不是一步把字号缩到最小：按「页边距 → 区块间距 → 行高 → 字号」共十几档，每试一档只是往预览里注入一段 CSS 再量一次（没有网络往返）。字号下限按**绝对像素**算——不低于 12px（≈9pt）——而不是按比例：同一个系数在小字号档上会缩得更狠，只盯比例会让小字号档一路缩到 10px 以下。收到底仍放不下时如实说明"只能精简内容或增加页数"，绝不假装成功。
- **按简历的版式覆盖**：新增 `resume_record.format_config`（迁移 `0013_resume_format_config`，只加列）。「自动一页」的方案写在这里而不是去改具名格式模板——后者会让所有引用它的简历一起变，而自动一页的结果只对当前这份内容成立。预览、导出 HTML、服务端 PDF 三处走**同一个解析函数**，避免出现"预览收紧了、导出的 PDF 没变"这种只有下载之后才发现的不一致。
- **实测高度由浏览器提供，规则全在后端**：`POST /api/resumes/{id}/layout/analyze` 接收前端量到的两个高度，返回诊断结论与逐档方案。规则放在后端是因为它纯是判断（多少算满、先动哪个旋钮、缩到哪为止），能被逐个钉住、也能被助手与导出复用；测量只在浏览器里做，因为版面只有真正排版之后才存在。
- **求职进度（新模块）**：侧栏在「投递台」之后新增「求职进度」。投递台记录的是**动作**（这个岗位投出去了、失败了），这一页记录的是**结果**（对方走到哪一步了）——一家公司一个岗位一条记录，走「已投递 → 筛选中 → 测评/笔试 → 面试 → Offer」，另有「已结束」「待确认」两个不参与推进的状态。四条与既有功能的接合点：**投递成功的岗位会自动记一条「已投递」**（不必手工再录一遍，用条目上的快照字段，岗位被删掉也记得住）；**把邮件/短信/站内通知粘进来就能识别进度**（也支持截图与 pdf/docx，复用岗位识别那条链路），识别结果**先预览再确认**——面板上每条写明"会新增 / 会更新 / 没变化"和原因；**合并按「公司 + 岗位」**，同一组合只有一条记录；**导出 CSV / JSON**（CSV 带 BOM，Excel 打开不乱码）。两条硬规则写在 `models/tracker.py` 并在界面与接口共用同一份：**状态只能前进，拒信除外**（乱序粘贴三个月前的「申请已收到」不会把已经在面试的进度打回去），以及**普通自动回执只能算「已投递」**，不能从「感谢投递」推断出面试或 Offer——识别不出来就是「待确认」，由用户自己判断。**识别接口只算不写**：`/parse` 返回合并预览，`/apply` 才写入，且两者执行的是同一份合并计划（有测试钉住预览与执行一致）。
- 新增 Alembic 迁移 `0013_resume_format_config`：给 `resume_record` 加一列 `format_config`（只加列、幂等、带完整 `downgrade`）。加列不改表集合，所以"旧备份仍可导入"依然成立；旧记录升级后该列是空对象，渲染结果与升级前一致。迁移对"表不存在"的历史库做跳过而不是报错——迁移链也会被用在只有部分业务表的老库上。
- 新增 Alembic 迁移 `0012_application_tracker`：新建 `application_track` 一张表（只加表、幂等、带完整 `downgrade`）。同一公司同一岗位有唯一约束——"漏斗只有一个口径"由结构兜住。岗位/简历用 `SET NULL`，记录不会因为岗位被删而消失。
- **事实台账（新模块）**：侧栏在「我的资料」之后新增「事实台账」。它解决的是"简历上这句话站不站得住"——个人资料记录**发生过什么**，台账记录一条条**可对外表达的主张**，每条写清四件事：原始事实（照实写、不包装）、简历表述（准备怎么写）、承担程度（参与 / 负责模块 / 主导方案或交付 / 项目负责人）与个人边界（团队做了什么、你做了什么）。**核实状态决定用途**而不是决定好坏：`已确认` 才能进正式简历；`待确认` 只能进草稿且必须带【待补】占位符；`已过期` 需先更新；`不采用` 保留原因供复盘但不对外。三条与既有功能的接合点：**生成简历时**只有已确认的条目会作为事实基线进入提示词（未确认的说法会被列成"禁止使用"，且承担程度与个人边界会约束表述强度）；**导出时**正文里还留着【待补】之类标记的简历会被拦下并指出具体位置——弹窗里可以选「仍要导出草稿」（接口层的 `allow_incomplete=true`）四种格式与浏览器打印都适用，拦下不等于堵死；**求职助手**新增 `list_claims` / `get_claim` / `create_claim` / `update_claim` 四个工具。**助手不能替用户确认**：写入路径一律不接受核实状态，新建的条目都是「待确认」——让模型把自己新建的条目标成"已确认"等于让它自己给自己发通行证。台账为空时整条生成链路的行为与以前**逐字节相同**（有测试钉住）。
- **未完成标记统一为可检索的约定**：`【待补…】`、`【待确认】`、`【待补充】`、`【待核实】` 四种写法在模型、接口与界面三处用同一份取值；界面里这类标记会高亮，因为同一条标记同时意味着"这条还没核实"和"导出终稿会被拦下"。**只有标记本身会被拦**，正文里正常出现的"待补充说明"之类不会被误伤（有测试钉住）。
- 新增 Alembic 迁移 `0011_claim_ledger`：新建 `claim_record` 一张表（只加表、幂等、带完整 `downgrade`）。与 `0010` 同样的理由——`inspect_archive` 会先把旧备份迁到 head 再校验表集合，所以停在更早 revision 的备份会自动补上这张表，不会因为"缺少数据表"被拒收。台账**不建外键**：它记录的是"关于某段经历的主张"，主体用 `subject` 文本关联，这样资料库怎么改都不会连带删掉用户已经整理好的事实基线。


- **设置里可以配置联网搜索了**：新增「联网搜索」卡片——搜索来源（Bing / DuckDuckGo / 自建 SearXNG）、SearXNG 实例地址、**抓取正文的条数**（0~3）与每次返回条数（1~15）。这几项一直存在于后端、接口也齐全（`GET/PUT /api/settings/search`），但此前**没有任何界面入口**，于是「抓取正文」永远是默认的 0，助手能拿到的只有搜索摘要。助手页那个开关是"这一次要不要联网"，这张卡片配的是"联网时怎么搜"。
- **大模型配置支持 Anthropic 原生协议**：快速预设新增「Claude（Anthropic 原生协议）」，一次填好地址、模型名与接口协议；选预设时**协议跟着一起切**，从 Claude 切回 DeepSeek 也会切回 OpenAI 兼容。
- `scripts/tests/Test-Uninstall-ResumeForge.ps1`：卸载器的回归测试（默认保留数据、`-Purge` 删除数据、拒绝在非项目目录运行、取消时不删任何东西）。此前这个脚本既没有测试也没有任何文档引用。
- **自动投递中心的数据底座**：为"岗位自动采集 + 匹配度分析 + 全自动投递"新增 4 张表——`job_match_analysis`（一个岗位保留**最近一次**匹配结论，`job_id` 唯一即覆盖）、`apply_queue_item`（用户显式勾选的待投递队列，同一岗位唯一）、`apply_task`（一次执行批次，采集与投递共用，靠 `kind` 区分）、`apply_task_item`（批次内每个岗位的执行条目，也就是投递记录）。岗位/简历被删除后记录仍可读（`SET NULL` + 快照字段），批次内的条目随批次 `CASCADE` 删除。新增 Alembic 迁移 `0010_apply_center`（只加表、幂等、带完整 `downgrade`）：**只加表**这一点是"旧备份仍可导入"成立的前提——`inspect_archive` 会先把旧备份迁到 head 再校验表集合，因此停在 `0009` 的旧备份被自动补上这 4 张表，不会因为"缺少数据表"被拒收（已补回归测试 `test_backup_apply_tables.py` 钉住）。匹配状态（`matched`/`expression_gap`/`evidence_insufficient`/`real_gap`/`to_confirm`）与准入闸门映射（`allow`/`block`/`needs_confirm`）只写在 `models/apply.py` 一处，前端镜像同一份取值，避免两处判断漂移。**注意**：这 4 张表只在含该迁移的版本中存在，降级到 `0009` 会丢失投递记录（降级前请另行备份）。
- **投递专用浏览器桥接层与站点适配器框架**：这是本功能第一次让应用访问招聘网站，因此把最不可测的两件事——**驱动真实浏览器**（Chrome DevTools Protocol）与**理解陌生表单**——封在两个可离线测试的边界之后。`browser/cdp_client.py` 用既有 `httpx` 访问 `/json/version`、`/json/new` 等 HTTP 端点、用 WebSocket 发 CDP 命令，**传输层可注入**（沿用 `create_provider(transport=httpx.MockTransport(...))` 的离线注入点）；`browser/browser_manager.py` 用 `subprocess.Popen(list, shell=False)` 拉起"投递专用浏览器"——**独立 `user-data-dir`**（Chrome 136+ 禁止在默认用户目录开远程调试）、优先 Chrome 未装则回退 Edge、只关后端自己拉起的那个进程（持有进程句柄，不按 PID 满世界找，避免误杀用户自己的浏览器）。站点适配器用注册表模式，首期只有 `BOSS直聘`（`sites/boss.py`），选择器集中为常量；**"选择器/入口失效"是一等的失败类型**（`selector_invalid`），失败时带可操作诊断（当前 URL / 页面标题 / 匹配控件数 / 期望控件描述）直接展示给用户。**局限**：BOSS 直聘的真实 DOM 与投递流程**无法联调验证**，首期不保证一次投递成功，需要用户首次真机试用后回传诊断信息来收敛选择器；应用**不做任何"绕过验证码 / 代替登录"的实现**。
- **投递台可以自选浏览器**：投递配置新增「投递台使用的浏览器」——`自动（优先 Chrome）` / `Chrome` / `Edge` / `自定义路径`（含路径输入框）。此前 `BrowserManager._locate_browser()` 写死"优先 Chrome、未装回退 Edge"，用户无法干预；更关键的是**明确选了 Chrome 却启动 Edge 是比报错更坏的行为**，因此指定浏览器后只找对应的那个，找不到就给可读的中文提示（说清是哪个没找到、让他去装或改用自定义路径），**绝不静默回退**；自定义路径必须是真实存在的文件。配置变更**真正生效**：浏览器管理器单例的缓存键从"只按端口"扩成"端口 + 浏览器选择 + 自定义路径"，改完不重启也生效，切换时先关掉旧实例。状态回报补上人类可读的浏览器名（`BrowserStatusOut.browser_name`），界面不再只甩一串路径。
- **界面标明当前招聘网站，且新增站点只改后端**：显式引入"当前站点"概念——配置新增 `site_key`（默认取注册表里第一个站点），`default_entry_url()` 等依赖站点的逻辑都改为按"当前站点"取；新增 `GET /api/apply/sites` 返回**已注册站点列表**（`key` / 展示名 / 主机名 / 入口地址 / 是否支持采集与投递）与**当前选中项**；投递台顶部标注「当前招聘网站：BOSS直聘」并如实说明目前仅支持这一个、后续会陆续增加。**硬性约束**：站点名与站点清单全部来自接口与类型，前端组件里不写死任何站点名——以后新增招聘网站只在后端注册表 `register` 一行，界面自动多出选项，前端零改动（`SiteOptionOut` / `SiteListOut` 是唯一数据来源）。
- **采集改走「网络响应优先、DOM 兜底」**：岗位列表与详情本来就是 XHR 渲染出来的，直接读接口响应拿到的是**同一份数据**，但字段更全、且不受渲染时序与字体反爬影响。最大的实际差别在详情页：接口带回**完整 JD**，而 DOM 只渲染出摘要——采集器把接口的描述写进 `Job.description`，于是**岗位匹配读到的是全文**；薪资也按站点自己的口径格式化（`20-40K` / `300-800元/天` / `20-40K·16薪`），与用户在页面上看到的那一条对得上。`browser/network_capture.py` 只做纯解析（吃 CDP 事件、吐结构化数据，不碰浏览器），所以能离线测。
  几条刻意的取舍，以及几个**只有真机时序才暴露得出来**的坑（离线测试用的假客户端默认"事件已躺在缓冲区里"，正好会把它们全遮住，所以假客户端也改成按真实时序推送）：
  **订阅必须早于导航**——列表接口的响应是在页面加载过程中回来的，导航之后再订阅会漏掉第一批；**取走与停止必须晚于等待结束**——`Page.navigate` 只是**发起**导航，返回时页面还没开始跑 XHR，导航完就取走并停止订阅等于永远拦不到东西，而且"取走"还要发生在"停止"之前（停止会连缓冲区一起清掉，顺序反了同样白拦）；**接口这条路一律不抛异常**——路径改了、被风控换成了验证页、结构不是预期的那一种，都只是安静地**退回 DOM**，不让一次采集失败（"选择器失效"仍然是一等的失败类型，兜底不等于把问题咽下去：只有页面**明确**无结果才返回空页）；**判断顺序反过来**——接口拿到了岗位就说明这次搜索有结果，此时 DOM 匹配 0 个只说明页面没渲染出来，所以"用不用接口结果"的判断放在"DOM 说无结果"之前，否则会把"抓到了"说成"没搜到"；**事件订阅是可选能力**——`CdpClient` 给它配的是"不支持"的空实现而不是抽象方法，于是不支持订阅的浏览器桥接实现照样能用，代价是**忘了在包装层转发它不会报错**（`StopAwareCdpClient` 曾经就是这样），两者叠加的净效果是：网络优先在生产里从未生效，而离线测试全绿。现在这几条时序与透传都有测试直接钉住。
  **局限**：接口路径与响应结构没有官方保证，仍要靠真机试用收敛；接口多带出来的经验/学历/技能标签目前**还没有下游**（存在 `SearchResult.extra`，`Job` 模型没有对应的列），等接匹配度分析与 HR 活跃过滤时再取。
- **投递台与匹配度分析界面（前端）**：侧栏在「简历中心」之后新增「投递台」。一页四块——**投递队列**（上移/下移调顺序、逐条改简历与招呼语、勾选后只投选中的或整队列投递、显式「开始投递」）、**自动采集**（关键词 + 城市 + 薪资/经验/学历，无法映射到站点查询参数时明确标出「未生效」，绝不静默忽略）、**执行进度**（逐岗位状态机「待投递→进行中→成功/失败/已跳过」、用「已处理 N/M」而非百分比、熔断时顶部醒目警示条、暂停/继续/停止按钮常驻）与**投递记录**（按结果检索、失败条目单独重投）。岗位详情新增「匹配度分析」入口（与只读招聘原文的「岗位需求解读」并列，并用 Tooltip 讲清区别）和「加入投递台」入口——命中「证据不足/待确认」需你逐条确认，命中「真实缺口」显示为「不投」并说明原因（入队接口 409 的结构化原因经 `QueueConflictError` 上抛，界面据此弹出对应确认）。准入结论只读后端返回的 `admission`，前端不自行再判一次；界面不渲染任何百分比/评分，进度用**轮询**（1.5s，任务进入终态自动停）而非 SSE。**不引入任何新的前端运行时依赖**（仍是 antd 5 + React Router，5 个运行时依赖不变）。

- **站点健康度：把"采集悄悄抓不到东西"变成用户看得见的「站点可能改版」提示**。招聘网站改版时，采集会开始**静默失败**——任务仍显示"采集完成"，用户却拿不到有用的岗位（能翻到列表、却读不出岗位名或抓不到 JD），只会以为自己不会用。这恰好是最该报警却最安静的一类故障。现在投递台顶部的「当前招聘网站」在判定为 `degraded` 时显示醒目、可操作的中文提示，措辞刻意分三层：**先说事实**（"读不出岗位页面" / "读不出职位详情"）、**再说推测**（"站点**可能**改版了"，不把推测说成结论）、**最后给步骤**（"先重试一次；如果一直这样，请到项目仓库反馈并附上「采集记录」里的这次批次"）。**健康站点不加任何装饰**，免得用户习惯性忽略这个标记。判据完全在后端（`services/site_health.py`，拆成"纯判断 + 读库"两层，可离线逐条测），前端只展示后端给的 `reasons`、不自行再判一次：
  **算作"可能改版"**——① `selector_invalid`（页面结构变化 / 选择器失效）类失败；② 采集**正常结束**却**详情普遍为空**（某一批里详情缺失**超过一半**）。后者正是"能采到列表、却抓不到 JD"这种以前完全看不见的漂移。
  **明确不算**——需要登录、需要验证码 / 安全验证、网络超时、以及"真的没搜到结果"。这些是**环境或用户侧**问题，把它们算成"站点改版"会天天误报、用户很快就忽略这个标记。
  **样本不足时不报警**——只看最近 5 次、少于 2 次一律报 `ok`：单次失败可能只是网络抖动，宁可不报也不制造噪声。阈值都是具名常量并写清理由。
  配套新增 `GET /api/collect/site-health`（返回每个站点的 `status` / `reasons` / 最近几次的统计明细）。

- **简历备注**：简历中心的每条简历可加一条**备注**（悬停查看全文，详情弹窗里也可编辑），用来标记"这份是哪一版、投给谁"等用途；备注随简历保存、可随时改。

- **新建空白数据集**：设置 → 数据集的「新建数据集」可以从零建一份**空白数据集**，不必先导出一份再导入——想给另一个求职方向单独开一套记录时直接新建即可。

- **回收站批量操作**：回收站支持**多选后一次性恢复或彻底删除**，不必逐条点；彻底删除 / 清空仍保留二次确认。

- **月历视图**：求职进度页的「提醒」支持在**列表 / 月历**两种视图间切换——月历按日期把提醒聚合到格子里，一眼看清本月哪天有什么事。

- **首页内嵌紧凑月历**：首页「近期提醒」卡片下方直接展示本月的紧凑月历，不必进求职进度也能扫一眼近期日程。

- **投递采集岗位类型选择**：自动采集条件新增**岗位类型**（校招 / 实习 / 社招），采集时按类型限定岗位。

- **日期选择器**：投递日期、发布日期、核实日期、内推提交日期等日期字段改为**日历点选**，不再手敲 `YYYY-MM-DD`。

- **列表行操作收纳为下拉菜单**：岗位、简历、内推、提醒、面经、面试复盘等列表的行编辑 / 删除收进「更多操作」下拉菜单，行更紧凑、也不容易误点（删除仍带二次确认）。

- **首页快捷入口重排为 7 项**：首页快捷入口更新为 岗位广场 / 简历中心 / 投递台 / 求职进度 / 模拟面试 / 求职统计 / 数据集与设置，并各配一句说明去哪。

- **助手「思考过程」前置展示 + 脉冲动画**：开启思考强度后，模型的思考过程显示在**正文之前**，生成中带「正在思考」脉冲动画，完成后可折叠回看。

### Changed

- **求职统计看板图表更整洁**。漏斗每一档加了全宽浅色轨道，数值为 0 的阶段显示空轨道 + 「0」，不再渲染成误导性的彩色小细条；「投递最多的公司」对明显非公司名的脏数据（纯数字、单字乱码）归并为「未填写公司」并诚实标注缺口，不再把垃圾当公司展示。
- **生成简历预览底部按钮排满整行**。导出/微调/优化/脱敏等操作按钮改为自适应网格铺满整行，右侧不再留大片空白；「下载 PDF」单独一行保持醒目。
- **投递记录卡片长文本硬截断**。失败信息、招呼语等过长内容在表格里只做截断，完整内容移入「详情」抽屉。

- **求职统计趋势时间范围可选 + 字号优化**：看板的趋势图可以从「近 1 / 3 / 6 个月 / 1 年」里选（接口本就支持 `trend_months=1..24`，之前前端写死 6 个月），并顺带优化了看板里的字号层级。

- **分享包生成后「打开所在文件夹」+ 使用说明**：离线分享包生成成功后，除了下载，还能一键打开它所在的文件夹；入口旁补一句「打开给别人看时怎么用」的说明，把脱敏快照与评论回传文件各是什么讲清。

- **简历详情 → 求职助手默认新开对话**：从简历详情进入求职助手时，默认**新开一段对话**而不是续到上一次，避免上下文串味。

- **助手「引用回答继续问」补取消按钮**：输入框上方的引用条现在能点「取消」撤销本次引用，不必等下次发送才失效。

- **列表行编辑/删除改图标按钮（回收站除外）**：岗位、简历、资料、内推、提醒等列表行的编辑 / 删除从文字链接改成图标按钮（带二次确认的删除仍保留），行更紧凑、也不容易误点。

- **使用指南排版优化**：应用内使用指南把超长说明按语义拆成可扫读的短要点，并统一小圆点标记、放宽条目间距（内容一字未删）。

- **首页快捷入口更新**：首页快捷入口跟上新功能，补上知识库等新增去处。

- **后端新增运行时依赖 `python-multipart`**（钉死版本、纯 ASCII 注释）：供 multipart 上传使用；一键启动器的依赖探针同步包含，旧虚拟环境会在下次启动时自动补装。

- **生成弹窗从「同步等待 + 转圈」改为「后台任务 + 阶段条」**：生成期间可以关掉弹窗继续后台跑（此前生成中 `maskClosable`/`closable` 都被禁用、文案还写着"请勿关闭弹窗"），完成后由「简历已生成」通知打开结果预览；「取消生成」从"前端断开连接"改为真正调用后端 cancel 接口并标记 `cancelled`（取消后不落库半成品）。进度展示从"Spinner + 文字列表"换成结构化阶段条 + 已接收字数（无百分比）。

- **大模型配置的「最大输出 Token」默认改为「不限制」**：新用户/新配置不调参数也能用服务商/模型的默认上限，而不是被默认的 4096 悄悄截断。「不限制」在请求里的语义不变——OpenAI 兼容协议下省略 `max_tokens`、由服务商决定上限；原生 Anthropic 协议下改用较大的兜底值（协议要求必须给这个字段）。只改**默认值**：已保存过 `max_tokens` 的存量配置原样保留，不会在升级时被悄悄改写。
- **使用指南排版优化：超长说明拆成可扫读的要点**。此前「导入岗位与投递」等步骤把自动采集、投递专用浏览器等一整段说明塞进**一个**要点里，整屏密密麻麻、看不下去。现在把这类超长单点按语义拆成多条短要点，并给要点列表加上统一的小圆点标记、放宽条目间距——内容一字未删，只是从"一堵墙"变成"一条条"。

- **`measureResumeLayout` 补充两个纯函数 `pagesNeededFor` / `overflowHeightFor`**：把"实测高度 → 约需页数 / 超出高度"的换算抽成前端统一的纯算术（与后端 `derive_pages` 同口径），预览的溢出提示与多页视图共用这一份，避免再各写一遍；已补单元测试。
- **预览溢出时不再一律注入 `overflow:hidden`**：改为"装得下才 hidden、溢出则 `overflow-y: visible`（横向仍 hidden）+ `html` 背景补白"，好让放不下的内容能被画出来而不是被裁掉；单页 / 装得下的路径行为不变。
- **选模板时直接看到每种模板的真实效果，不再"下拉盲选 + 另点「看效果」"**：简历预览工具栏与生成弹窗里，原来的「样式模板下拉框」与「看效果」按钮合并成**一个入口**——点开就是每种模板用你的简历（没有简历时用内置示例内容）真实渲染的缩略图，点「用这个模板」即写入并重新渲染（选中与查看是同一步，没有额外确认）。这次**不新建任何静态缩略图资源、也不新增预览接口**：直接复用既有的 `TemplateGalleryModal` 与 `/resume-templates/preview`。刻意**不把 iframe 缩略图塞进 antd `Select` 的下拉**——下拉一挂载/卸载就会反复销毁重建 iframe，以及"展开时才渲染"的时序都不好保证；选的是"打开一个以真实预览为选择面的弹窗"，并发渲染请求只在打开时发生（既有且已被接受的行为）。模板入口按钮仍可用键盘聚焦、打开后逐张卡片可聚焦选中。

- **字号从三档（小 / 标准 / 大）改为无级滑块（12–15.5px），拖动实时看到缩放**：预览工具栏与生成弹窗的「字号」由 `Segmented` 换成连续滑块。拖动过程中**不外发请求**——直接用一段本地探针 CSS 改预览里的 `--fs` 即时缩放，停手后**防抖 180ms** 才提交一次（`PATCH` + 重新渲染）；既不会每拖一格就打一串请求，也不牺牲手感。**存储路径刻意复用既有的 `font_scale_adjust`，没有新增字段、没有改数据库列**：滑块给的是绝对像素，提交时落到**最近的档位**、用该系数补齐差值，所以传输与落库仍是"枚举档位 + 一个系数"，后端与数据库完全不用改——**已有简历缺省即 1.0、零影响零迁移**；HTML 与 PDF 本就共用这个乘数，因此预览 / 浏览器打印 / 直出 PDF 仍是同一口径（自动一页也已在用该字段）。相邻档位之间的间距最大只需约 ×1.083、最小约 ×0.929，都落在既有 `font_scale_adjust` 的 `[0.88, 1.16]` 内，**因此没有放宽后端 `FORMAT_FIELDS` 的范围，自动一页的候选阶梯不受影响**。另保留一个「恢复默认字号」按钮与方向键微调。

- **删除改成"移入回收站"，不再直接销毁**（六类内容：岗位、简历记录、投递记录、事实台账条目、资料箱材料、助手会话）。以前所有删除都是**硬删除**，一次误点就没了；而这个工具里装着用户真实的简历、投递记录和与助手反复沟通出来的对话，其中"简历记录"还是花模型调用换来的。现在删除只打一个 `deleted_at` 时间戳：**列表里消失、回收站里看得见、可以原样恢复**；真正的销毁（彻底删除）在回收站里单独提供，且**只接受已经在回收站里的记录**——界面上因此不存在"点一下就没了"的路径。为什么用软删除而不是把行搬到一张"回收站表"：搬表意味着外键、快照字段、去重判据全部要重建一遍，而它们现在都是好的；软删除只多一句"什么时候删的"。**范围刻意克制**：只覆盖用户真正会心疼的六类，列级别、模板副本、数据集这类要么有明确的沿用关系、要么本来就能重建，不进回收站（每个可删项都进回收站，只会让回收站自己变成垃圾场）。
  软删除真正的成本不在删除本身，而在**每一条读取路径都要排除它**——少排除一处，用户就会看到"删了但首页统计里还有"，而这类半成品对删除功能的信任是毁灭性的（下次他就不敢删了）。所以这次把列表、单条读取与修改、首页统计、全局搜索、求职助手工具、以及投递台自动挑简历的路径**全部**对齐，并写了**逐入口**的测试把它们钉住。
  两个刻意保留的例外：**去重**必须把回收站里的内容视为"已存在"（否则"删掉 → 再采集一次"会造出第二条，而用户以为自己只是删了一条），并明确提示"在「回收站」里"；**彻底删除**必须绕过"当作不存在"的守卫，否则进了回收站就再也删不掉。

- **预览 / 导出 HTML / 浏览器打印 / 直出 PDF 四者的分页与标签样式现在收敛到同一套口径**（用户实测反馈："预览里专业技能是蓝色小框，导出 PDF 后没有了"、"预览一页装得下，浏览器打印却超出一页"）。两个根因：
  **① 自适应缩放压的是"视觉"而不是"版式"。** 以前有两处各自给 body 挂 `transform: scale(...)`——模板里的自适应脚本一处、`ResumePreview` 又一处。transform **只改变视觉、不改变版式**，而打印/导出的分页是按**未缩放的版式**算的；于是预览看起来刚好一页，打印却多出一页，两者永远对不齐。现在统一改成设置 `--fit-scale`，由模板 CSS 的 `--fs: calc(基准 * var(--fit-scale))` 吸收——所有尺寸本来就是 `calc(var(--fs) * N)` 推出来的，所以整份版式**真实变矮**，四者看到的是同一套版式 + 同一份 `@page` 规则。预览层不再自己算缩放，只负责量尺寸与读状态（脚本没跑时才用**同一套机制**兜底，不再制造第二种分页口径）。迭代而不是一次算出：页边距等绝对尺寸不参与缩放，高度与比例只是近似成正比，跑 2~3 轮即收敛，且缩放下限 0.5，实在放不下就如实报 `overflow` 让界面提示加页。
  **② 打印默认不画背景。** 浏览器**默认不打印背景图形**，技能标签的底色与左侧色条因此在打印时消失（`@media print` 里补 `print-color-adjust: exact`，不依赖用户去打印对话框勾选）。**直出 PDF 则是另一个问题**：它由 `pdf_exporter` 用 fpdf2 单独排版，技能那一行以前是用「、」连起来的**纯文本**，连标签的形状都没有——现在按与 HTML 同一套比例（字号 0.93×、内边距 0.71×、圆角）画成**带底色的圆角标签**，并在可用宽度内折行。
  同时清掉一处"同一份几何声明两遍"的隐患：`@media print` 里原本把 `body { width/height/padding }` 又抄了一遍（与 base 完全相同），改一处漏一处就会让预览与打印的页边距突然不一致，现在只保留打印特有的声明。
- **Markdown 导出补上性别与出生年**。预览页眉会显示这两项，md 里却查无此项——属于"同一份简历两个输出说得不一样"（与上面同一类问题，只是发生在文本导出上）。现在按字段逐个断言 md 的完整性（照片是**唯一**且有意的例外：内嵌 data URL 放进文本导出没有意义）。
- **薪资 / 经验 / 学历不再标「未生效」，而是采集后按岗位字段真的筛掉不符合的**（用户疑问"为什么永远失效"的落地）。这三项以前一律标着「可能未生效」，因为站点把它们做成**不透明的数字编码**（形如 `degree=202`）——没有公开说明，**猜错编码的后果是静默返回错误结果**，比"没筛"更糟，所以适配器一直如实标注而不是去猜。
  正确做法不是猜编码，而是换位置：**岗位列表接口本来就返回每条岗位的学历要求、经验要求与薪资数字**，采完之后按这些字段筛即可，不需要编码表，而且筛掉几条都能如实报出来。判据在 `services/apply/collect_filters.py`（纯函数、可离线逐条测）：
  - **学历**：用户填的是**自己的学历**，岗位上的 `jobDegree` 是**岗位要求的最低学历**，所以判据是"岗位要求 ≤ 我的学历"（要求「硕士」而我是「本科」→ 筛掉，方向写反了不会报错、只会悄悄筛错，所以有专门的用例钉住）。岗位写「学历不限」按满足处理。
  - **经验**：岗位要求的区间与用户的区间**有重叠**才留（含端点：要求 3-5 年、我有 5 年 → 重叠）。"不限"是确定的答案而不是"认不出"。
  - **薪资**：岗位**能达到**用户期望的下限才留（比较的是**薪资上限**：10-20K 对期望 15K → 留；10-12K → 筛掉）。日薪岗位与"期望月薪"不是一个单位，**判断不了就不比**。
  三条贯穿的原则：**① 判断不了就保留**（岗位没写学历/经验时**不排除**，宁可多给用户看几条，也不能悄悄丢掉他真正想要的那条，这类情况单独计数并告知）；**② 不猜**；**③ 你的条件如果没能被识别，明确说出来**——例如学历填了「管培生」，界面会写"这条条件没能识别，本次没有生效（换个写法试试）"，而不是假装筛过了。
  配套：`SiteAdapter.post_filter_conditions` 由适配器**声明**哪些条件走本地筛选（这份声明有约束力：没声明就不筛，避免拿缺失的字段去误杀岗位）；筛选结果写进 `task.config`（不新增数据库列，与 `unmapped_conditions` 同一套做法），界面上如实分三块说明——筛掉几条、有几条因为岗位没写字段而没能判断、自己的条件有没有被用上。任务文案也补齐了：有筛掉就说"另有 N 个不符合你填的筛选条件"，**全被筛掉**时说的是"都不符合你填的筛选条件"而不是"没有找到匹配的岗位"（说成后者会让用户去改关键词，而问题其实出在筛选条件上）。采集表单里那三个「可能未生效」标签也随之改成「采集后筛选」，并补上解释性提示（"填你自己的学历；要求高于它的岗位会被筛掉"）。
- **首页从"看数据"改成"看该做什么"**：此前首页只有四个计数卡加两个列表，功能变多之后它没跟上——进了首页看不出"我现在该做什么"。新增 **接下来做什么**，它只列**待办**、不列"你已经做了多少"（概览页上能推动人的只有前者）：还没核实的事实台账条目、投递队列里排着还没投的岗位、超过一周没有任何更新的进行中投递。每一条都带数字和去处，点一下就到那件事本身；三类都清空时明确说"眼下没有待办"，不留一片空白。另有按"要做的事"组织的**快捷入口**，以及"最近投出去的"列表。接口 `GET /api/stats` 相应增加 `pending_claim_count` / `pending_claims` / `apply_queue_count` / `stalled_application_count` / `favorite_job_count` / `latest_applications`，全部带默认值，旧前端不会因为多了字段而失败。
- **设置页分成三个标签页，编辑按钮跟着它真正控制的那一页走**：设置项随功能增加而变多，一整页排下来找起来很费劲——想改数据备份得先翻过整套模型参数。现在分成 **AI 模型**（模型配置、配置记录、联网搜索、助手技能）、**数据**（数据集备份与切换）、**应用**（版本更新）三页。**「编辑设置」从页面顶部移到了「AI 模型」页里**——它此前挂在页顶，却只控制模型配置，而其余几页一直是可编辑的，那个位置本身就在误导用户；现在它明确地是该页的操作，其余两页不需要进入编辑态就能用。
- **弹窗改成在卡片内部滚动，不再撑高整个页面**：`ConfigProvider` 把弹窗挂到 `body`，卡片因此脱离文档流，`<body>` 不再被撑高——此前滚动条跑在整个窗口的右侧，用户得先滑页面才能看到卡片下半部分，卡片右上角反而没有滚动条。配合给 `.ant-modal-content` 一个 `max-height` 与内容区 `overflow-y: auto`，投递设置这类长表单现在**在卡片自己身上滚动**；只给上限不给固定高度，内容少时卡片仍按内容收缩。所有弹窗一起生效。
- **简历照片只显示"正在使用"的那一张**：此前大图与下方缩略图并列，同一张脸在同一个卡片里出现两次，看起来像存了两张照片。备选照片移进 **照片库** 弹窗（那里本来就该承担"挑一张"的动作），卡片本身只回答"简历上用的是哪张"；删到只剩一张时照片库自动关上。
- **助手多选不再支起一个大框**：多选操作条此前是独立的一整条浅蓝卡片，下面还没有内容时就先占了一大块版面。改成与消息列表同宽的一行轻量工具条——左边报数、右边动作，用一条分隔线收尾。
- **「退出」按钮移到界面右上角**：它此前在侧栏底部、和「使用指南」挤在一起，而"退出应用"是全局动作、不是某个模块的入口。现在与仓库链接并排放在顶栏右侧；窄屏只留图标，避免顶栏被挤到换行。
- **使用指南的排版修好了**：布局其实是坏的，而且坏在一个不容易看出来的地方——`.user-guide-body` 声明了**两列**网格，而 DOM 里有**三个**孩子（步骤条、竖分隔线、内容区）。分隔线占掉第二列，内容区被挤到**下一行的第一列**：正文只有 196px 宽，中文每行两三个字就折行，整个弹窗被拉到 3000px 高（截图反馈"排版完全是乱的"）。改成用命名网格区域显式指定每个孩子的位置，孩子个数再变也不会悄悄错位；正文宽度从 104px 恢复到 388px，弹窗高度从 3406px 降到 866px。
- **深挖页补上返回入口**：`/claims/drill` 是从台账「拿去深挖」进来的独立路由，此前除了浏览器后退，界面上没有别的出口——用户会以为"进去就出不来了"。左上角新增「返回事实台账」。

- **「字号系数」现在对 HTML 预览也生效了**（此前只影响服务端 PDF）。它走的是"渲染前把系数预乘进基准字号"这条路——那是唯一对内置模板与用户自制模板都生效的路径，因此**刻意不给它 CSS 变量映射**：给两条路会叠乘，界面上调 1.1 实得 1.21 倍，而这种偏差只有拿尺子量才看得出来（已加测试钉死 `format_css` 对它不产出任何内容）。
- 模板的版式默认值（页边距、行高、区块间距）改为在 `resume_templates.py` 里声明，并有测试**逐个模板读文件核对**。自动一页靠这些数字判断"当前值是多少、还能往紧收多少"，抄错会让它收紧一个用户根本没设置过的值，或者该收没收——而这两件事在界面上都表现为"按了没反应"。
- 新增接口：`POST /api/resumes/{id}/layout/analyze`；`PATCH /api/resumes/{id}/layout` 新增可选的 `format_config`（不传 = 不涉及、空对象 = 清掉覆盖，两者语义不同且都有测试）。
- 新增运行时依赖 `websocket-client==1.8.0`（钉死版本、纯 ASCII 注释）：自动投递的 CDP 客户端用它走 DevTools 的 WebSocket 通道，HTTP 端点复用既有 `httpx`；纯 Python、有 wheel，首启安装仍是零编译。一键启动器的依赖探针同步包含 `websocket`，旧虚拟环境会在下次启动时自动补装。`.gitignore` 增加 `backend/data/browser-profile/`——投递专用浏览器的 `user-data-dir` 含登录态、体积可能很大，**绝不进 Git，也不进数据库备份包**（登录态由 Chromium 自己持久化，应用既不读取也不解析）。
- 使用指南的联网搜索一节重写：写清"最多 3 次"是**总数**（含开头那次自动搜索）、四个搜索参数各自的作用与代价，以及"开关"和"设置"是两件事。
- **助手消息支持多选批量删除**：消息区右上角新增「多选」，勾选后一次删掉多条（走后端已有的批量接口，一次最多 200 条，中途失败不会删一半）。换会话会自动退出多选，免得把上一条会话的消息 id 带到新会话里误删。删除前有二次确认。
- **拖拽导入补齐四个漏掉的入口**：经历参考文件（.md/.txt）、技能包（.md/.zip）、简历模板 HTML、数据集备份包（.zip）现在都能直接把文件从资源管理器拖进去。此前只有岗位识别、备选截图、资料箱附件、个人照片、助手附件这五处接了统一拖拽容器，剩下四处还是只能点按钮选文件。拖拽区贴着按钮本身、不做整行接管——设置页一行里常并排放着好几个导入入口，整行接管会让"拖到哪个框里"变含糊。
- `FileDropZone` 补上单元测试。这一层此前**一条测试都没有**，而项目里九个导入入口全挂在它上面——它要是坏了，"拖进来"会在所有地方一起失灵，却没有任何断言会红。
- **卸载流程补齐**：`uninstall.cmd` / `scripts/Uninstall-ResumeForge.ps1` 补上测试与文档。它默认只删启动器生成的东西（虚拟环境、`node_modules`、构建产物、`runtime/` 与各类缓存），保留 `backend\data` 与 `backend\.env`，**永远不删源码**，且只在真正的项目目录里运行；`-Purge` 才会连数据一起删。测试全部在临时目录里复制一份脚本来跑，不会碰当前仓库。
- 应用内使用指南同步：模板数量改为六套样式模板 + 格式模板（原文还写着"三套模板"）、侧栏「技能工作台」改为「工作台」（原文是改名前的旧称呼）、补上接口协议与联网搜索设置、消息多选删除、退出与卸载。`UserGuideModal.test.tsx` 此前把旧名字钉住了，所以它没能在改名时报警——已改为断言新措辞。

- 应用内使用指南**就地扩写「导入岗位」这一步**（标题改为「导入岗位与投递」、改进对应 `heading` 与 `description`）：补上自动采集的入口与「未生效」限制、匹配度分析与「岗位需求解读」的区别（一个回答"岗位要什么"，一个回答"你够不够"）、投递台的操作方式与「登录在你自己浏览器里完成」。**不新增第 8 步**——采集与投递仍是同一条「收进来 → 判断 → 投出去」的链路，指南是流程向导而非发行说明。`path` 保持 `/jobs` 不变。
- `.gitignore` 增加 `deliverables/`：需求、设计、评审等**协作过程文档**不属于产品代码，默认不提交（如需纳入版本管理，删掉这一行即可）。
- **采集批次现在会把"这次是哪个站点 / 失败类别 / 详情为空条数"记进 `task.config`**（不新增数据库列，与 `unmapped_conditions` 同一套做法）：`site_key`（采集开始时写入，读不出就留空、**不**编造默认站点）、`failure_category`（失败时写入，站点改版类为 `selector_invalid`，其余非 `SiteFailure` 的失败落 `unknown`，绝不留空）、`detail_missing`（搜索采集里"暂存了但正文为空"的条数，补详情模式不写这个键）。这三项既随批次记录暴露在 `GET /api/apply/tasks` 的 `config` 里，也是上面站点健康度判据的数据来源——此前事后无法按站点归因，也无法把"站点改版"与"网络超时"分开。
- **求职助手的「参考来源」更好读也更明显能点**：来源块（默认折叠的「参考来源（N）」）此前每条来源的标题与摘要贴在一起、条目之间也没有间隔，几条挤成一团；标题又长得像普通文字，用户不会想到能点开原网页。现在每条来源之间加了分隔线、标题与摘要之间留出间距；摘要最多显示两行、超出省略（完整内容点开原网页看）；标题补上外链图标与悬停下划线，把"可以点、会跳转"摆在明面上。`target="_blank"` 与 `rel="noopener noreferrer"` 保持不变，默认折叠与键盘可访问性也保持不变。
- **求职助手的「助手做了什么」改成默认折叠，但"改动了数据"依然一眼可见**：工具调用记录（查询 / 新增 / 修改了哪些东西）此前刻意**不折叠**，理由是"助手动了用户的数据，这件事必须一眼可见"；但用户反馈它每次回答都铺开一整列、太吵。改成默认折叠后，把原设计的意图**承接到折叠标题上**——标题写成「助手做了什么（N）· 改动了 M 项」，失败时再补一句红色的「N 项失败」，因此**不展开也能一眼看出它有没有动你的数据、有没有出错**；逐条明细与「前往查看」只是移到展开之后，链接功能不变。为此后端工具 SSE 事件新增 `changed` 字段（透传 `ToolResult.changed`），前端据此计数——**不再按工具名自己猜**哪些是写操作，避免与后端漂移；更早的历史消息缺这个字段时按"没改动"处理，绝不主动谎报改动。
- **岗位广场能一眼区分「手动添加」与「自动采集」，并按来源筛选**：岗位卡片（表格的职位列）标题旁新增角标——采集来的标「采集」、其余标「手动」。判定**只认 `recognition_source === 岗位采集`**（采集器写入的固定值），而不是拿 `source` 站点名去猜：`source` 会随注册的站点增多，等于"非手动添加"却可能是将来某个站点；而采集的 `recognition_source` 是白名单里的固定字面量。配套 `GET /api/jobs` 新增可选查询参数 `source_kind`（`collected` / `manual`），岗位广场筛选条新增「来源」下拉（自动采集 / 手动添加），筛选走服务端分页而不是只筛当前这一页。
- **岗位广场与收藏夹的岗位悬停显示导入时间**：职位标题上悬停会出现「导入于 YYYY-MM-DD HH:mm」，时间沿用 `formatDateTime` 的口径（后端 UTC 补 `Z` 后转本地）。只加 `Tooltip`、不拦截标题本身的点击与整行右键菜单。
- **收藏夹「简历」页签去掉多余的横向滚动条**：那张表硬套了 `scroll={{ x: 760 }}`，而简历列的固定宽度加起来约 620px，普通窗口里根本不会横向溢出，多出来的是一条用不上的滚动条。现在横向滚动只保留给更宽的「招聘信息」页签，简历页签按内容自然排布。
- **「我的资料」分区默认折叠 + 一键全部展开/收起，通用简历不再沉底**：资料分区很多时整页很长，现在查看态默认每个分区只显示标题栏，点标题展开/收起单个分区，页头新增「全部展开 / 全部收起」切换；编辑态仍全部展开（填字段需要看全）。分区标题统一由分区外壳给出，卡片本体不再自带标题（避免展开时标题出现两次）。「通用简历」卡片从页面最底部移到资料分区之前——它不属于资料表单，以前资料一多就得滚到底才看得到；页头原来的「通用简历」直达按钮随之移除（卡片已在顶部，不再需要跳转）。

- **简历详情与生成结果统一预览**：简历详情弹窗与生成弹窗共用同一套预览组件，两处排版、缩放、编辑行为一致。

- **简历中心新增「收藏」筛选**：可按是否收藏过滤简历列表。

- **简历中心新增「有无岗位」筛选**：可按是否关联了岗位过滤简历列表（通用简历不关联岗位）。

- **投递记录表格长文本省略 + 纵向滚动**：投递记录里过长的字段自动省略（悬停看全文），表格按固定高度纵向滚动，不再把整页撑高。

- **版本对比中照片显示为「[图片]」占位**：版本对比不再尝试对照片内容做文本化比对，照片位置显示「[图片]」。

- **求职助手「功能在哪」答得更准**：助手的能力目录改为单一事实来源，与侧栏菜单、使用指南双向对齐——问"某功能有没有 / 在哪"时答得更准，不会说"没有"或编入口。

### Fixed

- **投递台采集的岗位不再把 JD 整段塞进「职位描述」**。用户反馈"导入的岗位，职位描述、任职要求、其他招聘信息全挤在职位描述里"。两层根因：① **分段在正文里误切**——切分点只看"要求类标题词出现在哪"，而真实 JD 里有 ``为符合**岗位要求**的同学提供…`` 这种**正文用词**，于是从那里切开，描述被截成半句（实测 21 字）、真正的「任职要求」连同整篇正文一起进了要求段。现在要求小标题必须**另起一段**（前面是行首或句末标点；只有空格/开括号这种弱边界时，后面还得紧跟冒号），两个字段才都读得成话。② **「其他招聘信息」根本没有列可落**——采集端已经按小标题切成三段，但 ``candidate_job`` 只有描述与要求两列，第三段（福利待遇 / 公司介绍这类）只能留在描述里，导入后「其他招聘信息」永远是空的。新增迁移 `0021_candidate_additional_info`（**只加一列**、幂等、带完整 downgrade，旧备份仍可导入）把第三段接住并透传给 ``job.additional_info``；``split_job_sections`` 扩展为 ``split_job_fields``（返回三段，旧的两段签名保留为薄包装）。**备注不用改**：``create_job_record`` 早就按 ``recognition_source`` 自动补「来源：岗位采集」那行（库里的采集岗位已有 3 条带着它）。已入库的历史行不会自己变，而界面的「补齐详情」有意跳过**已有描述**的岗位（那趟是给"JD 都没抓到"的岗位补正文的），所以新增 ``scripts/repair_collected_job_sections.py``：只重切 ``job`` 的三个文本列、不删不改其它字段、默认只预览、``--apply`` 落库前自动备份。实测本库 21 条采集岗位里 1 条需要重切（正是用户截图那条：描述 329→148 字，要求 0→181 字）。

- **使用指南从"一堵墙"改成"可扫读"：每条要点加粗体短标签，正文减 25%，卡片加宽**。此前一轮对付"密密麻麻"的办法是**把长段落拆成更多条**——结果每条仍是一整段普通文字，几十条排在一起照样抓不到重点（用户再次反馈"字太多、用户完全抓不到重点"）。根因是**没有层级**，不是不够碎。这次改结构：`points: string[]` → `{ lead, text }[]`，每条先给一个 **2–8 字的粗体标签**当扫读锚点，后面跟一句说明；标签用强调色 + 半粗，与说明拉开层级。同时**真删掉 25% 的文字**（7401 → 5530 字），设计取舍与边界细节留给 `docs/user-guide.md` / `docs/architecture.md`——指南只回答"用户会不会因为不知道而用错或用不上"。版面：行高 2 → 1.75、条目间距 18 → 14，弹窗 **720 → 880**（左导航列固定 168px，多出来的宽度全给正文：720 时正文只剩约 400px、一条要点要折三四行，这正是"拥挤"的来源），并加 `maxWidth: calc(100vw - 32px)` 兜住窄窗口。**顺带修掉一个可见缺陷**：指南正文一直按纯文本渲染，于是 `**事实台账**` 在界面上原样显示成带星号的 `**事实台账**`——现在 `**…**` 渲染成粗体。更值得记的是**原来有一条断言把这个 bug 钉住了**：它断言界面上出现 `在你看到问题**之前**就定下来`（带星号），也就是"测试写成了 bug 的形状"；现已改为断言渲染后的文字，并加一条**全文不得出现 `**`** 的守卫。守卫本身也换了：不再钉"某一步要点数 > 10"（那正是上一版没见效的修法），改为钉**层级**——每条 `lead` 非空且 ≤ 8 字、`text` 非空且 ≤ 160 字、同一步内 `lead` 不重复。

- **题库「参考答案」生成失败**（报「模型未返回有效的参考答案 JSON」）。模型返回常带 ```json 围栏、前后缀文字或尾随逗号，解析改为剥离围栏 + 截取首个平衡的 `{...}` 再 `json.loads`，失败时自动重试一次（重试请求追加"只输出 JSON"约束），仍失败才报错并附原始返回摘要。
- **历史复盘点开变纯文本、反向优化不可用**。复盘历史点开后丢失排版、无法选简历导致「反向优化简历」失效，旧版纯文本记录还会渲染崩溃——补齐富还原路径（分析/建议照常渲染、可重新选简历并回写），旧版记录降级为只读提示。
- **投递队列右键菜单实现**。原先把 `<tr>` 包进 Dropdown 会造成行重建竞态（偶发"元素不在文档中"），改为 `onRow` + 定位式菜单，稳定渲染。

- **「简历写作增强」在界面上重新够得着**。STAR 量化改写 / 话术生成器 / 多风格润色 / 中英互译这四项的后端、接口、面板组件、README 与应用内使用指南**全都齐了**，唯独简历详情预览打开编辑弹窗时**没有把简历 id 传进去**——而「写作增强」页签只在拿到简历 id 时才挂载，于是功能"存在"却点不到。补上 `resumeId={detail.id}` 即可。新增的用例特意写在**调用点**这一层（`ResumeDetailPreview.test.tsx`）：组件级测试抓不到这种断链，只有从预览点进「微调内容」再断言页签出现才能守住；实测把那一行去掉，用例立刻变红。

- **「模板市场 → 使用此模板」不再假装已经套用**。该按钮此前只弹一句"已选用「互联网」模板组合：现代 样式 + 紧凑 版式 + 标准字号 字号"，**不会写入任何配置**——而模板市场在「工作台」，压根没有一个"当前简历"可套。文案说"已选用"、按钮叫"使用此模板"，用户会以为生效了、导出后才发现没变。现在按钮改为**「这套怎么选」**、提示改为**「…的建议组合：…。生成或预览简历时，用「样式」和「版式」按这个组合选一下即可。」**（`message.success` → `message.info`，因为并没有"成功"发生什么），README 与 `docs/user-guide.md` 里"一键套用"的说法同步改掉。

- **求职助手对界面功能的了解从 21 项补到 56 项，并加上防呆闸门**。用户反馈「问怎么给简历加水印，助手说没有这个功能」——而它明明有。用 12 个「某功能存不存在 / 在哪」的问题实测，**8 个答错**：把「纯文本导出」说成没有、把「通用简历」指到错页面、否认「匹配度参考分」存在、说会话不能导出/存资料箱、查不到照片上限、说投递台不能选浏览器也没有失败诊断、说首页没有快捷入口清单、说简历没有备注字段。**根因不是它胡编——它从不编造，不知道就明说"我不敢替你下结论"——是它手上的功能目录只有 21 条，而界面上实际有 56 项**（README 功能表列了 43 行）。修法是把 `feature_catalog.FEATURES` 按**逐个读前端代码核对过**的真实路径补全到 56 条（并修正了原有条目里的错路径：`version_diff` 不在简历详情而在列表行「更多操作」、质量检测的按钮就叫「质量检测」、分享包按钮叫「离线分享」、简历生成根本不在「简历中心」而岗位行与「我的资料」），然后**把锚点换成 README 功能表**：新增守卫测试要求 README 每一行都能映射到目录、映射里的 key 必须真实存在、目录里不能有 README 未提及的孤儿条目、README 表与映射双向一致。于是「加了功能 → 按 AGENTS.md 更新 README → 忘了登记给助手」会**直接测试变红**（实测：临时改掉一个 key，报错精确指出「多格式导出 → watermark」），而不是等用户问到才发现助手答"没有这个功能"。目录渲染（每请求动态注入）从 2.5KB 涨到 7.8KB，仍只占总提示词约 6k tokens。**另有三个"文档超前于实现"的功能如实标注为「当前界面不可达」而不是指路或否认**：简历写作增强（STAR/话术/润色/互译，组件齐全但「写作增强」页签只在拿到简历 id 时挂载、两个调用点都没传）、匹配度参考分（后端算好且带免责，前端类型与界面从未接过）、以及「模板市场 → 使用此模板」只弹提示不套用配置。改后同样 12 个问题复测 **12/12 正确**。

- **「卡住」的判定收敛到一处**。`/api/stats` 的 `stalled_application_count` 与求职看板的 `stalled_count` 此前各算各的（阈值 `_STALLED_DAYS = 7` 写在路由里），是同一件事的两份实现。现在共用 `models/tracker.py` 的 `STALLED_DAYS` / `is_stalled`，并新增一条**跨接口一致性**用例钉住两处给出同一个数——否则会出现"卡片标红但统计不计数"。`/api/stats` 仍走 SQL 过滤（首页是热路径），但阈值与判定口径来自同一处，等价性由该用例守着。

- **比率口径收敛到一处**。`analytics._rate` 与 `referral_service.referral_stats` 各写了一份 `round(..., 4) if denominator else 0.0`，现在抽到叶子模块 `services/ratios.py`。**放叶子模块而不是某一方内部**是因为看板要复用内推的转化口径、必须 import `referral_service`，反向 import 就成环。

- **投递日期留空的代价现在说得出来，且一键可填**。手动新增进度的表单里，「投递日期」此前只标「YYYY-MM-DD」——留空是阻力最小的路径，而后果（该记录不进投递趋势与周内分布）用户看不到，统计页那张空图就是这么来的。现在字段旁写明后果，并加了一个「填今天」按钮（沿用纯文本输入，不引 DatePicker）。**刻意不预填今天**：投递台的自动记录知道日期（`record_applied` 是"刚投出去"，唯一调用方也从不传 `applied_on`），手工录入**不可能**知道（可能投递当天录，也可能三周后照着一封通知补录），替他填会把记录钉在错误的月份上、在趋势图里造出一个从未发生过的尖峰。两条路径的差异是**正当的**，已在 `_apply_payload` 就地注释说明，免得被下一个人当成不一致"修"掉。**现有数据不回填**：那几条是键盘测试数据，填任何日期都是往刚建立的统计口径里注入伪造数据，正确做法是让缺口提示把它们点出来。

- **求职统计页的漏斗图与趋势图不再随窗口放大**。用户反馈"六阶段漏斗和近 6 个月投递趋势里的内容明显太大，和整页不协调"。根因不在图表里的数值，而在 **SVG 的 `viewBox` 缩放**：两张图都是 `viewBox="0 0 620 …"` + `width="100%"`，浏览器会按「容器宽 ÷ 620」把**整张图连同字号一起放大**——宽屏（内容区约 1900px）下放大 3 倍，13px 的标签被画成 40px、28px 的条被画成 90px、170px 高的趋势图被画成 520px，而同一页的指标卡仍是 13/20px。此前一轮"紧凑化"（条高 42→28、图高 240→170）之所以没效果，正是因为它调的是**被放大的那组数值**、倍率一点没变；当时的用例只断言 `viewBox` 里的数值（`height < 260`），所以"宽屏下被放大 3 倍"这个真问题照样通过。修法两处：① 两张图的 `<svg>` 加 `maxWidth: viewBox 宽度`，把倍率**封顶在 1:1**（宽屏不再继续放大，窄屏仍按比例缩小），并居中避免封顶后贴左；② 两张卡从各占整行改为 `Row` + `Col xs={24} xl={12}` **并排**，宽屏各占一半（列宽≈图表原始 620px，正好 1:1），窄屏自动堆叠，卡片补 `height: 100%` 让并排时两列底边齐平。新增的回归守卫钉住真正的不变量——**渲染宽度不得超过 `viewBox` 宽度**（`svg.style.maxWidth === \`${viewBoxWidth}px\``），它在本次修复前以 `expected '' to be '620px'` 失败；另加一条用例断言两张图位于同一个 `Row` 内。

- **PDF/Word 导出的性别字号与预览一致**。用户反馈"下载的 PDF 里性别和姓名一样大、一样粗、一样是强调色"，而预览里性别是姓名旁的小号灰字。根因是 `pdf_exporter._draw_resume` 把性别拼进 `name_line = name + "  " + gender`、整行用 `name_ratio`（1.86× 基准、粗体 B、强调色）渲染；`docx_exporter._header` 同样拼成一个 run。修法是把性别拆成独立段：PDF 用新增的 `plan_name_gender` / `draw_name_gender`（姓名按 `name_ratio`/粗体/强调色，性别按新增的结构化模板参数 `name_extra_ratio`/常规字重/muted 灰），Word 把性别写成独立 run；测量路径（`measure_content_height`）与渲染路径严格互逆（"量的就是画的"），性别不再并入姓名行的字号计算，放不下时沿用 multi_cell 折行（与预览 flex wrap 一致）。`name_extra_ratio` 按项目铁律纳入 `TEMPLATE_LAYOUT_DEFAULTS`（classic/modern 1.0、compact/minimal/elegant 0.93、technical 0.95），由 `ResumeLayout` 带入 PDF、不硬编码副本；`test_resume_layout.py` 的 CSS 漂移守卫同步补上 `.name-extra` 核对条目。无性别时行为不变。

- **PDF/Word 导出的照片不再被压扁变形**。用户反馈"PDF 里照片被拉伸变形"，而预览里比例正常。根因是 fpdf2 的 `image()` 在同时给定 `w` 与 `h` 时**强制拉伸**到该尺寸、不保持宽高比，用户照片宽高比 ≠ 模板照片框比例（`photo_width_ratio / photo_height_ratio`）即变形。修法是新增纯函数 `crop_image_to_cover(data, target_ratio)`（Pillow 居中裁剪，`object-fit: cover` 的等价物，顺带 `ImageOps.exif_transpose` 转正手机竖拍方向），PDF 与 Word 共用这一份实现；比例已一致且无 EXIF 方向时原样返回不重编码。Pillow 已是 fpdf2 既有依赖，不新增运行时依赖。坏图仍走既有"跳过照片"兜底路径。新增纯函数用例（横图/竖图/比例一致/坏图）与端到端用例钉住。

- **投递台的 BOSS 采集、筛选、自动沟通与时间显示修复**。城市筛选不再把“杭州”等中文名直接塞进 `city=` 导致 BOSS 返回 `code=19 / 参数值错误`，而是把城市名动态解析成站点编码（常用城市有离线兜底，未知城市会明确报错）；配置多个关键词时逐个搜索并跨关键词去重，单批上限仍按整批计算。搜索/详情接口的非零错误码会先于 DOM 兜底明确失败；网络解析的宽容**只给字段、不给容器**——卡片内的字段改名（`jobName`→`jobTitle` 等）仍能解析，而容器与键只认站点自己的名字（`zpData` / `jobList` / `jobInfo`），`data`、`result`、`jobs` 这类泛化名字一律不接受：页面上几十个响应里任何一个都更像"岗位列表"，而取的正是第一个匹配项，猜错就是静默写进垃圾岗位。容器改名时返回"抓不到"并退回 DOM，**不返回空列表**（那会被说成"这次真的没搜到"，用户以为关键词太窄）。页面结构变化时按“接口响应 → 多候选 DOM → 明确诊断”降级，不再把错误页里的推荐链接伪装成搜索结果。详情阶段遇到未登录或验证码不再被吞成“成功但 JD 为空”，而是原样提示用户处理。自动投递修复三处确定性问题：勾选行提交真实 `job_id`（不再把队列行 id 当岗位 id）、BOSS 的立即沟通不再强制要求本地岗位版简历、沟通流程等待聊天输入框并支持 `contenteditable`，点击真实发送按钮后以本人消息或成功提示确认结果。采集结果会在批次完成时自动刷新，切换批次清空旧选择；采集记录、投递记录和进度时间统一把后端 UTC 转为本地时间。为控制站点改版的影响面，原先 1052 行的 BOSS 适配器拆为城市、页面探针、搜索和投递模块，926 行的任务运行器拆为生命周期、投递执行、采集执行与配置解析模块，原导入路径保持兼容。
  **真机联调又逮到两个只有连着真实站点才会出现的根因**，两者的症状都指向别处，值得单独记下来。① **就绪判定被站点的导航链接提前触发**：`SELECTOR_SEARCH_READY` 里那条宽松的 `a[href*="/job_detail/"]` 会命中页头「职位搜索」导航项——它的 `href` 恰好是 `https://www.zhipin.com/job_detail/`（没有岗位 id、也不以 `.html` 结尾），而且比岗位卡片**先**渲染（实测 0.66s vs 2s+）。于是"页面已就绪"在 0.9 秒就被判真、网络订阅窗口随之关闭，而 joblist 接口此时还没发出，**整条"网络优先"路径静默失效**；退回 DOM 又抓到那条导航链接本身，采到的"岗位"标题是「职位搜索」。修法是要求岗位链接必须以 `.html` 结尾，精确区分导航项与真实岗位（兜底仍只认链接形状、不认 class，卡片改版时照样有效）。② **登录/验证码判定不看可见性**：BOSS 把登录弹窗的整套模板（`.sign-form` 等 6 个）写在页面标记里、默认隐藏，岗位详情页尤其明显——用 `!!document.querySelector(LOGIN)` 判定的话，**每打开一个详情页都会被判成"需要登录"**，采集在抓完列表、开始补详情时整批失败，而用户其实是登录着的（实测：登录状态下这 6 个节点都在，但 `getClientRects().length` 全为 0）。现已改为"看得见才算拦截"，同一个判据也用在空态上（否则站点若把空态容器当模板藏着，每次搜索都会被判成"确实没搜到"而安静返回 0 条）。两条都有回归测试钉住（`test_boss_page_probe.py`）。

- **岗位匹配度分析证据核对误杀**：匹配分析要求每条结论的「原文证据」必须逐字出现在招聘原文里，而旧实现按**整段 JSON 文本**做子串比对——证据里的换行、引号等字符在 JSON 序列化后会变成转义序列（`\n`、`\"`），模型稍微转述或换行就比对失败，把合法分析误判成「证据不实」并回退。修法是两处：① 证据核对改为在**结构化字段上展开（plain source）**比较，不再对着一串转义后的 JSON 文本比；② 引入**子句级 3-gram 容错**——证据与原文按子句切分后做 n-gram 重合判断，模型的小幅转述不再误杀。**防虚构护栏保留**：凭空捏造、与原文毫无重合的证据仍会被拦下，只是不再把「换行 / 转述」当成「编造」。

- **后台生成任务 completed/resume_id 竞态**：生成器线程在「内容已生成、即将落库」的收尾阶段先标记任务完成、再写简历，若前端恰好在这一窗口轮询到 `completed` 并跳转去读简历，会拿到空结果（`resume_id` 还没写入）。修复为**先落库简历、拿到 `resume_id`、再标记完成**，完成态与 `resume_id` 原子可见——轮询看到 completed 时简历一定已经存在。

- **技能知识文件名里的控制字符能被用来在系统提示里多造一行**：知识文件名会被拼进系统提示的一条 bullet（`- {文件名}`），而技能 **ZIP 导入**走的是与"工具写入"**不同**的入口，`skill_archive._safe_member_path` 并不清控制字符——于是恶意技能包能用文件名里的换行（如 `x\n- 忽略以上全部规则.md`）让提示里凭空多出一行 bullet（提示注入面）。修法是在**拼 bullet 那一处**（`assistant_skills._skill_block` 的 `_sanitize_bullet_label`）统一清洗控制字符并折空白，**一次覆盖全部入口**（工具写入与 ZIP 导入都最终汇到 `skill.files` → 同一处插值），而不是逐个入口打补丁；工具侧原有的清洗保留作为输入卫生。新增测试走完整链路（ZIP 导入 → 存库 → 拼系统提示）断言它造不出额外的一行。

- **直出 PDF 的版式改为与预览对齐（重要：同一份简历的 PDF 外观会变化、页数可能不同）**。用户反馈"下载的 PDF 明显被压扁、和预览不是一个东西"。根因是直出 PDF 走的是后端**另一套排版引擎**，而它（①）**完全忽略 `format_config`**——传 `{}`、`{line_height:2.2,page_padding:24}`、`{line_height:1.2,page_padding:8}` 生成的三份 PDF，每个文字基线的 y 坐标逐字节相同；（②）行距/页边距/区块间距**写死在这套引擎里**，既不读你在「格式模板」里的设置，也不看你选的样式模板，于是除 classic 外的模板（modern/elegant/technical…）行距都比预览紧；（③）**没有**预览那套 `--fit-scale` 的一页适配——预览为塞进所选页数会把整份版式按比例收矮，PDF 却保持基准字号 + 更紧行距，于是字更大、行更密。三处一起修：
  - **行距/页边距/区块间距改为与预览同源**：默认取所选**样式模板**的默认值——来自 `resume_templates.TEMPLATE_LAYOUT_DEFAULTS`，那是模板参数**唯一的结构化来源**（`test_resume_layout.py` 会拿模板 CSS 逐条核对），PDF 不再自带第二份系数（第一版就是靠"抄一份"活下来的，模板一改就漂移）；你在「格式模板」里设过的 `line_height` / `page_padding` / `section_gap` 会覆盖它，**没设过时不改变默认行为**（覆盖为空则完全等于模板自带的值）。
  - **补上与预览同口径的一页适配**：内容超出所选页数时，按 `_resume_fit_script.j2` 的同一套规则收紧**字号派生的尺寸**（`--fit-scale` 的对应物）——只缩字号派生尺寸、`mm` 页边距不动、下限 `0.5`、最多 3 轮、每轮按 `可用高 / 内容高 × 0.995` 跳档，高度比较的容差对齐到 1px（≈0.26mm）；**关键：是否"装得下"以真实渲染出来的页数为准**，而不是连续内容高度。连续高会**低估**分页后的实际高度——`ensure_space` 为不切断条目会提前换页、在页尾留出一段空隙，于是"连续高 < 可用高 + 容差"仍可能排成两页；只按连续高判定会出现"请求 1 页、产出 2 页，而 `overflow` 还在说装得下"的**说谎标志**。到下限仍多页就如实置 `overflow`（`ResumePDF.scale` / `ResumePDF.overflow`），并且最终渲染后**再兜一次**（`pages > page_limit ⇒ overflow=True`），**不裁内容**，与预览"塞不下就溢出并提示"一致。
  - **测量与决策分开**：新增 `measure_content_height`（只回答"某个缩放下内容多高"）与 `rendered_page_count`（真实渲染、只回答"排出几页"）两个测量入口，与 `decide_fit_scale`（只回答"缩到多少 / 是否溢出"，纯函数）分离，后续要做"内容超出时**并排展示多页**而不是截断"时只需替换决策这一步。
  - **回归测试换成"读回真实 PDF 几何"**：旧测试是拿 PDF 自己的常量跟自身比，结构性测不出这类漂移。新增一组用 `pypdf` 读回文字矩阵的断言——行距覆盖生效（相邻正文基线间距随之变化，能抓住"又被忽略"）、页边距覆盖生效（x 起点随之变化）、**各模板的行距与注册表一致**（期望值从注册表读，不抄常量）、一页适配会缩字号装下 / 装不下时如实标记且不丢正文，以及**"连续高说装得下、真实分页却两页"的说谎边界**（用刻意构造的"盲区"内容钉住——旧实现会返回 `scale=1.0, overflow=False` 而实际 2 页；把修复改回去这条会直接变红）。**字号层级系数（姓名 / 求职意向 / 联系方式 / 区块标题 / 条目标题 / 条目副行 / 技能标签的比例）也已一并结构化**进 `TEMPLATE_LAYOUT_DEFAULTS`：此前 PDF 侧藏的是一份**只对 classic 成立**的副本，导致 modern / elegant / technical 等模板的层级比例与预览最多差约 9.6%（technical 实测 9.62% → 0%）；现在 PDF 从注册表读、不再自带第二份，`test_resume_layout.py` 的 CSS 守卫**逐模板逐条**核对这组比例（含"只改注册表"造成的漂移也逮得住）。
  - **用户可见影响**：因为口径变了，**同一份简历的 PDF 外观会与早先版本导出的不同、页数也可能变化**。`docs/user-guide.md` 的「下载 PDF」说明与「常见问题」已同步为新行为。

- **站点改版时会"静默抓错"**：岗位列表接口的**卡片内字段**一旦被改名或挪层级，采集器以前会安静地解析出 `title=""` 的"岗位"——上层看到的是"采到了 N 条"，而数据是废的，用户既不报错也拿不到东西（他只会以为自己不会用这个筛选条件）。现在**读不出岗位名的卡片不算一条结果**；如果整页卡片都读不出名字，就判定为「结构不认识」并**明确失败**（退回 DOM 那条路 + 记一条日志），而不是拿空岗位交差。这条是用**改版变异**测出来的：在已知的合法结构上做字段改名/挪层级，看我们是明确失败还是静默产出垃圾——后者才是最该报警却最安静的那种改版。
  刻意保留的两处降级：**公司名**读不出来时保留这张卡片（岗位名才是承重字段，丢掉它用户就少看到一个岗位，且不知道为什么）；**详情**里岗位名读不出来但正文在时仍然算一条（详情这条路的价值本来就是正文）。两处取舍都写进了测试，免得被"顺手"改成一律丢弃。
- **新增契约漂移回归测试** `tests/test_site_contract_drift.py`（20 例）。起因是仓库里**一份真实抓取样例都没有**（`tests/fixtures/` 是空的），现有测试用的全是**手写 payload**——那只能证明"解析器能解析我们想象出来的结构"，证明不了"站点的真实结构还能被解析"。所以这份测试换了个角度：拿已知的合法形状做**变异**（字段改名、字段消失、列表多套一层），把"哪些改版必须明确失败、哪些允许降级"固化成断言。

- **任务配置快照里的"非配置字段"不再让整份配置退回默认值**。`TaskRunner._load_config` 把 `task.config` 还原成配置模型（`extra="forbid"`），一旦遇到多余键就抛 `ValidationError` 并被兜底**静默退回全默认值**。补详情会在 `task.config` 里带一个只属于这次任务的 `backfill_job_ids`，若不处理，用户设的限速会被一起丢掉——而且完全不报错。现在还原配置时显式剔除这类"只属于这次任务、不属于配置"的键（`_load_config(ignore=...)`），既保住用户设置，又不把一次性字段塞进公开的配置接口。
- **求职助手能读到（并修改）已删除的内容**：助手工具里几处**单条读取**用的是 `db.get`，而它会绕过查询过滤——列表里看不到的岗位 / 简历 / 资料，只要知道 id 就能被助手读出来、甚至继续被改（比如给一份已删除的简历调版式）。这与 P5 立下的"回收站里的内容不会出现在其它任何地方"直接冲突，属于"删了但还能被翻出来"。修在**共用实现** `trash.get_live()` 上（6 处替换），而不是每处各写一遍 `is_deleted` 判断——少一处就漏一处。助手工具里剩下的 4 处 `db.get` 是深挖会话 / 备选岗位 / 技能 / 模拟面试，它们不在回收站范围内，保持不变。
- **求职助手读简历读不全**：`get_resume` 以前只返回 `id / title / content`，把**模板、版式覆盖、页数上限、字号档位、目标岗位、来源、生成警告**全丢了——可助手手里就有 `update_resume_layout`：不知道当前模板与页数，等于让它闭着眼睛改版式，改完还回一句"已按你的要求调整"。现在一次给全。
  顺带修掉一个更隐蔽的问题：简历正文超出 6000 字符预算时，原来是**从中间截断整段 JSON**——模型会收到一段缺了结尾的内容，进而以为简历里就这么多东西，回答时漏掉后面的经历（而用户完全看不出来）。现在改成**按重要性整段省略**（荣誉 → 校园经历 → 技能 → 项目 → 教育，经历留到最后），并在结果里明确写出 `content_omitted_sections` 与一句说明，让模型知道自己没看全、可以再单独去查。

- **模拟面试：只打了空格的人设会渲染出一个空的「面试官人设」标题**（P6 普查时发现）。提示词里用的是 `{% if persona %}`，而 Jinja 认为**纯空白字符串为真**——于是用户在人设框里敲了几个空格再删掉、或粘贴了一段空白，模型就会看到"面试官人设（用户自定义、优先级高于默认风格）"这个标题下面什么都没有。它很可能据此**自己编一条人设**，比不给更糟。修在 `build_interview_messages`（进提示词的唯一汇聚点，不依赖调用方是否 strip），考察重点同理——纯空白的 focus 会盖掉"未指定，按面试类型通用考察点提问"的兜底文案。
- **补齐"用户自定义必须真实生效"的端到端断言**（P6）。这类问题的特征是**存进去了但没用上**，所以只测"保存成功"完全发现不了；新增的断言一律验到**最终产物**上：
  - **投递招呼语**：用户为某个岗位自己写的那条必须真的发出去，而不是被默认招呼语顶掉（此前只测过"默认招呼语被写回记录"，自定义那条谁赢谁输没人管）；
  - **自制简历样式模板**：模板里的样式必须真的出现在渲染结果里、且同样拿到版式自适应脚本（此前只测过"危险内容能不能洗掉"，那是"能不能安全地存"，和"存进去的东西有没有拿去渲染"是两件事）；
  - **联网搜索的两项设置**：关掉开关时必须**同时**收走工具与那段"你拥有 web_search 工具"的说明（只删工具会让模型去调用一个不存在的工具）；把"抓取正文条数"从 0 调大后，工具描述与系统说明必须一起改口（反过来会让模型放着拿到的正文不用、回头跟用户说"我只能看到摘要"，用户于是以为这个设置没用）；
  - **面试的难度 / 轮数 / 考察重点 / 人设**：四项逐项独立断言（一起改四项再断言"都在"，可能掩盖"其中一项其实是被另一项带进去的"），并单独验"没填人设时那一段整段消失"。
  计划里最初怀疑的"面试人设被默认值覆盖"**经实测证伪**——人设、技能提示词、格式模板参数三条链路本来就是通的，所以这次主要是把它们的正确行为钉住，防止日后被重构悄悄改掉。

- **采集去重的判据只留一份实现**（`job_service.find_by_job_identity`）：采集、暂存区、以及"勾选导入"三处共用它。以前采集器自己写了一份判据，而导入是新加的——各写一份必然漂移，症状会很刺眼：采集说"这条没采过"，导入时又说"岗位广场里已经有了"，同一件事给出两个相反的答复。判据本身也**两张表都查**：只看岗位广场会在暂存区堆出重复候选（勾一次就重复导入一次），只看暂存区会把已经在岗位广场里的岗位重新捞回来。
- **「来源」常量收敛到一处**：采集与候选导入写进 `Job.recognition_source` 的两个取值定义在 `schemas/job.py`（与读取白名单同一处），不再由两个模块各抄一遍字符串——抄两遍的结果是其中一个改了另一个不知道，而症状正是上面那条 500。
- **采集到的岗位名被粘上了薪资，而且数字显示成方框；薪资列全空、职位描述与任职要求也对不上**（用户实测反馈）。从库里读出真实数据后，根因是 **BOSS 的两类反爬混淆**，以前完全没有处理：
  ① **字体反爬**——数字被映射到 Unicode 私用区（`U+E030`~`U+E039` 依次对应 `0`~`9`），DOM 里读出来的就是这些码位，界面上显示成方框；常用汉字还会被换成**康熙部首**（`⼯` `⽤` `⽅`）。真实标题是 `全栈工程师\ue032\ue033-\ue032\ue036K`，用户看到的就是"全栈工程师-K"。私用区映射用 18 条真实数据交叉验证过（9 个数字全部自洽），不是猜的。
  ② **水印 token**——在中文词中间插入品牌词做抄袭溯源：`本boss科` / `业boss务` / `使kanzhun用` / `长直聘期`，直接混进了 JD 正文。
  另外**新版列表把岗位名与薪资放在同一个节点里**，于是"取标题"把薪资一起取了进来，而独立的薪资节点取到空——两个缺陷叠在一起才是用户看到的完整症状。
  修法：新增 `sites/boss_text.py`（纯文本、可离线逐条测）做还原与拆分；**顺手修掉一个差点踩进去的坑**——整段做 NFKC 会把中文全角标点（`：` `（`）一并折成半角，为修几个异体字而改掉整篇标点得不偿失，所以**只对部首/兼容表意字那三个区做折叠**。水印规则也定得很窄：**只有 token 夹在汉字之间、或位于文本开头且后接汉字时才删**——品牌名 `BOSS直聘` 本身就含这两个词，绝不能误伤（有测试钉住）。已修复库里 18 条历史数据（薪资空 18 → 0）。
- **「职位描述」与「任职要求」被混在一起，或两个都不显示**（用户实测反馈）：接口只给一整段 `postDescription`，而界面上是两个字段——以前一律整段塞进描述、要求留空，于是用户看到"两件事混在一起"。现在按 JD 里**真实存在的小标题**切分（取最早出现的 `任职要求`/`岗位要求`/`任职资格`/`【任职要求】` 等），切分点太靠前（整篇本来就只有要求段）时**不切**，把全文留在描述里——否则会造出一个空的「职位描述」，比不切更难看。同一条修复让 18 条历史数据里的 8 条拿到了独立的任职要求段。
- **采集的岗位有近一半（8/18）职位描述完全是空的**（用户实测反馈）：不是文本问题，是**详情页根本没抓到**，两个原因叠加。一是详情页的"就绪"选择器里含了 `.job-name`，而详情页顶部/推荐位也有岗位名，于是等待在 JD 的 XHR 回来**之前**就判定"就绪"，网络层提前收网、拿不到详情接口的响应。二是识别详情接口的路径片段写死成整串（`/wapi/zpgeek/job/detail.json`），站点给路径加个版本后缀、改一段命名，整条"网络优先"通路就**静默失效**（失败方式是退回 DOM，从外面完全看不出来），而 DOM 恰好是最容易被改版打穿的那一层。修法：就绪选择器只认真正属于 JD 正文的容器（并补上新版布局的候选），接口识别改成**多片段候选**（URL 宽松 + 结构严格把关），列表与详情两套候选**互不重叠**（有测试钉住，避免把列表响应当详情解析）。
- **采集成功之后，岗位广场一片空白、首页统计全部清零，两处都报"服务器处理请求时发生内部错误"**（用户实测反馈，最严重的一次）。后端日志里的根因很清楚：`JobOut` 通过**继承 `JobCreate`** 复用了字段定义，于是把**输入侧的白名单校验**也一并带进了读取路径——采集器写入的来源是「岗位采集」，而 `RECOGNITION_SOURCES` 里没有它，`JobOut.model_validate(row)` 就抛 `ValidationError`。列表接口是逐行构造响应的，**一条"按旧规则写入"的数据足以让整份岗位列表、首页统计、投递台选岗一起 500**；用户在界面上看到的是"数据全没了"，而库里的岗位其实完好无损。两处修：① 白名单补上「岗位采集」（它本来就是合法的录入来源）；② **读取侧不再重跑输入校验**——把继承来的校验器逐个覆盖成直通（同名方法替换父类实现），并把 `note_images` 从"输入侧上限 2 张"里解放出来（那是单次请求体的限制，不是这一列能存多少）。**输入侧校验照旧生效**（来源 / 状态 / 图片仍会被拦），修的是读取容错而不是把闸门拆掉——有测试同时钉住这两侧（`test_job_out_read_tolerance.py`，含一条"库里存在采集岗位时 `/api/jobs` 与 `/api/stats` 必须 200"的端到端回归）。
- **简历预览弹窗的底部按钮要一路翻到底才看得见，且弹窗底部多出一条横向滚动条**（用户实测反馈）：底部操作条改为**吸附在弹窗底部**（`position: sticky`），内容再长也不用翻到最后才点得到。横向滚动条则是另一回事：那一行原本是两个**不换行**的分组做 `space-between`，宽度不够就横向溢出。同一类问题在别处也有——只设 `overflow-y: auto` 时浏览器会把 `overflow-x` 也算成 `auto`，内容溢出 1px 就冒出一条滚动条（用户截图里的"不需要左右翻页却有滚动条"）。因此给 **8 处弹窗体**统一补上 `overflow-x: hidden`，并给底部操作条加 `flex-wrap`。
- **模板预览不是按 A4 纸显示**（用户实测反馈）：内置样式预览把 iframe 拉成"容器宽 × 72vh"，比例与真实 A4 完全不同——用户既看不出排得好不好看，也看不出"一页装不装得下"；而「复制改一份」右侧的预览更直接：**写死 `transform: scale(0.54)`**，列宽是可变的，于是要么没铺满、要么被裁掉（这正是"预览没有一次性展示全"的原因）。新增 `A4PreviewFrame`（`components/templates/`）：统一画一张 794×1123 的 A4 纸，按容器实测尺寸等比缩放（`ResizeObserver` 而非写死断点），**完整一页永远可见**；内置样式大图预览、模板一览、样式模板编辑器三处共用它，删掉两套各写一半的旧样式。
- **投递台采集仍然失败：报"页面没有切换到目标地址"，而浏览器里岗位列表已经渲染好了**（用户第二次实测反馈）。上一轮加的"等待页面就绪"确实生效了，但**两个各自独立的缺陷叠在一起**，谁都过不去：
  ① **新鲜度判据太严**：原判定是"当前地址与拼出来的目标地址**整串相等**"，而 BOSS 会把 `/web/geek/job` 规范化成 `/web/geek/jobs`、并补默认参数，于是**每一次正常导航都被判成"旧文档还没走"**，等满 15 秒超时后报出那句"页面没有切换到目标地址"——页面其实早就好了。现在改成**"目标地址里的每个查询参数都在当前地址里取到相同的值"**：既容纳站点的规范化（路径改名、参数顺序、多出的参数都不影响），又仍然拦得住"还停在上一页"（`page=1` 与 `page=2` 的路径完全相同，只看路径会把翻页读成上一页的内容——这条有回归测试钉住）。
  ② **卡片选择器过期**：`job-card-wrapper` 早已不是 BOSS 的岗位卡片 class（现为 `li.job-card-box`），于是"匹配到的控件数"恒为 0，页面等待必然超时。选择器改为**多候选并列**（新旧 class + 结构相对定位），并新增一个更稳的锚点 `a[href*="/job_detail/"]`——改版可以换 class，但"点进去看岗位"必须有个链接。
  更关键的是**改掉了"DOM 判据能否决接口数据"的结构**：以前页面等待一失败就立刻抛异常，哪怕网络层手里已经拿到完整的岗位列表。现在页面等待的失败**先记下来**，采集按「① 接口响应 → ② 已记录的等待失败 → ③ DOM 卡片 → ④ 岗位链接兜底 → ⑤ 带诊断的失败」逐级降级——**接口这条路只要拿到了岗位，DOM 改版与否都不再影响采集成功**。同时补上一个只在"三条路都拿不到任何岗位"时才触发的诊断失败：以前那种情况会返回一个"0 条"的空页，把"工具坏了"说成"关键词没搜到"，用户于是永远不会把诊断反馈回来。顺带修掉两个同源缺陷：翻页标志一直取的是"页面探针"的值，而那个探针根本不报 `has_next`（等于**永远没有下一页**，采集在第 1 页就停），现在优先读接口响应里的 `hasMore`；以及"页面已就绪但卡片选择器失效"时会静默返回 0 条。
- **求职助手一进多选就出现一大片空白**（用户实测反馈）：`.assistant-workspace` 的网格写死了三行（标题 / 消息区 / 输入区），而多选态会在标题下多插一条选中操作栏，于是子元素从 3 个变成 4 个、**网格按位置错位**：操作栏顶替了消息区的 `minmax(320px, 1fr)` 主行、自己被撑到 320px 高（表现为标题下方一大片空白），消息区反被挤成 `auto` 缩到内容高度、连滚动区都没了。补一行 `auto`（`.assistant-workspace.is-selecting`）即修复——多一条工具条就该多一行，而不是顶掉主行。
- **版面诊断内容一多，简历预览就被挤得很小，而缩放标签还写着 100%**（用户实测反馈）：预览高度原本按"窗口高度 − 预览容器顶端位置"算，于是**上方内容越长、预览越小**，而标签显示的是"缩放系数"（`fitScale × zoom`）而不是**实际比例**——用户看到的是"我什么都没改，预览却缩水了，还说是 100%"。两处修正：预览高度改成**按窗口给固定预算**（不再跟随上方内容），上方内容再长也只让弹窗自身滚动；缩放标签改为显示**真实比例**（默认"适应页面"时显示实际百分比，放到真是 100% 才显示 100%），重置按钮的文案也跟着改成「适应页面」。同时给诊断建议列表加了高度上限与内部滚动——建议可能有 5~6 条，它不该占掉预览的空间；简历预览弹窗也加宽到 `min(960px, 96vw)` 并让弹窗体自身滚动。
- **点「开始采集」时浏览器里明明加载出了岗位，应用却说"共新增 0 个岗位"**（用户实测反馈）：`collect_search` / `fetch_job_detail` / `open_apply` 在 `new_tab(url)` 之后**立刻**执行采集脚本——`new_tab` 只是让浏览器**开始**打开地址，返回时文档还是空的，`querySelectorAll` 匹配 0 个元素，脚本于是返回空列表、`has_next` 为假，采集循环第一页就 `break`，`collected = 0`。这个缺陷最恶劣的地方不是"没采到"，而是它把"什么都没做"伪装成"采集完成"，用户完全无法察觉自己一个岗位都没拿到。修复：新增"等待页面可用于采集"的通用原语 `browser/page_ready.py`（导航后轮询直到目标选择器匹配数 > 0，或出现登录失效 / 验证码时**立刻**失败，或超时失败；等待可被用户点的"停止"打断——轮询步经过 CDP 客户端既有的检查点，不用一个长 `time.sleep` 把停止信号堵在外面），采集与详情两个站点方法复用它；导航改为**复用同一标签页**（`Page.navigate`），不再每页 / 每岗位各新开一个标签（按 20 个岗位算会堆出 20+ 个标签页）。
- **采集的 0 结果不再无条件当成功**：区分"关键词真的搜不到"（页面明确呈现无结果状态 → 返回空页是合法的）与"没抓到"（**必须失败**，并按项目既有约定抛出带可操作诊断的失败：当前 URL / 页面标题 / 匹配到的控件数 / 期望控件的候选描述）。**关键取舍**：等待的**唯一失败时限是超时**，**不**按 `readyState === 'complete'` 提前判失败——`readyState` 只代表文档与静态资源加载完毕，**不代表 SPA 该渲染的内容已经渲染完**（BOSS 直聘的岗位卡片是 `complete` 之后由 XHR 异步渲染的，首屏稍慢就会被误判成"结构变化"）；超时后再按证据给不同文案：没切换到目标地址 / 已加载但找不到卡片（可能结构变化，可反馈给维护者）/ 尚未加载完（网络慢或拦截，稍后重试）。采集过程中抛出的 `SiteFailure` 现在会被任务运行器接住并让任务**失败**（message 带上诊断），而不是像以前那样被顶层兜底成一句"内部错误"、或被当成"完成"；真正的 0 则在完成态明确说明"没有找到匹配的岗位（关键词或城市可能太窄）"，整页都是重复岗位时则明确说"都已存在、没有新增"。几种收尾在界面上一眼可辨。
- **多份招聘信息导入后，确认面板里每份的"原文"都是空的**：拆分时让模型给出的原文摘录只被当作字段锚点用完就丢，前端读的 `recognized_text` 一直是空串——用户看不到可核对的原文，也就无法判断拆分对不对。现在每份草稿都带回**它自己那段**摘录（没有可用模型、退回本地切分时，每段本身就是原文）。
- **联网搜索"最多 3 次"实际是 4 次**：打开联网开关时那次自动预搜没有计入预算，而系统提示、README 与使用指南都写着 3 次。现在预搜也占一次额度（失败的预搜不占——它没给模型任何资料）。
- **开了正文抓取，却仍告诉模型"不打开网页"**：工具描述与系统提示里都写死了"只返回搜索摘要（不打开网页）"。描述是模型判断"这个工具能给我什么"的唯一依据，于是它放着拿到的正文不用、反复换关键词搜，或者直接告诉用户"我只能看到摘要，建议你自己去看"。现在两处措辞都跟着「抓取正文的条数」走。
- **Anthropic 协议的 Base URL 提示少了 `/v1`**：接口协议那一栏的提示写的是 `https://api.anthropic.com`，而 provider 拼的是 `{base_url}/messages`——照着提示填会打到官方根本不存在的路径上，报错只会说 404/400，看不出是地址写错了。
- **助手系统提示否认了它自己有的能力**：提示里写着"教育经历、工作经历、项目、技能、奖项等结构化条目你也改不了"，而 `add_profile_entry` 一直能追加这些条目。模型照着提示回答，用户听到的是"我没有修改你资料的权限"——「把资料箱里的材料整理进个人资料」这条路就这样被挡在门外。现在改为：**能追加一条，但改不了已有条目**，并补了一条测试钉住这个矛盾（提示里必须提到工具，且不能再出现那句一票否决）。
- **系统提示里"不要声称已打开搜索结果页面"与设置冲突**：打开「抓取正文」后应用确实会抓取正文并交给模型，而这句写死的规则会让它放着正文不用。现在这条移到联网工具说明里，按是否抓正文分别措辞。
- **删除两处死代码与过时说明**：后端未使用的 `DEFAULT_ANTHROPIC_BASE_URL` 常量删除；README 与使用指南里"当前仅直接支持 OpenAI Chat Completions 兼容协议，原生 Claude 需要兼容网关"的说法已不成立，改为说明两种协议怎么选；设置页顶部那条"支持所有兼容 OpenAI Chat Completions 协议的模型服务"的提示补上 Anthropic 原生协议。
- **点「启动浏览器」只弹出一个空白窗口**：拉起浏览器时最后一个参数写的是 `about:blank`，于是窗口确实开了、调试端口也确实就绪（界面显示「运行中」），但用户面对的是一张白页——既不知道该去哪里，也没有可以扫码登录的页面，而界面还提示"请在弹出的窗口里扫码登录一次"。启动浏览器的全部意义就是打开那个要登录的网站，因此站点适配器新增 `entry_url`（BOSS直聘为 `https://www.zhipin.com/`），拉起时直接打开它。同时新增 `POST /api/apply/browser/open` 与界面上的「打开招聘网站」按钮：标签页被关掉或跳走时用它找回登录页，不必重启浏览器（走 `Page.navigate` 复用当前标签页，反复点不会越堆越多标签页）；`BrowserStatusOut` 增加 `entry_url` 并在界面上展示，用户能提前看到这个窗口会去哪里。
- **联网搜索会把口语当关键词、并因此返回一堆字典页**：开着联网问"帮我把外卖平台项目整理成台账条目"时，搜索引擎把整句分词后只保留了首字「帮」，拿回来 8 条字典 / 百科页；而**相关性过滤器在"问题里没有任何求职词"时会整体短路放行**（`terms` 为空 → `return results`），那套"明显无关"过滤根本没机会执行，于是垃圾全被塞进模型上下文。两处一起修：**没有任何求职词时也丢掉高置信度的明显无关结果**（字典 / 百科 / 词典 / 笔顺 / 拼音这类；判定刻意保守——只要结果里出现招聘 / 求职线索或链接带 careers / jobs 标记就不算"明显无关"，判不了相关就保留）；**三个搜索来源（Bing / DuckDuckGo / SearXNG）统一经过同一套查询改写**，剥掉「帮我 / 请 / 把…整理成 / 一下」这类口语填充词（改写是**可测的纯函数**，本来就正常的 query 不被改坏、整句都是填充词时退回原句）。零结果报错也不再以"含求职词"为前提——非求职问题被过滤空时同样如实报错，而不是静默返回空让模型瞎编。
- **采集来的岗位技能标签为空（列表页标签栏空白、搜索与匹配按空标签走）**：早期采集器有一条自己直写 `job` 表的老路径，只写了正文、从没算过 `keywords`，于是库里留下了一批"有正文却 `keywords='[]'`"的岗位。当前采集链路已改成「暂存 → 导入 → `create_job_record`（内部 `refresh_job_keywords`）」，**新采集不再复现**；但已经落库的那批既重采集修不好（按 `source_url` 去重会跳过），也补不了详情（那条路径只补空，而这些行有正文）。新增一次性修复脚本 `scripts/repair_job_keywords.py`：**默认只预览、不改库**，`--apply` 前自动把数据库整体备份到 `data/backups/`，**只补空、绝不覆盖**你已有（或改过）的标签，且**幂等**，并复用运行时同一实现重算（不重拼一遍解析逻辑，否则两边会漂移）。顺带修好采集导入的**来源**——新导入路径曾漏传 `source`，会让采集来的岗位落到默认值「手动添加」，现在透传站点名（与旧数据口径一致，如「BOSS直聘」）。补齐两处此前缺失的回归测试：**导入后技能标签非空**、**采集导入保留真实来源**。

- **粘贴多份招聘文本的自动拆分更稳健**：含分隔符、编号等边界的多份招聘信息拆分更稳定，减少"该拆没拆"或"该合没合"的边界错误。

## 0.8.0 - 2026-09-17

### Added

- **模拟面试（新模块）**：侧栏新增「模拟面试」。可设定关联岗位、面试类型（技术面 / 项目深挖 / 行为面 / HR 面 / 综合面 / 案例分析 / 英语面试 / 压力面）、难度、面试官风格（严谨专业 / 温和引导 / 持续追问 / 压力质询）、轮数与自定义人设，面试官逐轮提问、点评上一答并追问，满轮或主动结束后生成评分报告（总分 + 四个维度 + 做得好的地方 + 可改进项）。报告可以一键存进资料箱的「面试复盘」分类。**流程由代码控制**：轮数、何时结束、何时出报告都由后端决定，模型只负责同一轮里问什么、怎么点评，所以界面上的"第 N/6 轮"是可信的。助手新增 `list_interview_sessions` / `get_interview_report` 两个工具，可以读取记录帮你复盘（面试本身需要用户逐轮作答，助手不代答）。
- **多份招聘信息一次导入**：粘贴含多份招聘信息的材料（招聘网站的整段复制、聊天记录、多张截图与文档）后，系统会拆成多份草稿并**先让你确认再保存**——每份显示岗位名、公司、地点、薪资与原文摘要，可取消勾选不要的、可"单独编辑"某一份，也可一次保存勾选的全部。拆分走 AI（提示词 `job_multi_extract.md`，每份必须给出**原样复制的原文摘录**作为字段锚点，跨段字段会被丢弃）；没有可用模型或 AI 拆不出来时退回本地按分隔线 / 序号小标题 / 第二个公司标签切分。新增 `POST /api/jobs/parse-multiple`。
- **简历模板体系**：样式模板从 3 种扩到 6 种（新增优雅 / 技术 / 极简），并新增**格式模板**——只调行高、页边距、区块间距、强调色、字号系数的版式覆盖，与样式模板自由组合（例如"优雅样式 + 紧凑版式"）。`GET /api/resumes/templates` 一次下发模板、字号、格式参数清单与内置版式预设；生成前与预览时都能切换，`PATCH /api/resumes/{id}/layout` 只重新渲染、不重新生成内容。
- **工作台（技能 + 模板）**：原「技能工作台」升级为「工作台」，分「助手技能」「简历模板」两页。模板页可以逐个预览每种样式（真实渲染缩略图，可切字号与版式）、从内置模板复制一份来改（右侧实时预览，服务端渲染所以看到的就是导出效果）、导入 HTML 文件、自制格式模板（只填参数，不用写代码）、重命名与删除。自制模板走**沙箱渲染**：导入时剥离 `<script>` 与外链并补齐 CSP，渲染用 Jinja `SandboxedEnvironment`。
- **所有导入入口都支持拖拽**：岗位识别、备选岗位截图、资料箱附件、个人照片、助手附件统一用一个拖拽容器（`FileDropZone`）——直接把文件从资源管理器拖到对应区域即可导入，按 `accept` 过滤不支持的类型并给出提示；只接管文件拖拽，不影响页面内其它拖拽（例如资料页的区块拖动排序）。
- **助手消息可以引用、删除与导出**：右键任意一条消息可「引用这条继续问」（被引用内容的快照存进消息里，原消息删除后引用仍可读）、复制或删除；输入框上方显示引用条，只对下一次提问有效。会话列表的「更多」新增「导出为 Markdown」（含时间、附件名、助手做过的操作与参考来源）与「存进资料箱」（直接变成一条面试复盘材料）。
- **可以退出应用**：侧栏底部新增「退出」，点击后后端优雅退出，页面切成"已退出"并提示如何重新启动（此前只能去关掉后端窗口）。
- 新增接口：`POST /api/jobs/parse-multiple`、`/api/resume-templates`（自制模板增删改查 + `builtin-source` + `preview`）、`/api/interview`（创建 / 列表 / 详情 / 逐轮回答 / 结束 / 删除 / 存进资料箱）、`/api/assistant/conversations/{id}/export` 与 `/to-material`。
- 新增 Alembic 迁移 `0009_templates_and_interview`：新建 `resume_template`、`interview_session`、`interview_message` 三张表；`resume_record` 加 `format_name`；`chat_message` 加引用字段。

### Fixed

- **真实运行环境端到端联调发现的两个问题**（用隔离数据库 + 本机假模型服务跑通 82 项检查时暴露，已各自补上回归测试）：
  - **模拟面试开场必然 500**：面试官需要候选人的个人资料作为提问依据，而那段代码把资料按 `model_dump()` 序列化后用 `json.dumps` 直出——资料里带 `created_at` / `updated_at` 两个 `datetime`，`json.dumps` 直接抛 `TypeError`。已改用 `model_dump(mode="json")`。此前单测把这一步打桩绕过了，所以只在真跑时才会出现。
  - **生成时选的格式模板没有存进记录**：生成接口把样式模板、页数、字号都写进了 `resume_record`，唯独漏了 `format_name`，于是重新打开预览或导出时会悄悄退回模板自带版式——用户会以为"我选的版式没生效"。
- 列表行的操作不再重复：岗位广场的「详情 / 生成简历」与简历中心的「预览 / 导出」此前在行上蓝色链接和「更多」菜单里各出现一次，菜单现在只保留行上没有的操作；收藏夹同理——「取消收藏」只由行上的星标按钮承担，不再出现在菜单里。整行右键仍给出完整清单（右键时鼠标不在行内链接上，补全更方便）。
- **模型列表端点与 Base URL 校验共用一套规则**：`POST /api/settings/llm/models` 复用 provider 的地址校验（远程必须 HTTPS、HTTP 仅限本机），避免两处判断漂移；`cn.bing.com` 不可用时联网搜索回退 `www.bing.com`。
- **简历预览页脚的按钮组溢出**（截图反馈）：按钮换成可换行布局，不再把「完成」挤出弹窗。
- **使用指南的步骤标题互相挤压**（截图反馈）：步骤改为左侧竖排导航 + 右侧内容，标题每项独占一行，再加模块也不用重新排版；内容补上资料箱、工作台、模拟面试与对话导出。
- **资料图片上限前后端不一致**：后端常量曾是 6 张，与界面上写的 2 张不一致，现已统一为 2 张（总文件数 4 个）。

### Changed

- **求职助手页头不再重复显示技能**：右上角的技能模块去掉（输入框旁边已有可勾选的技能菜单，勾选框不再只是高亮）。
- **消息与页头的时间、思考强度更清楚**：思考强度加上文字标签（只显示「默认」时用户不知道在调什么）。
- **个人照片一行横排、可左右滑动**：此前多张照片竖着堆会把「我的资料」右栏撑得很长。
- 助手系统提示与默认引导对话同步新模块（模拟面试、工作台模板、消息引用与导出），并明确"助手开不了模拟面试、只能读取记录帮你复盘"。
- 使用指南、README 与 CHANGELOG 同步本次新增能力；版本号提升到 `0.8.0`。

## 0.7.0 - 2026-09-17

### Added

- **资料箱（新模块）**：侧栏新增「资料箱」，用来放还没整理进简历的零散材料——证书、作品、链接、笔记、实习/校园材料。条目可以带正文、链接、备注与附件（图片最多 2 张、文本文件最多 4 个，正文与图片都存在本地数据库）。求职助手可以直接创建、修改和读取资料（`list_materials` / `get_material` / `create_material` / `update_material`），所以"帮我把这段记下来"现在有地方落。
- **助手技能工作台（新模块）**：侧栏新增「技能工作台」，可以新建技能（提示词 + 知识文件）、查看与编辑已有技能的提示词全文、启停和删除，页面上有「技能是什么 / 工作台怎么用」的引导和三个可直接改用的模板（简历要点精炼、面试问题预测、岗位匹配分析）。设置页的「助手技能」列表现在点技能名就能打开同一个详情弹窗。后端新增 `POST/PUT /api/assistant/skills` 与 `GET /api/assistant/skills/{id}`（详情带回知识文件正文，超出上限时标记 `files_truncated`，前端据此避免覆盖式写入）。助手也拿到了 `list_skills` / `get_skill` / `create_skill` / `update_skill` 四个工具。
- **备选岗位**：岗位广场新增「备选岗位」抽屉，把还没核对的招聘信息（粘贴文本或截图）先存下来，之后再点「导入到岗位」用正式表单预填原文导入，导入状态会标记成"已导入 #id"。助手同样可以增改与导入（`list_candidate_jobs` / `create_candidate_job` / `update_candidate_job` / `import_candidate_job`，重复导入不会创建第二份）。
- **简历版式可选**：生成前可以选最大篇幅（1-3 页 A4）、字号（小/标准/大）与模板（经典 / 现代 / 精简）；默认一页 + 标准字号。内容塞不下时预览里会提示，并提供"增加到 N 页 / 改为小字号"一键重排——只重新渲染，不重新调用模型。三套模板共享同一份正文片段与 `data-resume-path` 标注，点击定位编辑照旧可用。
- **导出 PDF 可以直接下载**：新增服务端 PDF 生成（`fpdf2` + 系统中文字体：Windows 微软雅黑/黑体、macOS PingFang、Linux Noto CJK），`GET /api/resumes/{id}/export?format=pdf` 直接返回文件；找不到中文字体时该按钮退回「浏览器打印 / 另存为 PDF」，接口返回 409 并说明可执行的处理办法。两个入口都在导出菜单里。
- **生成简历时可以补充要求**：生成弹窗新增「补充要求（选填）」，最多 2000 字，会附在生成提示词后面影响表达方向（事实锚定、篇幅上限与防虚构规则不变）。
- **个人照片可以存多张**：我的资料页的简历照片支持上传多张（最多 8 张）、点缩略图切换使用哪一张、删除（删掉正在用的那张会自动改用下一张）；切换时同步写回 `user_profile.photo`，因此简历生成、预览与导出链路一行未改。旧数据里已有的一张照片会在首次打开照片面板时自动转成可管理的记录。
- **大模型配置增强**：模型名称旁新增「获取可用模型」（`POST /api/settings/llm/models`，按当前账号拉取服务商模型列表并可直接选用）；快速预设从 5 个扩到 15 个（新增通义千问、智谱 GLM、MiniMax、硅基流动、OpenRouter、Gemini、xAI Grok、Groq、Mistral、LM Studio）；新增可选的「高级调整」——Top P、频率惩罚、存在惩罚、随机种子，留空即不发送。
- **求职助手增强**：输入框下方的工具条里可以直接开关已启用的技能；新增「思考强度」选择（默认/关闭/低/中/高，通过请求级 `reasoning_effort` 下发，不支持的服务商会忽略）；消息气泡显示发送时间；首次进入且没有任何会话时自动创建一条带内置引导的对话（`prompts/assistant_welcome.md`）。
- **助手会话管理对齐常见聊天应用**：每条会话的「更多」或整行右键支持置顶、收藏、复制分享链接（`/assistant?conversation=<id>` 深链）、重命名、在新对话中继续（复制最近 10 条消息到新会话）、移动到分组、复制会话 ID、归档与删除；侧栏筛选新增「已归档」，列表显示消息条数与更新时间。后端 `chat_conversation` 新增 `archived`、`group_name`，并新增 `POST /api/assistant/conversations/{id}/fork`。
- **助手可以自己联网搜索**：打开「联网搜索」后会下发 `web_search` 工具，助手能自己决定搜什么、换关键词多搜几次（**一轮回答最多 3 次，由代码强制**，超出后工具会告知模型次数已用完），命中的来源会累积到回答下方的来源卡片里；搜索实现同时改进——结果上限 5→8、按主机名与路径去重、用人单位官网与招聘页优先排序、`cn.bing.com` 不可用时回退 `www.bing.com`。
- **软件更新检查与一键更新**：设置页新增「软件更新」卡片，调用 GitHub Releases 对比本地版本（结果缓存 15 分钟，网络不可用时只提示不报错）；项目根新增 `update.cmd` 与 `scripts/Update-ResumeForge.ps1`——git 检出走 `git pull --ff-only` + 依赖同步，普通目录下载 zip 后只覆盖程序文件，`data/`、`.env`、`runtime/`、`node_modules/`、`.venv/` 一律不动，支持 `-DryRun` 预览。
- **列表操作不再裸露**：岗位、简历、收藏夹、技能工作台与设置页的技能列表，行操作都改成「主操作 + 更多菜单」，删除只出现在菜单里并需二次确认；整行支持右键唤起同一份菜单（岗位：详情/生成简历/自行编写/相关简历/编辑/删除；简历：预览导出/重命名/收藏/删除）。首页的「最近岗位 / 最近生成的简历」两张卡片不在此列：它们只是跳转链接，右键菜单没有可放的动作。
- **岗位备注支持图片**：备注可以再放两张图片（招聘截图、内推码等），列表里显示图片数量、详情抽屉里可放大查看；助手新增岗位时会标注 `AI 助手录入`。
- 新增运行时依赖 `fpdf2==2.8.4`（连带 `fonttools==4.65.0`，均钉死版本、纯 ASCII 注释）。
- 新增 Alembic 迁移 `0008_materials_and_modules`：新建 `material`、`candidate_job`、`profile_photo` 三张表；`chat_conversation` 加 `archived`/`group_name`，`job` 加 `note_images`/`recognition_source`，`resume_record` 加 `template`/`page_limit`/`font_scale`/`custom_instruction`，`llm_config_record` 加 `top_p`/`frequency_penalty`/`presence_penalty`/`seed`；`database_compat.py` 的老库补列清单同步更新。
- `GET /api/resumes/templates` 返回可用的模板、字号档位、默认值与"服务端能否直接生成 PDF"；`PATCH /api/resumes/{id}/layout` 只改版式不重新生成。
- 新增接口：`/api/materials`（增删改查 + 分类）、`/api/candidate-jobs`（增删改查 + 导入标记）、`/api/profile/photos`（上传/切换/重命名/删除，最多 8 张）、`/api/update/check`。

### Fixed

- **软件更新后旧备份导不回来**（用户反馈的"导出的数据在更新后还能不能导入"）：两处根因都已修掉。
  - 备份格式校验从 `format != BACKUP_FORMAT_VERSION` 改成"只拒绝比当前代码更新的格式"，并把版本号类型校验补上——此前只要导出格式升到 2，所有历史备份会立刻被判为"不受支持"；现在 `format=1` 的包在将来仍可导入，来自更新版本的包给出"请先升级应用"的明确提示。
  - 应用表清单（`_APPLICATION_TABLES`）从手写常量改为从 `Base.metadata` 推导——此前新增一张表却忘记同步清单，会让**自己导出的备份因为"缺少数据表"被拒收**，而这类故障只有用户真去恢复数据时才会暴露。
  - 未知表与 `sqlite_sequence` 的处理同步放宽（后者是 SQLite 内部表，不再被当成"未识别的数据表"）。
- 求职助手联网搜索在无关结果上的误报收敛：结果去重与官网优先排序后，第三方平台仍保留但排在官方招聘页之后，不会再让百科/诗歌类结果出现在摘要里。

### Changed

- **岗位的录入来源会自动写进备注**：保存岗位时按识别输入自动补一行「来源：粘贴文本识别 / 图片识别 / 文档识别」（从备选岗位导入或助手录入时分别标注对应来源），已标注过的不重复追加；岗位详情抽屉新增「信息来源」一栏。
- **助手系统提示重写**：补上各模块的用法（岗位、备选岗位、资料箱、技能、简历版式）与技能改动的确认要求，并新增一段"联网搜索工具已开启"的使用规则（何时搜、怎么换关键词、摘要不可信、不得声称已打开网页）。
- 使用指南：步骤标题在 720px 弹窗里会互相挤压（显示成"完善资料…"），已改为允许换行并留出间距；六步的文案同步了资料箱、备选岗位、照片多张、版式选择、直接下载 PDF、技能开关、会话菜单、更新方式等新能力，最后一步改名为「数据备份与更新」。

- 岗位识别、个人资料识别与助手附件新增**文档**支持：PDF 与 DOCX 的文字由后端在**本机**提取，原始文件不外发，因此文档识别不需要多模态模型，没配置模型时本地规则也能解析出一部分字段。
  - 提取出的文字与粘贴文本合并成同一份 `source_text`，字段锚定校验照旧生效。**不插来源标记**：本地规则会把 `[文档：jd.docx]` 这样的孤行当成正文（岗位描述里会多出一行文件名），文件来源在界面上本来就以附件形式可见。
  - 解析不可信文件也各自设限：PDF 最多读 30 页、一次识别的文档文字总量封顶（超出时截断并在结果里说明）、DOCX 只读白名单成员 `word/document.xml` 而不解压整包。
  - 扫描件（没有文字层的图片型 PDF）会明确提示改用截图上传，加密 PDF 提示先解除保护，损坏或改名的文件在调用模型之前就以 422 失败。
- 识别与助手附件新增常见图片格式：`bmp`、`tif/tiff`、`jfif`。BMP/TIFF 先在本机转成 PNG/JPEG 再发送——多数服务商不接受这两种格式，直接放行等于必然返回 400。截图类内容优先用无损 PNG，超限才退到 JPEG 并逐级降采样；TIFF 在浏览器里渲染不出来，暂存区与消息里显示为文件名标签而不是破图预览。
- 新增运行时依赖 `pillow==12.3.0`、`pypdf==6.18.1`（均钉死版本，纯 ASCII 注释）；Windows 一键启动器的依赖探针同步包含这两个包，旧虚拟环境会在下次启动时自动补装。
- 不支持的文件给可执行的提示，而不是笼统的"格式不受支持"：HEIC/HEIF、SVG、旧版 `.doc`、RTF、ODT、xlsx/pptx 各有对应文案（例如 HEIC 提示先在系统里导出为 JPEG，`.doc` 提示另存为 `.docx` 或 PDF）。
- 文档提取的说明会跟着附件回传并展示：`ChatAttachmentOut` 新增 `notes`，`kind` 增加 `document` 取值（旧记录与旧客户端不受影响，缺省为空）。
- 新增 `scripts/Build-Release.ps1`：发行包只从 git 档案生成（`git archive <ref>`），因此不会漏掉跟踪的文件、也不会夹带私人数据——`backend/data/`、`backend/.env`、`runtime/`、`node_modules/` 与各种缓存都被 `.gitignore` 挡在外面，`.env.example` 是唯一例外（它本就要随包发送）。写完后脚本会重新打开压缩包自检：必需文件在、必需目录非空、禁止路径不在、`start.cmd` 保持 CRLF 行尾；任何一项不过就**删除该压缩包并报错**，不会留下半成品。CI 在 Windows 上运行 `scripts/tests/Test-Build-Release.ps1`，真的打一次包并断言包内文件与 `git ls-tree -r HEAD` 完全一致、出包清单与后端 `app/preflight.py` 的运行时清单一致。

### Fixed

- **修复手工压缩包缺文件时"完全看不出病因"的问题**（真机反馈：朋友解压后双击 `start.cmd`，后端在导入阶段就抛 `FileNotFoundError: backend\app\data\skills.json`，而控制台只有一句 `Backend exited with code  before becoming healthy. See runtime\backend.stderr.log.`——退出码是空的，也没有任何可执行的线索）。三处一起改：
  - **启动器把病因打出来**：后端/前端未就绪时，控制台直接打印 `runtime/backend.stderr.log`（或 `frontend.stderr.log`）的最后 12 行，并修掉退出码为空时印出 `exited with code  before` 的问题（`ExitCode` 在某些时刻仍是 null，`Get-ProcessExitCodeText` 现在会退回 `unknown`）。
  - **启动器回收自己遗留的进程**：端口被上次启动留下的进程占着时，先按记录（PID + 启动时间 + 命令行三者都匹配）结束它再启动，而不是让用户自己去找。只有端口被别的程序占用时才报错，报错文案不变。
  - **后端启动前自检**：新增 `app/preflight.py`，由 `app/__init__.py` 在导入任何子模块之前调用。缺文件或技能词典结构不对时抛一条中文消息（缺哪些文件、怎么处理），不再是一串导入栈。
- 进程记录的读写原语从 `Stop-ResumeForge.ps1` 移入 `ResumeForge.Common.ps1`（`Stop-RecordedProcess` / `Get-ProcessRecordMatch` / `Get-ResumeForgeProcessPattern`），启动路径与停止路径共用同一套 PID、启动时间、命令行校验，避免两处各自漂移；属性读取改走 `PSObject`，手改过的记录在 `Set-StrictMode` 下也不会抛错。

### Changed

- **简历生成的写作口径重写**（`prompts/resume_generate_system.md`）：角色改为资深 HR 视角，并补齐三类此前没有的约束——
  - **措辞标准**：去口语、空话套话与互联网黑话（"赋能""抓手""闭环"）；强动作动词开头，但**不得升格职责范围**——"参与"不能写成"主导"，"协助"不能写成"负责"。这条属于防虚构的一部分。
  - **篇幅与粒度**：每条描述性要点 30 字以内、同一段经历或项目的要点不超过 6 条、个人总结 2-3 句；单段内优先核心成果，其次核心职责，最后日常事务。
  - **分模块呈现要求**：工作/校园/项目/教育/技能/奖项各自给出什么内容——技能只能选候选事实里已有的项并按熟练度与岗位相关度排序，教育只在资料里有 GPA、课程、荣誉时呈现，奖项没有就输出空数组。
  - 岗位分支的第 2 条增加「先拆解 JD（高频硬技能、职责动词、职级、行业术语）」与关键词自然融入，并明确**只在候选事实确有对应内容时**使用：候选人确实不具备的 JD 要求不硬塞，避免"关键词覆盖"变成新的虚构入口。
  - 原有 9 条规则一条未删（真实性、事实锚定、参考事实、量化、输出格式、身份字段留空、岗位筛选/全貌优先、附件事实不得遗漏），只重新编号；通用简历的「全貌优先」等约束逐字保留，并新增按两种模式渲染模板的回归测试。
  - 这些约束只在**提示词层**生效，代码不做强制校验；`docs/prompt-tuning.md` 新增「措辞与篇幅口径」一节说明边界，并写明不要为了字数上限去放宽 `resume_quality.py` 的质量门槛。
  - 按这套口径真机生成一份简历后补了第 12 条：**候选条目本身没有可写事实时不要输出空壳条目**（资料里有个项目只有名称，被候选筛选选中后模型原样输出，成品上就是一条没有内容的经历）。
  - 真机两轮实测后按结果收紧：字数上限从「30 字以内」改为「30 字左右、最多不超过 35 字」（两轮各有一条贴线超出），个人总结补上「不超过 120 字」，并要求经历/校园/项目的要点以强动作动词开头、写清"做了什么 + 结果"，项目 highlights 不能只堆技术名词。
  - **新增代码侧措辞门槛**（`services/resume_wording.py`）：提示词点名禁掉的黑话与空话由本机做确定性检查（`CLICHE_WORDS` / `CLICHE_PHRASES`），命中后复用既有的**一次质量重试**改写（重试提示词同步说明了短板清单里可能只有措辞问题）；重试后仍命中时在结果 warnings 里告知用户，而不是静默留下。只在启用美化拓展时检查——关闭美化时正文是用户资料原文，改它等于篡改用户自己的表述。
  - 四轮真机实测后，把**可度量**的约束（每条要点最多 35 字、单段不超过 6 条、个人总结不超过 120 字、不得出现黑话与空话）同时写进用户提示词末尾的「输出前自查」清单：只写在系统提示里时，超标条数在 0~5 之间波动（最长 42 字）；加上清单后落在 31-34 字区间（上限内），黑话也不再触发改写。
- 前端识别暂存区从"图片暂存"扩展为"截图与文档暂存"：`useImageStaging` → `useRecognitionFiles`、`ImageStagingField` → `RecognitionFileField`，按钮文案与 `aria-label` 改为「添加截图或文档」，接受的文件类型由 `utils/attachments.ts` 统一导出（`RECOGNITION_ACCEPT` / `ASSISTANT_ACCEPT`）。
- `POST /api/jobs/parse-text` 与 `POST /api/profile/parse-text` 新增可选请求字段 `documents`；图片与文档是两个字段，但共用同一份额度：一次最多 4 个、单个 ≤ 2 MB、合计 ≤ 5 MB。未传 `documents` 时行为与之前完全一致。
- **附件格式判定改为「内容为准」**：扩展名（以及浏览器按扩展名给出的 MIME）与实际内容不符时，按内容的真实格式收下。图片站/CDN 常对 `.jpeg` 链接按浏览器偏好返回 WebP，另存下来名字与内容就对不上——此前这种情况会以「内容与声明图片格式不一致」直接拒绝，用户既不知道真实格式也不知道下一步。现在这类文件照常可用：按真实格式发送给模型，并在附件说明里注明「文件实际是 WebP，已按真实格式处理」。被改名的文档同样按内容判定（真 docx 命名为 `.pdf` 也能提取文字）。
  - 放宽的只有"名字 vs 内容"这一层：内容认不出、或真实格式不在白名单内（HEIC/HEIF、AVIF），仍然拒绝，且提示会写明真实格式与可执行的处理办法（改成 `.png`、先导出为 JPEG、"那是文档，请改名 `.pdf`"等）。声明 MIME 与扩展名不一致、data URL 前缀与扩展名不一致，仍按原样拒绝。
  - 个人照片（`PUT /api/profile` 的 `photo`）仍是旧规则：它的校验在 `schemas/profile.py`，与附件不共用实现，本次未放宽——`schemas` 目前不依赖 `services`，为复用原语而反向依赖不划算，留作后续（先把校验原语移到中立模块再统一）。
- **奖项不再参与岗位相关性筛选**（`profile_matching._select_entries`）：奖项名里几乎不会出现岗位技能词（"国家励志奖学金"对任何 JD 都是零分），按相关度打分等于每份定向简历都静默丢掉全部奖项——真机四轮生成里 `awards` 一直是空数组，用户填过的获奖经历一次都没出现在成品里。现在奖项按资料录入顺序取到 3 条上限，与通用简历走同一条语义；**校园经历仍然按相关性丢弃**（这两类此前共用一条分支，已拆开）。

## 0.6.0 - 2026-09-16

### Added

- 识别结果现在**常驻标明来源**：「AI 识别」还是「本地规则」以往只在识别完成的提示条里出现一次，提示条几秒后消失，而这两者的可信度并不相同——AI 识别的每个字段都校验过能在你给的内容里找到出处，本地规则只是按模式匹配。现在岗位识别与个人资料识别的结果下方都有一枚常驻标记，悬停说明该来源意味着什么、要核对到什么程度；改动粘贴的文本会撤掉标记，因为那次识别的结论已不再对应当前内容。
  - 原 `RecognizedTextField`（只显示模型抄录的原文）扩展为 `RecognitionOutcome`，同时负责来源标记与抄录折叠块，两个识别入口共用一份实现，不会各说各话。

### Fixed

### Changed

## 0.5.0 - 2026-09-16

### Added

- 全应用补了一轮**悬停提示**，目标是"看不懂的地方都能问一下鼠标"，而不是给每个控件都挂上提示：
  - **只有图标的按钮现在都有说明**。此前有 5 个漏了——对话标题的保存、简历编辑器里每条经历的删除、通用简历的重命名与删除、技能卡片的删除；它们只有 `aria-label`，读屏能用，但看得见的人悬停上去什么也得不到。删除类按钮的提示还会说明**删掉的是什么范围**（例如技能会连知识文件一起删）。
  - **说不清自己的词都补了人话**：简历预览的「抓手 / 编辑」两个模式（"抓手"是设计软件的行话，"编辑"听着像跳转到别的页面）、简历列表的「美化拓展」档位标签（"均衡"单看等于没说，现在悬停会讲这一档到底做了什么，用的是生成弹窗里那份文案，通用简历的"深度"档说法也跟着区分）、数据集与模型配置记录上的「当前」（"当前"指代不明，现在说明是哪一份在生效）。
  - **长内容不再既被截断又看不到全文**：助手的附件名来自用户、长度没有上限，而标签本身不换行，此前会把聊天气泡顶变形——现在截断到 260px 并保留悬停查看全名；助手页头的会话标题（用户可编辑到 128 字）同样加了截断与悬停全文；调整资料模块顺序时被截断的模块名也补上了。
  - 悬停提示一律走**既有约定**：图标按钮的 `title` 与该按钮的 `aria-label` 同源，表单字段继续用 `Form.Item` 的 `tooltip`，与周围代码保持一致。

- 应用内**使用指南**补齐了它一直没跟上的功能：从 5 步扩到 6 步（配置 AI / 完善资料 / 导入岗位 / 制作简历 / 求职助手 / **数据备份**），并把已有步骤的措辞更新到当前行为——截图识别、通用简历、纯手动配置预设、最大输出 Token「不限制」、助手可以直接读写数据（但永远不会删除）、以及数据集导出与切换。
  - `docs/user-guide.md` 一直是新的，只有这份应用内的指南停在最初版本；而它才是用户（尤其新用户）唯一会主动打开的那份。
  - 新增 `AGENTS.md` 的「使用指南同步」一节，把"用户可见的新功能要同步改哪一处、什么情况可以不改"写成规则，供后续维护者与协作代理遵守。
  - 指南里每一步的 `path` 由测试对着 `App.tsx` 的真实路由表校验：改了路由名而没改指南，测试会直接失败并点名是哪一步；步骤标题的唯一性与每步的文案完整性也在同一处守着。

### Fixed

### Changed

## 0.4.0 - 2026-09-16

### Added

- 求职助手页面现在会**显示正在生效的助手技能**：页头常驻一枚「技能：<名称>」标识（点它直接去设置页管理），让"回答为什么是这个风格"在界面上可解释——此前技能只在服务端改变作答方式，界面完全看不出来。没有启用任何技能时，空会话的引导区会说明**技能这个功能存在**并给出入口，用完一次就自动退场，不与页头重复。
  - 页头只列前两个名字，更多时收成「等 N 个」，长名字不会把标题挤变形。

- 大模型配置的「快速预设」新增**纯手动配置（不套用任何预设）**：选中后清空 `Base URL` 与模型名称，从空白开始填（API Key 不受影响），适合走中转站、公司网关等不属于任何内置厂商的地址。它与既有的「自定义模型（OpenAI 兼容）」区别只在选中时的行为——后者**保留**当前内容，用来避免误点后丢失已填好的接口信息；保存后下拉也会停在「纯手动配置」上，不会跳回「自定义模型」。

- 新增**通用简历**：在「我的资料」页可以编写**不针对任何岗位**的跨行业简历，用于方向还没定的投递。可以**AI 生成**（从资料一键生成，不做岗位定向）也可以**从头手写**，可以有多份并各自命名、重命名、删除。通用简历同样出现在简历中心（目标岗位列显示「通用简历」标签与求职意向）。
  - **生成时"全貌优先"**：不做岗位相关性筛选，也不重排——各方向的经历、校园经历、项目、技能和奖项都保留（只受单份简历的篇幅预算限制，超出的部分会在结果的告警里说明）。这与"把岗位传成空"完全不同：后者会静默丢掉校园经历与奖项、清空个人总结、并且只保留被经历提到的那些技能。
  - 求职意向沿用资料里用户自己填的那份，生成时不会编造岗位名；个人总结也不会出现"面向某某岗位"这类绑定岗位的表述。
  - 后端新增 `has_job` 列表过滤（`false` 只取通用简历）与 `PATCH /api/resumes/{id}` 改名接口（只改名称，不动正文与告警）。`POST /api/resumes/generate` 的 `job_id` 变为可选，并新增可选的 `title`。
  - 无关联岗位的简历记录本身一直是合法的（`job_id` 可空），因此**本次不需要数据库迁移**；但 `ManualResumeModal` 此前在无岗位时会提前返回、资料都不加载，导致通用简历根本打不开编辑器，这一处一并修好。
  - 简历中心与详情弹窗的「通用」标记一律按 `job_id` 判断：无岗位记录的 `job_title` 存的是求职意向，按它判断会把通用简历显示成岗位简历。
  - 「我的资料」页很长，通用简历卡片在页面底部，因此页面头部加了一个「通用简历」直达按钮。

- 「手动添加岗位」和「我的资料」的识别支持**图片输入**：可以直接在文本框里按 Ctrl+V 粘贴截图，也可以选择本地图片文件，一次最多 4 张（单张 ≤ 2 MB、合计 ≤ 5 MB，png/jpg/webp/gif）。图片与粘贴文本可以同时给，两边都参与识别。
  - **图片识别的防虚构做法**：模型在同一个请求里先**逐字抄录**图片文字（`transcription`），字段必须逐字出现在这份抄录里才会被采纳——沿用文本识别那套来源锚定校验。需要说明的是，这个保证比文本路径弱：它证明的是"字段与模型自己写下的文字一致"，而不是"字段来自用户给的材料"，模型把幻觉写进自己的抄录就能通过。因此识别的抄录原文会回传给前端供对照核对（`recognized_text`，默认折叠展示），图片模式的结果也会额外附一条「请对照截图核对」的提示。
  - **发了图片但模型没抄录时直接判失败**，不做"看起来成功"的降级：否则「文本+图片」的请求里，粘贴文本中合法锚定的字段会全部活下来、结果被标成 AI 识别成功，而图片其实一个字段都没贡献。
  - **识别图片需要多模态模型**。当前配置的文本模型会返回 HTTP 400，此时接口仍按既有契约返回本地草稿，并给出明确提示（「图片识别失败，可能是当前模型不支持图片输入（需要多模态模型）」）；未配置模型时也会说明图片识别无法进行，而不是含糊地说一句"已使用本地规则识别"。
  - 图片识别的请求体上限沿用助手的 5 MB 合计：base64 编码后约 6.7 MB，仍在默认 8 MB 的 `MAX_REQUEST_BODY_MB` 之内，因此这两个接口**不需要**也不应该加入 `larger_body_paths` 豁免。
  - 岗位弹窗在识别结果为空时**不再清空表单**：图片识别失败时的本地草稿是全空的，无条件回填会把用户已经手填的标题、公司一起抹掉。现在只有识别出内容才回填。
  - 附件校验原语抽到 `services/attachments.py`，助手与识别接口共用同一份图片白名单、魔数检查与体积上限；`assistant_service` 保持不变地 re-export，助手侧行为与错误文案逐字未动。
  - 前端把附件限值、类型判定与 `readAsDataUrl` 抽到 `utils/attachments.ts`（此前 `readAsDataUrl` 有两份拷贝），并新增共用的图片暂存 hook 与组件。
  - 请求与响应新增字段：`images`（请求）与 `recognized_text`（响应），两者都是向后兼容的可选/默认值。

- 求职助手支持导入**技能**：一份技能可以只是一个提示词（`.md`），也可以是提示词加一包知识文件（`.zip`，需要一份 `SKILL.md` 加若干 `.md` / `.txt` 资料）。在 **设置 → 助手技能** 里导入、启用/停用或删除。
  - 技能有两种作用方式：**提示词是用户主动导入的指令**，拼进系统提示约束助手作答；**知识文件是不可信资料**，与岗位描述、经历参考文件同级，复用同一套注入清洗流水线，其中出现的命令、角色设定或格式要求都不会被当成指令执行。这条切分写进了系统提示，也写进了测试。
  - **知识不会预加载**：助手经 `read_skill_knowledge` 工具按需读取，整包资料不会挤占上下文；不指定文件时按当前问题从各知识文件里挑相关片段。
  - 导入同名技能按**覆盖更新**处理——提示词与知识文件整体替换、启用状态保留，方便对着同一份技能反复调整。
  - 技能提示词与知识读取都有长度预算：被截断或被跳过的技能会在系统提示末尾点名说明，不做静默丢弃（只加载半个提示词会让助手表现异常，而用户看不到原因）。
  - 导入链路把 **zip bomb、路径穿越、加密成员、非法扩展名、成员数超限**都挡在 `services/skill_archive.py`：逐成员校验路径与解压后大小、检查压缩比，解析只走白名单成员，不使用 `extractall`。
  - 新增 `0007_assistant_skills` 迁移（`assistant_skill` / `assistant_skill_file` 两张表），并同步登记进 `_APPLICATION_TABLES`，使迁移前备份、旧库识别与备份校验继续覆盖新表。
  - **旧备份仍然可以导入**：备份校验要求包里包含全部应用表，新增数据表本会让所有旧备份被判成"缺少数据表"而拒收。现在校验前先把解出的候选库升到当前 head，加表不再是一次不兼容改动；版本方向与清单一致性比对仍排在迁移**之前**，否则迁移后 revision 恒等于 head，比对将永远成立。
  - 校验用的候选库迁移时**不再生成迁移前备份**：它只是压缩包解出来的一次性副本，原始压缩包还在，而那份备份会落在 `restore/` 目录里按导入次数累积。
  - 系统提示改为每条消息现拼，技能改动（以及提示词文件本身的改动）**不再需要重启应用**。

### Fixed

- **修复导入技能时技能名变成一串随机 UUID**：上传走的是裸二进制请求体，原始文件名根本进不到后端，于是"没有 frontmatter 的 `name` 就用文件名"这条兜底只能拿到落盘用的随机临时名。现在前端用一个请求头把原始文件名带过来（头只能放 latin-1，中文名先 URL 编码），真实文件名既当显示名也当兜底技能名。
  - 连带的**更严重症状一并消失**：技能名是"同一个技能"的判据，名字每次都随机的话，**同一个文件反复导入会不断堆出新副本**，而「助手技能」卡片上写着"导入同名技能会覆盖更新"——现在这句话才真的成立。
  - 请求头来自客户端，所以后端只把它当显示名：先剥掉路径部分（`../../evil.md` → `evil.md`）和不可打印字符再入库。
- 修复求职助手**每次进入空会话都会闪一下**：首次加载时"还没有会话"和"会话正在加载"在详情为空时长得一样，页面分不清，于是先画出引导、被骨架屏顶掉、再画回来。现在会等会话列表与详情都确定下来再决定画哪个。（回归测试用 `MutationObserver` 数空态元素的挂载次数，闪一下就会留下 `remove` + `add`。）
- 修复 `scripts/bump_version.py` 在中文 Windows 上**算完版本号却崩在最后一行提示**：控制台编码是 cp936，而"本次包含新增数据库迁移"那行提示里的 `⚠` 无法编码，抛 `UnicodeEncodeError` 让本该成功的 dry-run 以非零退出。现在输出流降级为 `errors="replace"`：无法编码的字符变问号，中文提示照常显示。

- **修复了三个会让"干净电脑双击 start.cmd"直接失败的问题**（全部在真机上复现；CI 因为跑在英文 locale、无空格路径、固定 Python 3.12 上，一个都抓不到）：
  - **中文 Windows 上 `pip install` 直接崩**：`backend/requirements.txt` 首行是中文注释，而 pip 24.x 对无 BOM 的 requirements 文件会回退到 locale 编码（中文 Windows 是 cp936），抛 `UnicodeDecodeError` 后首次安装即中止。现在该文件保持纯 ASCII，且启动器在**任何 Python 子进程启动之前**就设好 `PYTHONUTF8=1`（原来是装完依赖才设，只保护了 uvicorn）。同一文件也影响 `pip install -r requirements-dev.txt`，因为它是 `-r requirements.txt`。
  - **路径含空格时前端永远起不来**：前端启动手写了 `cmd /s /c ""<npm.cmd>" run dev …"`，而 `Start-Process -ArgumentList` 不加引号、`/s /c` 又会剥掉首尾引号，于是 `C:\Program Files\nodejs\npm.cmd` 变成裸路径，cmd 报 `'C:\Program' is not recognized`。现在把 `npm.cmd` 直接交给 `Start-Process -FilePath`，由它自己套好 cmd 包装——记到的进程仍是 `cmd.exe`，`stop.cmd` 的识别不受影响。
  - **Python 3.14 装不上锁定的依赖**：`py -3` 会挑最新的解释器，而 `pydantic-core==2.33.1` 没有 cp314 轮子（`No matching distribution found`）。现在支持的版本窗口是 **3.10 – 3.13**，按 `-3.12`、`-3.13`、`-3.11`、`-3.10` 依次探测（3.12 优先，与启动器自身安装的版本和 CI 一致）；只有 3.14 的机器会明确提示版本窗口并走既有的自动准备路径装一个 3.12。若 `.venv` 是超出窗口的解释器建的，会移到 `runtime/venv-unsupported-<时间戳>/` 后重建，而不是反复用同一个坏环境失败。
- 启动链路的其它修复：
  - `start.cmd` 现在用 `%*` 转发参数，`start.cmd -BackendPort 8010` 可用（此前 README 文档化了这些参数，而端口被占用时启动器恰好提示用户用它，却传不进去）。
  - 启动失败的清理改用 `taskkill /T`，不再孤儿化 `cmd → npm → node` 进程树（此前会残留 Vite 占着端口）。
  - 健康检查的超时从固定 30 秒改为按服务区分（后端 90 秒、前端 120 秒：冷启动要跑迁移，且杀毒软件会扫描刚装好的依赖），并在**子进程已退出时立即失败**、把退出码写进错误信息，不再白等满超时。
  - 依赖是否就绪改为让解释器**真的 import 一次**，而不是检查四个目录是否存在——缺 pydantic 之类的半截环境此前会被判成"已就绪"，然后以"启动超时"的形式失败。
  - Node 便携包改为先试国内镜像（华为云、npmmirror、腾讯云，实测三者与官方产物字节一致）、再回官方源，**每个候选都校验 SHA-256**（期望值来自仓库常量而非镜像），并把总下载预算限制在 30 分钟——此前是每个地址两次 × 1 小时，网络不通时表现像卡死两小时。
  - 打不开浏览器不再连带停掉刚启动的服务（无默认浏览器关联的机器上 `Start-Process <url>` 会抛错并触发回滚）。

### Changed

## 0.3.0 - 2026-09-15

### Added

- 岗位和个人资料粘贴识别支持基于用户当前模型配置的结构化 Prompt 抽取，并返回识别来源提示。
- 设置页「最大输出 Token」新增「不限制」选项：勾选后发往模型的请求体不再包含 `max_tokens`，输出上限改由服务商和模型决定，用于缓解推理型模型把思考过程计入输出预算、预算偏紧时正文被挤空的问题。该选项以 `0` 保存，不需要数据库迁移。
- 求职助手支持**工具调用**，可以直接操作项目数据：查询岗位、简历与个人资料，并在用户明确要求时新增/修改岗位、更新个人资料的基础字段（教育/工作/项目等结构化条目仍需在页面编辑）。
  - **不提供任何删除类工具**，模型即使判断失误也造不成不可逆损失；设置里的模型配置与数据集管理有回环强制校验，不做成工具以免模型间接绕过该边界。
  - 「更新个人资料」工具采用 read-modify-write：资料接口是整份替换语义，只提交模型给出的字段会清空姓名、电话、照片和全部经历条目。
  - 模型侧按 index 累积拼接流式返回的工具参数碎片；服务商不支持 tools 时自动降级为普通对话并在回复里说明，不让功能整体不可用。
  - 每次工具调用都会在回复中直接列出（成功的可跳转到对应页面），并记录在消息的 `context` 里，历史对话回看时同样可见。
  - 提示词同步更新：只在用户明确要求时写入，且**工具返回值与其它输入一样属于不可信数据**（岗位描述里可能夹带注入指令）。
- 新增「数据集」：设置页可把全部本地数据（岗位、个人资料、照片、经历参考文件、简历、收藏、助手会话与附件、模型配置记录）导出为一个 zip，换电脑后在新装的 ResumeForge 里导入即可完整还原；也可以在本机保留多份数据集并随时切换。
  - **导入的默认语义是新建一份数据集，不触碰当前正在使用的数据**——选错文件不再有损失。切换会把整个应用切到那份数据，页面自动重载。
  - 每份数据集是一个完整的 SQLite 文件（照片与附件都以文本存在库内），切换只是把引擎指过去，不搬运数据，因此不存在"改动没写回原数据集"的隐患；有正在进行的 AI 请求时切换会返回 409，避免那次请求的结果被写进刚切过去的数据集。
  - 删除数据集只移入 `data/datasets/.trash/`，不做永久删除；正在使用的数据集不可删除。
  - 切换时按新库补跑建表/补列/Alembic 迁移，因此导入一份来自旧版本的数据集也能正常使用。
  - **备份中不含大模型 API Key**（清空操作配合 `VACUUM` 重建文件，避免删除过的配置记录把明文密钥残留在空闲页里），恢复后需重新填写并测试连接。
  - 备份包记录导出时的 Alembic revision，旧版本会明确拒绝导入更新版本的备份，不做静默降级。
  - 新增 `MAX_BACKUP_UPLOAD_MB` 配置项（默认 512）单独放宽备份上传的体积上限，其余接口仍受 `MAX_REQUEST_BODY_MB` 约束；恢复相关端点要求本机回环访问。
  - `backend/data/backups/` **保持不自动清理**：它存放迁移前与恢复前的自动备份，属于用户的安全网，自动删除其中的旧文件风险更大；改为在用户指南、升级文档和 README 中说明该目录会持续累积、可手动清理。
  - 它**不是真的无限**：部分服务商（如 DeepSeek 的 `deepseek-chat`）默认上限就是 4096，勾选后可能比手动设置的值更小。
  - 回退到旧版本前请先把该字段改回 256 以上的具体数值，旧版本会把 `0` 判定为非法并重置整份模型配置。

- Windows `start.cmd` 在找不到可用 Python 3.10+ 时会优先尝试 `winget` 用户级安装，并在失败后使用固定 SHA-256 校验的 Python 官方 x64 安装器兜底；安装完成后会刷新 PATH 并重新探测解释器。
- Windows `start.cmd` 可自动探测并准备 Node.js 20.19.0+/npm：优先复用现有环境或通过 `winget` 用户级安装，失败时使用固定 SHA-256 校验的 Node.js 24.19.0 官方 x64/ARM64 便携包；新增离线启动器回归测试并接入 Windows CI。
- 求职助手会话支持置顶和收藏，置顶会话在全部列表中自动排在最前，侧栏仍可按收藏筛选；新增 `0006_chat_conversation_flags` 数据库迁移并兼容已有 SQLite 数据。
- 置顶或取消置顶不再刷新会话更新时间，取消置顶后会话恢复到置顶前的时间排序位置。
- 聊天图片附件支持即时显示、紧凑缩略图和点击查看大图，避免等待助手响应期间图片暂时消失。

### Fixed

- 收紧 AI 粘贴识别的原文锚点校验：本地规则未识别到的字段不再接受模型返回的无依据内容；补充岗位和个人资料接口在 AI 成功、AI 失败和未配置模型时的回归测试。
- 放宽 Windows 首次启动时 pip/npm 依赖下载的读取超时并增加有限重试；改用关键运行包和 Vite 可执行文件判断依赖完整性，使下载中断后重新运行启动器可以自动修复残缺环境。
- 修复 Windows 一键启动器从资源管理器启动时在项目根目录执行 `npm ci`、误报缺少 `package-lock.json` 的问题；缺少锁文件的旧压缩包会安全回退到一次 `npm install`。
- 修复启动失败清理阶段对空前端进程调用 `Stop-Process` 的异常，并支持通过 `py.exe -3` 检测真实 Python 解释器，避免 Microsoft Store 别名导致误报。
- 探测 Node.js/npm 版本后不再把 PowerShell 5.1 报告的 `$LASTEXITCODE` 残留给调用方：此前该值可能为 `-1`，导致通过 `-Command` 包装调用启动器的环境（如 Windows CI）在启动成功后仍被判为失败。
- 修复 `stop.cmd` 在非 UTC 时区上无法停止服务的问题：进程记录改存 Unix 秒用于启动时间比对，此前记录里的 UTC 时间被按本地时间重新解析，差值恒等于时区偏移，启动器会一直以“记录与进程不符”为由拒绝停止。
- 修复岗位详情抽屉的操作按钮栏：内容不足一屏时按钮浮在正文中间，内容可滚动时下方内容又会从吸底栏与容器底边的缝隙透出。操作栏改用抽屉自身的 footer 插槽常驻底部，正文仍由内容区独立滚动。
- 修复助手会话置顶迁移在 SQLite 外键级联下重建父表、可能清空历史消息的问题；新增迁移回归测试并为会话列表增加稳定的 ID 排序兜底。
- 修复助手历史消息恢复后按更新时间显示异常的问题，保留会话原始更新时间并确保置顶状态变化不会污染排序。
- 前端 CI 使用受控 Vitest worker 数和更宽的异步测试超时，避免共享 runner 资源紧张时误报测试失败。
- 设置页 API Key 回归测试等待异步配置回填完成，避免慢速 CI 环境把初始空值误判为失败。

### Changed

- 粘贴识别采用“AI 优先 + 本地规则兜底”：模型只抽取不可信文本中的已有事实，输出经过 JSON、字段长度和原文锚点校验；未配置模型或调用失败时继续使用原有本地解析结果。

- 一键启动器默认后端端口调整为 8005；手动开发仍使用 8000，并可通过 `-BackendPort` 参数覆盖，相关文档已同步说明。
- 个人资料文本解析器按职责拆分为独立模块，保留 `app.services.profile_text_parser` 原有入口和解析行为，便于后续维护规则而不影响现有数据。
- 岗位粘贴文本解析按职责拆分为 `services/job_parser/`，JD 技能/学历/年限规则拆分为独立模块；保留 `job_text_parser` 与 `jd_parser` 原有导入入口和解析行为。
- 岗位相关性筛选与简历生成按职责拆分为资料上下文、岗位匹配、参考事实、Prompt 预算、JSON 规范化、事实回填、一致性检查和质量门槛模块；保留原有服务入口和导出符号，未改变生成行为。
- 前端共享类型、资料/设置/岗位/简历编辑页面与 CSS 按职责拆分为独立模块；原有类型入口和页面入口保留兼容导出，交互行为与样式契约不变。
- 后端 FastAPI 装配迁移到 `app/application.py`，早期 SQLite 兼容字段集中到 `app/database_compat.py`；`app.main` 继续导出 `app`、生命周期、健康检查和兼容常量，启动命令无需修改。
- 将岗位解析、资料解析、助手搜索、简历质量重试等超长测试按主题拆分为独立测试模块；岗位元数据、英文标题和分隔符场景现独立维护在 `test_job_text_parser_metadata.py`。仅移动测试代码并保留原有断言，新增模块会在 CI 中一起收集。
- Windows 启动器按公共工具、Python/Node 运行时和进程管理拆分为 PowerShell 模块，`Start-ResumeForge.ps1` 保留原参数和启动入口。
## 0.2.0 - 2026-08-21

### Added

- Windows 一键启动与停止入口：`start.cmd` 会在首次运行时准备缺失依赖、校验本地端口和代理连接、启动前后端并打开浏览器；`stop.cmd` 仅停止由启动器记录的 ResumeForge 进程。
- 首次访问使用引导、左侧常驻使用指南入口，以及面向用户的升级/备份/回滚文档。
- 完整用户使用手册，涵盖模型配置、资料维护、岗位导入、简历制作、求职助手、隐私边界与故障处理。
- 开源仓库徽章、社区行为准则和 npm 包仓库元数据。
- 岗位关联的用户手写简历流程，可从个人资料预填并在简历中心区分“AI 生成”和“用户编写”。
- 岗位和简历收藏状态，以及统一展示两类收藏内容的分页收藏夹。
- 岗位需求解读：仅依据招聘原文按需生成结构化总结、带原文证据的需求和通用求职建议。
- AI 求职助手：支持本地会话历史、SSE 流式回复、停止生成、显式关联岗位/简历/脱敏资料、文本附件、兼容模型的图片附件，以及可选的 Bing RSS 联网搜索。
- 简历预览“编辑”模式，可点击或使用键盘选择纸面内容并定位到结构化编辑器字段。
- 设置页为 `temperature`、超时时间和最大输出 Token 增加悬停说明。
- GitHub Actions 跨平台后端检查、前端测试/构建检查和非阻断依赖审计。
- Ruff、覆盖率、统一换行和编辑器基础配置。
- 贡献指南、安全政策、PR 模板和 Dependabot 更新配置。
- 项目根目录 `AGENTS.md`，集中记录渐进式改造、测试、安全、文档和发布维护规则。
- 可独立创建空库且支持降级往返的 Alembic 版本化迁移，以及检测到任一业务表用户数据时的升级前 SQLite 自动备份。
- Alembic `0003_job_additional_info`、`0004_resume_favorite`、`0005_chat_assistant` 和 `0006_chat_conversation_flags` 迁移，兼容补充岗位其他信息、旧简历收藏默认值、助手会话历史表及会话置顶/收藏字段。

### Changed

- 个人资料局部粘贴识别采用合并草稿策略：本次文本未提供的既有字段不会被清空，方便分段补录后再核对保存。
- 删除配置记录只移除记录本身；即使删除当前记录，当前正在使用的模型配置仍会保留，之后可以重新保存为记录。
- 联网搜索先解析受限的候选结果集，再执行招聘相关性过滤，避免无关结果过早占满可用结果位。
- 发布质量清理：移除已确认的无用导入并启用 Ruff `F401` 检查，同时复核忽略规则、文档链接和本地数据文件边界。
- ESLint 忽略 Vite 运行时缓存，避免一键启动后把生成的第三方模块误判为项目源码。
- 移除未被项目使用的 `httpx2` 开发依赖，减少安装和供应链审计范围。
- 完善 README 与架构文档中的简历一对多、生产部署、数据库升级和测试说明。
- 设置页提供明确的“自定义模型（OpenAI 兼容）”预设入口，已有自定义配置可正确回显；预设切换会同步服务标识，无需鉴权的本地兼容服务可留空 API Key，配置记录与既有数据保持兼容。
- 设置页会根据规范化后的官方 Base URL 正确回显历史 DeepSeek 配置，即使旧数据的展示标识仍为 `custom`；自定义模型名称保持不变。
- API Key 框默认隐藏配置记录内部引用，用户点击眼睛时才临时显示当前已保存密钥，隐藏、取消、保存、修改地址或切换配置后立即清除显示值。
- 首次访问和常驻使用指南扩展为五步，并同步当前资料、岗位、简历、收藏和求职助手工作流。
- 临时开发虚拟环境不再纳入版本控制；保留现有 Git 历史，不做破坏性重写。
- 增强跨行业岗位粘贴识别：除互联网岗位外，扩展医疗、教育、制造、零售、物流等常见职位、组织、技能和职业资质表达；支持中英文/编号分区、字段同义词、全角文本、同一行多字段头部、常见英文月份日期，以及 Markdown 或方括号包装的职责/要求标题。规则仍先生成可编辑草稿，不保证覆盖所有网站模板。
- 修复招聘官网紧凑文本的岗位识别：支持从“地点-岗位名(职位编号)公司 + 官网链接”中拆分职位、公司和链接，从压缩元数据行提取地点、校招类型及发布日期，并将“职责要求”正确归入任职要求而非职位描述。
- 岗位新增“其他信息”字段，将福利、团队/公司介绍、职位 ID、部门、工作安排及申请流程与职责、要求分开保存；该字段参与搜索、技能标签、岗位相关性和简历生成上下文。
- 岗位广场将原“更新时间”改为招聘“发布时间”；解析只接受明确发布语义，“更新于/last updated”保留在其他信息，无法确认时显示为空。
- 自行编写简历时在编辑器旁展示只读岗位职责、要求、技能和其他信息，方便边写边对照。
- 增强个人资料粘贴识别：支持 `Employment Dates` 等经历字段、校园组织信号、连续无空行的多段经历，以及无标题的“组织/公司 + 角色 + 日期”结构；异常超长字段会安全截断并提示核对。
- 岗位技能标签统一常见缩写/别名（如 `K8s`、`Golang`、`Fast API`、`Postgre SQL`），能识别紧凑的技术技能列表，同时过滤日期年限、框架后缀、普通英文词和 `closed-loop` 等造成的误判。
- 缓存粘贴解析中的可复用正则，降低大段合法输入的解析延迟。解析仍为本地规则，结果保存前可编辑核对。
- 助手联网搜索会压缩可识别的求职问题、过滤缺少招聘语义的词典/百科/诗歌等结果；找不到直接相关来源时不再展示无关链接。助手回答以安全的轻量 Markdown 呈现标题、重点、列表、代码和 HTTP(S) 链接，提升可读性而不执行模型生成的 HTML。
- 求职助手支持常用 Markdown 表格的响应式展示；回答生成期间提供动态状态提示，消息气泡会按内容自适应宽度，联网参考来源默认收起并可按需展开。
- 优化求职助手的历史对话与输入区：悬停可查看完整对话标题，管理操作按需显示；回答中的 Markdown 或裸 HTTP(S) 链接均可直接访问，关联上下文与开关控件会随屏幕宽度合理换行。
- 求职助手历史记录现以三点菜单收纳重命名和删除操作；超长标题悬停时会滚动展示，同时保留完整标题提示。输入区将关联选择器与开关移至不同层级，减少视觉拥挤。
- 修复求职助手查询“最近招聘信息”时被 Bing RSS 误解为首字检索的问题：互联网企业招聘会使用稳定的短查询，并保留官网常见 `Careers` / `Jobs` 页面；模型不会把未标注日期的摘要误称为刚发布招聘。
- 调整求职助手历史记录标题区宽度；超长标题以右侧渐隐效果收尾，同时在悬停时平滑滚动展示完整内容。
- 移除求职助手历史记录三点操作区的 Ant Design 默认左侧大间距，使标题与操作按钮的布局更紧凑。
- 求职助手的新对话会展示可点击的常用提问提示；回答完成后的会话刷新保留已显示消息，并在绘制前直接定位最新内容，避免先跳回顶部再滚动到底部。

### Removed

- 移除手动添加岗位时通过招聘链接自动读取并填充的实验性功能及其网络抓取、官网适配和模型整理链路；保留本地招聘文本识别与可手动填写的投递链接字段。

### Security

- A4 HTML 导出为固定脚本生成一次性 CSP nonce，并收紧内联脚本策略；简历内容仍按原有转义和本地资源边界渲染。
- 生成简历发送给模型的上下文不再包含照片、姓名、联系方式等身份字段，身份信息仅在本地结果组装时恢复。
- 将岗位 JD 和总结文件内容明确隔离为不可信模型资料，提示模型忽略其中夹带的命令或角色设定。
- 设置接口和配置记录只返回 API Key 脱敏占位符，并校验脱敏引用绑定的 Base URL，避免误发到其他主机。
- API Key 显式查看使用独立 POST 接口，仅接受直接连接本机回环地址的请求并设置禁止缓存响应；普通读取、保存和记录接口继续保持脱敏。
- 求职助手只在用户显式选择时加载岗位、简历或脱敏个人资料，且没有直接写入项目数据的权限；会话历史和模型上下文均有条数与字符预算。
- 助手附件增加扩展名、MIME、UTF-8、图片签名、base64、单文件和总大小校验；历史图片不会在后续轮次重复发送。
- 助手联网只访问固定 Bing RSS 端点，限制响应体和结果数、不跟随结果链接、拒绝 XML DTD/实体并把摘要视为不可信数据；招聘搜索提示优先官网，但仍要求用户核验来源。
- 关闭 HTTP 客户端包含完整 URL 的 INFO 请求摘要，避免助手联网搜索词进入本地日志；保留不含敏感查询参数的业务错误日志。
- 限制 OpenAI 兼容上游仅使用回环 HTTP 或 HTTPS，拒绝凭据、查询参数和片段；统一处理异常响应并限制响应体大小，日志不记录上游正文。
- 增加请求体大小边界、请求 ID 和友好的未处理异常响应。
- 修复 SSE 生成流程的持久化顺序：简历成功落库后才发送 `saved`/`done` 事件。
- 升级 FastAPI/Starlette、React Router、pytest 与 pytest-asyncio 到已修复/兼容版本；Python（含开发依赖）和 npm 生产依赖审计均不再报告已知漏洞。

## 0.1.0 - 2026-08-17

### Added

- FastAPI、React、SQLite 构成的本地简历管理基础版本。
- 岗位、个人资料、AI 定制简历、历史记录、搜索和多格式导出功能。
- OpenAI 兼容大模型配置，以及基于事实的一致性检查。
