# 在线体验构建专用配置。
#
# 用 `vite build --mode demo` 加载本文件（vite 会自动读 `.env.demo`），
# 比依赖 shell 环境变量更可靠：`loadEnv()` 只扫 .env* 文件，不会把
# 任意进程环境变量带进来，所以 `VITE_DEMO_MODE=1 npx vite build` 那种写法
# 在真实构建里是读不到的（`base` 会静默退回 "/"，产物就不能部署到子路径）。
VITE_DEMO_MODE=1
